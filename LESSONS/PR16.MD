🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON16.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON17.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №16](../LESSONS/LESSON16.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №17](../LESSONS/LESSON17.MD) |

---

# 🧪 Практическая работа №16  
## Создание разностной резервной копии

⏱️ **Продолжительность:** 90 минут  
🎯 **Цель работы:**  
Научиться создавать разностные резервные копии, понимать их место в стратегии резервного копирования, анализировать размер и содержимое разностных копий, а также освоить восстановление с использованием полной и разностной копий.

🖥️ **Оборудование:**  
Виртуальная машина с Windows Server / Windows 10, установленными SQL Server Developer Edition и SSMS.

---

## 📋 Общая информация  

| Параметр               | Значение                                                                      |
|------------------------|-------------------------------------------------------------------------------|
| Время выполнения       | 90 минут                                                                      |
| Количество заданий     | 6                                                                             |
| Вариантов              | 30 (по числу студентов в группе)                                             |

---

## 🧠 Глубокое погружение: перед началом

- Вспомните, что такое **экстент** и как SQL Server отслеживает изменения для разностной копии.
- Подумайте, почему разностная копия обычно меньше полной, но может вырасти к концу дня.
- Что произойдёт с цепочкой восстановления, если после разностной копии сделать новую полную?

---

## 📋 Ход выполнения работы

### 🔹 Этап 1. Подготовка среды (10 мин)

1. Запустите **SSMS** и подключитесь к экземпляру SQL Server.
2. Создайте базу данных для экспериментов с уникальным именем:

```sql
CREATE DATABASE BackupDiff_Фамилия;
GO
USE BackupDiff_Фамилия;
GO
```

3. Создайте таблицу и наполните её данными:

```sql
CREATE TABLE dbo.TestData (
    ID INT IDENTITY(1,1) PRIMARY KEY,
    Value INT,
    Comment CHAR(1000) DEFAULT 'A'
);

INSERT INTO dbo.TestData (Value)
SELECT TOP 1000 ROW_NUMBER() OVER (ORDER BY (SELECT NULL))
FROM sys.objects a CROSS JOIN sys.objects b;
GO
```

4. Создайте папку `C:\Backup\ПР16\` для хранения бэкапов.
5. Убедитесь, что модель восстановления – **FULL** (иначе разностные копии не имеют смысла, хотя технически разрешены):

```sql
ALTER DATABASE BackupDiff_Фамилия SET RECOVERY FULL;
```

---

### 🔹 Этап 2. Полная резервная копия (15 мин)

Создайте полную копию, которая будет основой для разностных:

```sql
BACKUP DATABASE [BackupDiff_Фамилия]
TO DISK = N'C:\Backup\ПР16\BackupDiff_Фамилия_Full.bak'
WITH INIT, NAME = N'Полная копия', COMPRESSION, CHECKSUM, STATS = 10;
```

**Скриншот** выполнения с количеством страниц и скоростью.

---

### 🔹 Этап 3. Внесение изменений и разностная копия (20 мин)

1. Добавьте новые данные (имитация работы):

```sql
INSERT INTO dbo.TestData (Value)
SELECT TOP 500 ROW_NUMBER() OVER (ORDER BY (SELECT NULL)) + 1000
FROM sys.objects a CROSS JOIN sys.objects b;
GO
```

2. Создайте **разностную копию**:

```sql
BACKUP DATABASE [BackupDiff_Фамилия]
TO DISK = N'C:\Backup\ПР16\BackupDiff_Фамиília_Diff.bak'
WITH DIFFERENTIAL, NAME = N'Разностная после 500 строк',
     COMPRESSION, CHECKSUM, STATS = 10;
```

3. Запишите количество обработанных страниц (из сообщения). Сравните с количеством страниц в полной копии.

---

### 🔹 Этап 4. Ещё изменения и вторая разностная (15 мин)

1. Добавьте ещё данные:

```sql
INSERT INTO dbo.TestData (Value)
SELECT TOP 1000 ROW_NUMBER() OVER (ORDER BY (SELECT NULL)) + 1500
FROM sys.objects a CROSS JOIN sys.objects b;
GO
```

2. Создайте **вторую разностную копию** (в тот же файл или новый – на выбор, но лучше в тот же с NOINIT):

```sql
BACKUP DATABASE [BackupDiff_Фамилия]
TO DISK = N'C:\Backup\ПР16\BackupDiff_Фамилия_Diff.bak'
WITH DIFFERENTIAL, NOINIT, NAME = N'Разностная после ещё 1000 строк',
     COMPRESSION, CHECKSUM, STATS = 10;
```

3. Снова зафиксируйте количество страниц.

---

### 🔹 Этап 5. Анализ содержимого разностных копий (20 мин)

1. Выполните `RESTORE HEADERONLY` для файла с разностными копиями:

```sql
RESTORE HEADERONLY FROM DISK = N'C:\Backup\ПР16\BackupDiff_Фамилия_Diff.bak';
```

Обратите внимание на поля:
- **BackupType** = 5 (разностная)
- **Position** – номер набора в файле
- **BackupSize**, **CompressedBackupSize**
- **DatabaseBackupLSN** – LSN полной копии, с которой сравнивается разностная

2. Запросите историю из msdb:

```sql
SELECT 
    bs.database_name,
    bs.backup_start_date,
    CASE bs.type WHEN 'D' THEN 'Полная' WHEN 'I' THEN 'Разностная' END AS Тип,
    bs.backup_size/1048576 AS SizeMB,
    bs.first_lsn, bs.last_lsn, bs.database_backup_lsn,
    bmf.physical_device_name
FROM msdb.dbo.backupset bs
JOIN msdb.dbo.backupmediafamily bmf ON bs.media_set_id = bmf.media_set_id
WHERE bs.database_name = 'BackupDiff_Фамилия'
ORDER BY bs.backup_start_date;
```

3. **Вопрос:** Как по LSN определить, что вторая разностная копия включает изменения, накопившиеся после первой?

---

### 🔹 Этап 6. Проверка восстановления (10 мин)

Попробуйте восстановить базу, используя полную и одну из разностных копий:

```sql
RESTORE DATABASE [BackupDiff_Фамилия_Restored]
FROM DISK = N'C:\Backup\ПР16\BackupDiff_Фамилия_Full.bak'
WITH REPLACE, NORECOVERY,
     MOVE 'BackupDiff_Фамилия' TO 'C:\Data\BackupDiff_Фамилия_Restored.mdf',
     MOVE 'BackupDiff_Фамилия_log' TO 'C:\Data\BackupDiff_Фамилия_Restored.ldf';

RESTORE DATABASE [BackupDiff_Фамилия_Restored]
FROM DISK = N'C:\Backup\ПР16\BackupDiff_Фамилия_Diff.bak'
WITH FILE = 1, RECOVERY;  -- FILE = 1 для первой разностной
```

Проверьте, что восстановленная база содержит все данные до второго добавления (если использовали первую разностную) или до последнего (если вторую).

**Скриншот** успешного восстановления.

---

## 📊 Итоговая таблица наблюдений

| Тип копии | Размер файла (МБ) | Количество страниц | DatabaseBackupLSN | BackupStartDate |
|-----------|-------------------|--------------------|-------------------|-----------------|
| Полная    |                   |                    |                   |                 |
| Разностная 1 |                |                    |                   |                 |
| Разностная 2 |                |                    |                   |                 |

---

## ❓ Контрольные вопросы (с углублением)

1. Как SQL Server определяет, какие экстенты включить в разностную копию?
2. Почему размер второй разностной копии может быть больше первой, даже если добавили меньше данных?
3. Что произойдёт с разностными копиями после создания новой полной?
4. Можно ли восстановить базу, имея только разностную копию, без полной?
5. Какой LSN в заголовке разностной копии указывает на соответствующую полную?
6. Почему разностная копия не поддерживает Point-in-Time Recovery?
7. Как часто нужно делать разностные копии, чтобы уложиться в RTO = 2 часа, если полная копия восстанавливается 1 час?
8. Что будет, если во время создания разностной копии продолжать изменять данные?
9. Как проверить целостность разностной копии?
10. В чём разница между разностной копией уровня базы данных и разностной копией файловой группы?

---

## 📝 Вывод

- Чему научились о разностных копиях?
- Как изменялся размер копий по мере добавления данных?
- Как LSN помогают понять цепочку зависимостей?
- Какие трудности возникли и как их преодолели?

---

## 📋 Требования к оформлению отчёта

**Файл:** `Отчёт_Фамилия_Группа_ПР16.docx`

**Структура:**
1. Титульный лист.
2. Цель работы.
3. Ход выполнения (этапы 1–6) с **скриншотами**:
   - Этап 2: полная копия (сообщение)
   - Этап 3: первая разностная
   - Этап 4: вторая разностная
   - Этап 5: результаты `RESTORE HEADERONLY` и запрос msdb
   - Этап 6: восстановление (команды и результат)
4. Таблица наблюдений.
5. Ответы на вопросы.
6. Вывод.

**Требования к скриншотам:**
- Виден текст команды и результат.
- В заголовке окна – имя базы.
- Подпись под каждым скриншотом.

---

## 📊 Критерии оценивания 

| Критерий | «5» (отлично) | «4» (хорошо) | «3» (удовл.) |
|----------|----------------|---------------|---------------|
| **Стартовый скрипт** | БД с фамилией, таблицы, FULL модель, без ошибок | БД создана, но мелкие недочёты | Ошибки или не выполнен |
| **Полная копия** | С опциями, скриншот с количеством страниц | Копия есть, но нет параметров | Не выполнено |
| **Разностная копия 1** | Создана, зафиксирован размер, есть скриншот | Создана, но нет анализа | Не выполнено |
| **Разностная копия 2** | Создана с NOINIT, виден рост, скриншот | Создана, но нет сравнения | Не выполнено |
| **RESTORE HEADERONLY** | Анализ всех полей, скриншот | Запрос есть, анализ поверхностный | Не выполнено |
| **Запрос к msdb** | Анализ LSN и зависимостей, скриншот | Запрос есть, анализ неполный | Не выполнено |
| **Восстановление** | Успешно, проверка данных, скриншот | Выполнено, но нет проверки | Ошибки при восстановлении |
| **Таблица наблюдений** | Заполнена полностью, цифры со скриншотами | 1-2 пропуска | Отсутствует |
| **Ответы на вопросы** | Полные, с терминами, понимание LSN | 2-3 неполных ответа | Большинства нет |
| **Оформление отчёта** | Структура, подписи, вывод | Мелкие нарушения | Неструктурирован |

---

## 📊 Шкала оценки

| Баллов | Оценка |
|--------|--------|
| 4.6 – 5.0 | 5 |
| 3.6 – 4.5 | 4 |
| 2.6 – 3.5 | 3 |
| < 2.6 | 2 |

**Бонусы:** +0.5 за дополнительные исследования  
**Штрафы:** -1 за отсутствие скриншотов, -0.5 за нечитаемые, -1 за отсутствие вывода

❗ **Без таблицы наблюдений — оценка не выше «4»**

---

## 📤 Сдача работы

1. Файл `.docx` загрузить в [Google-форму 237](https://forms.gle/hazYv7aM2L71oC3s6).
2. Указать ФИО, группу.
3. Срок: до **23:59 4 марта 2026 г.**

---

📜 **Лицензия:** CC BY-NC-SA 4.0  
👨‍🏫 **Автор:** Руслан Ринатович Сафиулин  
📅 **Дата:** 24.02.2026

🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON16.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON17.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №16](../LESSONS/LESSON16.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №17](../LESSONS/LESSON17.MD) |
