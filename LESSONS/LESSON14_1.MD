### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR12.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR13_1.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №12](../LESSONS/PR12.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №13](../LESSONS/PR13_1.MD) |

---

# **🎓 Лекция 13.1. Основы хранимых процедур: создание и простые примеры**
⏱️ **Продолжительность:** 90 минут (теория 45 мин + практика 45 мин)

### **📋 План занятия:**
1. **Введение** (10 мин) — что такое процедуры и зачем они нужны
2. **Синтаксис создания** (15 мин) — от простого к сложному
3. **Команда PRINT** (10 мин) — вывод информации
4. **Практические примеры** (15 мин) — полезные процедуры
5. **Управление процедурами** (10 мин) — создание, изменение, удаление
6. **Практическая работа** (30 мин) — создание своих процедур

---

## **📖 Часть 1. Что такое хранимые процедуры? (10 минут)**

### **🔹 Формальное и простое определение**

**Формальное определение:**  
Хранимая процедура (Stored Procedure) — это предварительно скомпилированный набор команд SQL, хранящийся в базе данных и выполняемый как единый блок.

**Простое определение для студентов:**  
Это как "макрос" или "сохраненный скрипт" в SQL Server. Вы пишете команды один раз, сохраняете под именем, а потом запускаете одной командой.

### **🔹 Аналогия из жизни**

**Ситуация:** Вы каждый день готовите сложный завтрак по рецепту.  
**Без процедуры:** Каждое утро ищете рецепт, достаете ингредиенты, готовите.  
**С процедурой:** У вас есть готовый "полуфабрикат" — нужно только разогреть.

**В SQL это выглядит так:**
```sql
-- БЕЗ процедуры (каждый день одно и то же):
SELECT name FROM sys.databases;
SELECT COUNT(*) FROM sys.tables;
EXEC sp_spaceused;

-- С процедурой (создали один раз):
CREATE PROCEDURE usp_DailyCheck AS ...

-- И теперь каждый день:
EXEC usp_DailyCheck;  -- Всё выполняется автоматически!
```

### **🔹 Зачем администратору хранимые процедуры? (таблица преимуществ)**

| Ситуация | Без процедуры | С процедурой | Выигрыш |
|----------|---------------|--------------|---------|
| **Ежедневная проверка** | Каждый день пишете 10 запросов | `EXEC usp_DailyCheck` | **Экономия 90% времени** |
| **Диагностика проблем** | Ищете скрипты в заметках, копируете, исправляете | `EXEC usp_Diagnose @ProblemType = 'Slow'` | **Быстрее в 5 раз** |
| **Отчёт для руководства** | Собираете данные из разных мест, оформляете в Excel | `EXEC rpt_WeeklyReport` | **Автоматизация** |
| **Новичкам** | Долго объяснять, что и как делать | `EXEC usp_StandardCheck` | **Стандартизация** |

### **🔹 Реальный пример из практики администратора**

**Проблема:** Утром нужно проверить:
1. Все ли базы данных работают
2. Хватает ли места на дисках
3. Есть ли блокировки
4. Работают ли резервные копии

**Решение с процедурой:**
```sql
-- Утром вместо кучи запросов:
EXEC adm_MorningCheck;

-- И получаем готовый отчёт:
[09:00] Начало проверки...
[09:00] Базы данных: 15/15 работают ✓
[09:00] Дисковое пространство: 45 ГБ свободно ✓
[09:00] Блокировок: нет ✓
[09:00] Резервные копии: все свежие ✓
[09:00] Проверка завершена за 2 секунды
```

---

## **📖 Часть 2. Базовый синтаксис создания (15 минут)**

### **🔹 Структура простой процедуры — разбираем по косточкам**

```sql
-- Разберём каждую строку:
CREATE PROCEDURE имя_процедуры  -- 1. СОЗДАЁМ новую процедуру
AS                              -- 2. НАЧИНАЕМ описание, что она делает
BEGIN                           -- 3. ОТКРЫВАЕМ блок команд
    -- Здесь пишем команды SQL
    -- Можно любые: SELECT, INSERT, UPDATE, DELETE
    -- Можно вызывать другие процедуры
    -- Можно использовать PRINT для сообщений
END                            -- 4. ЗАКРЫВАЕМ блок команд
GO                             -- 5. ЗАВЕРШАЕМ создание (важно!)
```

**🔹 Почему GO так важен?**  
`GO` — это не команда T-SQL, а команда **SQL Server Management Studio (SSMS)**. Она говорит: "Всё, что было до этого — отдельный пакет, выполни его". Без `GO` SQL Server попытается выполнить `CREATE PROCEDURE` вместе со следующим кодом, что вызовет ошибку.

### **🔹 Наша первая процедура — подробный разбор**

```sql
-- Создаём процедуру с именем usp_HelloWorld
CREATE PROCEDURE usp_HelloWorld  -- usp_ = User Stored Procedure
AS
BEGIN
    -- Команда PRINT выводит сообщения
    PRINT 'Привет, мир! Это моя первая процедура!';
    
    -- Показываем текущую дату
    PRINT 'Сегодня: ' + CONVERT(VARCHAR, GETDATE(), 104);  -- 104 = dd.mm.yyyy
    
    -- Команда SELECT возвращает данные
    SELECT 
        'Ура! Я создал процедуру!' AS Сообщение,
        GETDATE() AS ВремяСоздания,
        @@SERVERNAME AS ИмяСервера;
END
GO  -- Не забываем!

-- Теперь запускаем процедуру
EXEC usp_HelloWorld;  -- EXEC = EXECUTE (выполнить)
```

**🔹 Что происходит при выполнении:**
1. **Компиляция** — SQL Server проверяет синтаксис, оптимизирует запросы
2. **Сохранение** — процедура сохраняется в системных таблицах базы
3. **Выполнение** — когда вызываем EXEC, выполняются все команды внутри

### **🔹 Разница между процедурой и скриптом**

| Критерий | Скрипт (.sql файл) | Хранимая процедура |
|----------|-------------------|-------------------|
| **Хранение** | На диске (файл) | В базе данных |
| **Выполнение** | Открываем в SSMS, F5 | `EXEC имя_процедуры` |
| **Безопасность** | Права на файл | Права в SQL Server |
| **Скорость** | Компилируется каждый раз | Компилируется один раз |
| **Использование** | Разовое | Многократное |

---

## **📖 Часть 3. Команда PRINT — наш лучший помощник (10 минут)**

### **🔹 Что такое PRINT и зачем он нужен?**

**PRINT** — это команда для вывода текстовых сообщений. Представьте, что это:
- `Console.WriteLine()` в программировании
- `echo` в командной строке
- Сообщения в логе выполнения

### **🔹 Синтаксис и примеры**

```sql
-- 1. Простой текст
PRINT 'Начало выполнения процедуры';

-- 2. С датой и временем
PRINT 'Время запуска: ' + CONVERT(VARCHAR, GETDATE(), 120);  -- 120 = yyyy-mm-dd hh:mi:ss

-- 3. С переменными (о них подробнее в следующей лекции)
DECLARE @ServerName NVARCHAR(100) = @@SERVERNAME;
PRINT 'Имя сервера: ' + @ServerName;

-- 4. С числами (нужно преобразовать в текст)
DECLARE @Count INT = 42;
PRINT 'Количество: ' + CAST(@Count AS VARCHAR(10));  -- CAST преобразует число в текст

-- 5. Разделители для красоты
PRINT '========================================';
PRINT '         ОТЧЁТ ПО СЕРВЕРУ';
PRINT '========================================';
PRINT '';  -- Пустая строка для разделения

-- 6. Многострочные сообщения
PRINT 'Шаг 1: Проверка баз данных...';
PRINT 'Шаг 2: Проверка подключений...';
PRINT 'Шаг 3: Проверка места на дисках...';
```

### **🔹 Где отображается результат PRINT?**

1. **В SSMS (SQL Server Management Studio):**
   - Вкладка **"Сообщения" (Messages)**
   - После выполнения процедуры
   - Отдельно от результатов SELECT

   ![SSMS Messages Tab](https://via.placeholder.com/600x200?text=Вкладка+Messages+в+SSMS)

2. **В приложениях:**
   - В потоке сообщений SQL Server
   - Если приложение умеет их обрабатывать

3. **В логах:**
   - Можно настроить запись в системный журнал
   - Для отладки сложных процедур

### **🔹 PRINT vs SELECT: когда что использовать?**

| Задача | Что использовать | Пример | Почему |
|--------|-----------------|--------|--------|
| **Информационные сообщения** | PRINT | `PRINT 'Начало проверки'` | Не мешает данным |
| **Прогресс выполнения** | PRINT | `PRINT 'Обработано 50%'` | Виден ход работы |
| **Возврат данных пользователю** | SELECT | `SELECT * FROM таблица` | Удобный табличный вид |
| **Отладка переменных** | PRINT | `PRINT 'Значение: ' + @var` | Быстро и наглядно |
| **Формирование отчёта** | Оба | PRINT для заголовков, SELECT для данных | И красиво, и информативно |

### **🔹 Ограничения PRINT**

```sql
-- ОШИБКА: PRINT не работает с результатами SELECT
PRINT SELECT * FROM sys.databases;  -- Так нельзя!

-- Правильно: сначала в переменную, потом PRINT
DECLARE @Count INT;
SELECT @Count = COUNT(*) FROM sys.databases;
PRINT 'Баз данных: ' + CAST(@Count AS VARCHAR);

-- Ограничение длины: максимум 8000 символов
DECLARE @LongText NVARCHAR(MAX) = REPLICATE('A', 9000);  -- 9000 символов
PRINT @LongText;  -- Обрежется до 8000 символов
```

---

## **📖 Часть 4. Правила именования (10 минут)**

### **🔹 Технические ограничения**

```sql
-- 1. Длина имени: максимум 128 символов
CREATE PROCEDURE usp_ОченьДлинноеИмяПроцедурыКотораяДелаетМногоРазныхВещейИДажеЕщёЧтотоТамДополнительное AS ...  -- Ошибка!

-- 2. Допустимые символы:
--    Буквы (A-Z, a-z, русские тоже)
--    Цифры (0-9), но не в начале
--    Символы: _ (подчёркивание), @, #, $
CREATE PROCEDURE usp_Check_DB_2024 AS ...  -- OK
CREATE PROCEDURE usp_Check@Server AS ...    -- OK (но лучше избегать)
CREATE PROCEDURE 1stProcedure AS ...        -- ОШИБКА: начинается с цифры

-- 3. Регистр букв:
--    SQL Server по умолчанию НЕ чувствителен к регистру
CREATE PROCEDURE usp_MyProc AS ...
EXEC USP_MYPROC;  -- Сработает (но так делать не стоит)
```

### **🔹 Префиксы: язык профессионалов**

**Зачем нужны префиксы?** Чтобы сразу понимать, что это за процедура.

| Префикс | Что означает | Пример | Когда использовать | Почему важно |
|---------|--------------|--------|-------------------|--------------|
| **usp_** | User Stored Procedure | `usp_GetUsers` | Все свои процедуры | Стандарт де-факто |
| **sp_** | System Procedure | `sp_helpdb` | Только встроенные | Microsoft использует |
| **adm_** | Administrative | `adm_BackupAll` | Админ-инструменты | Понятно назначение |
| **rpt_** | Report | `rpt_MonthlySales` | Процедуры отчётов | Отдельная категория |
| **util_** | Utility | `util_CleanTemp` | Вспомогательные | Утилиты, не бизнес-логика |
| **app_** | Application | `app_RegisterUser` | Бизнес-логика приложения | Для разработчиков |

### **🔹 Почему НЕЛЬЗЯ использовать sp_ для своих процедур?**

**Миф:** "sp_ значит Stored Procedure, буду использовать для своих процедур"  
**Правда:** Это большая ошибка! Вот почему:

```sql
-- ПЛОХО:
CREATE PROCEDURE sp_MyCheck AS ...  -- sp_ для своей процедуры

-- Что происходит при вызове:
EXEC sp_MyCheck;
-- 1. SQL Server ищет в системной базе MASTER
-- 2. Не находит там
-- 3. Ищет в текущей базе
-- 4. Находит и выполняет

-- Результат: потеря производительности + путаница

-- ХОРОШО:
CREATE PROCEDURE usp_MyCheck AS ...  -- usp_ для своих процедур

-- При вызове:
EXEC usp_MyCheck;
-- 1. SQL Server сразу ищет в текущей базе
-- 2. Быстро находит и выполняет

-- Результат: быстрее + понятнее
```

### **🔹 Рекомендации по именованию**

```sql
-- ХОРОШИЕ имена (понятные, информативные):
CREATE PROCEDURE usp_GetDatabaseSizes
CREATE PROCEDURE adm_DailyBackupCheck
CREATE PROCEDURE rpt_WeeklySalesReport
CREATE PROCEDURE util_CleanOldData

-- ПЛОХИЕ имена (непонятные, бессмысленные):
CREATE PROCEDURE proc1                -- Что делает?
CREATE PROCEDURE sp_check             -- sp_ + непонятно что
CREATE PROCEDURE xyz                  -- Совершенно непонятно
CREATE PROCEDURE GetDataFromTableXYZ  -- Слишком длинное, нет префикса

-- ЗОЛОТАЯ СЕРЕДИНА:
CREATE PROCEDURE usp_GetUserOrders    -- Префикс + что делает
CREATE PROCEDURE adm_CheckDiskSpace   -- Префикс + что проверяет
CREATE PROCEDURE rpt_DailyActivity    -- Префикс + период + что
```

---

## **📖 Часть 5. Создание полезных процедур (15 минут)**

### **🔹 Процедура для проверки сервера — с подробными комментариями**

```sql
-- Создаём в базе master, чтобы была доступна везде
USE master;
GO

CREATE PROCEDURE usp_ServerInfo
AS
BEGIN
    -- 1. ЗАГОЛОВОК отчёта
    PRINT '========================================';
    PRINT '     ИНФОРМАЦИЯ О СЕРВЕРЕ SQL SERVER';
    PRINT '========================================';
    PRINT '';  -- Пустая строка для читаемости
    
    -- 2. ОСНОВНАЯ ИНФОРМАЦИЯ
    PRINT '--- ОСНОВНАЯ ИНФОРМАЦИЯ ---';
    
    -- 2.1. Версия SQL Server
    PRINT 'Версия SQL Server:';
    SELECT @@VERSION AS ServerVersion;
    PRINT '';  -- Разделитель
    
    -- 2.2. Имя сервера и службы
    PRINT 'Имя сервера: ' + @@SERVERNAME;
    PRINT 'Имя службы: ' + @@SERVICENAME;
    PRINT '';
    
    -- 2.3. Текущая база данных
    PRINT 'Текущая база данных:';
    SELECT DB_NAME() AS CurrentDatabase;
    PRINT '';
    
    -- 3. БАЗЫ ДАННЫХ
    PRINT '--- БАЗЫ ДАННЫХ ---';
    
    -- 3.1. Все базы данных
    PRINT 'Все базы данных на сервере:';
    SELECT 
        name AS ИмяБазы,
        state_desc AS Статус,
        create_date AS ДатаСоздания,
        recovery_model_desc AS МодельВосстановления
    FROM sys.databases 
    ORDER BY name;
    PRINT '';
    
    -- 3.2. Статистика
    DECLARE @TotalDBs INT, @OnlineDBs INT, @OfflineDBs INT;
    
    SELECT 
        @TotalDBs = COUNT(*),
        @OnlineDBs = SUM(CASE WHEN state = 0 THEN 1 ELSE 0 END),
        @OfflineDBs = SUM(CASE WHEN state != 0 THEN 1 ELSE 0 END)
    FROM sys.databases;
    
    PRINT 'СТАТИСТИКА:';
    PRINT '  Всего баз: ' + CAST(@TotalDBs AS VARCHAR);
    PRINT '  В сети: ' + CAST(@OnlineDBs AS VARCHAR);
    PRINT '  Не в сети: ' + CAST(@OfflineDBs AS VARCHAR);
    PRINT '';
    
    -- 4. ЗАВЕРШЕНИЕ
    PRINT '========================================';
    PRINT 'ОТЧЁТ СФОРМИРОВАН: ' + CONVERT(VARCHAR, GETDATE(), 120);
    PRINT '========================================';
END
GO

-- Тестируем процедуру
EXEC usp_ServerInfo;
```

### **🔹 Процедура для мониторинга подключений**

```sql
CREATE PROCEDURE usp_ConnectionMonitor
AS
BEGIN
    -- Заголовок
    PRINT 'МОНИТОРИНГ ПОДКЛЮЧЕНИЙ К SQL SERVER';
    PRINT 'Время проверки: ' + CONVERT(VARCHAR, GETDATE(), 120);
    PRINT '----------------------------------------';
    PRINT '';
    
    -- 1. Текущие подключения
    PRINT 'ТЕКУЩИЕ ПОДКЛЮЧЕНИЯ:';
    SELECT 
        session_id AS 'ID Сессии',
        login_name AS 'Пользователь',
        host_name AS 'Компьютер',
        program_name AS 'Программа',
        status AS 'Статус',
        cpu_time AS 'CPU (мс)',
        memory_usage AS 'Память'
    FROM sys.dm_exec_sessions
    WHERE is_user_process = 1  -- Только пользовательские процессы
    ORDER BY session_id;
    
    -- 2. Статистика
    PRINT '';
    PRINT 'СТАТИСТИКА:';
    
    DECLARE @TotalConnections INT;
    DECLARE @ActiveConnections INT;
    DECLARE @MostActiveUser NVARCHAR(100);
    
    -- Общее количество
    SELECT @TotalConnections = COUNT(*)
    FROM sys.dm_exec_sessions
    WHERE is_user_process = 1;
    
    -- Активные (выполняющие запросы)
    SELECT @ActiveConnections = COUNT(DISTINCT session_id)
    FROM sys.dm_exec_requests
    WHERE session_id > 50;
    
    -- Самый активный пользователь
    SELECT TOP 1 @MostActiveUser = login_name
    FROM sys.dm_exec_sessions
    WHERE is_user_process = 1
    ORDER BY cpu_time DESC;
    
    PRINT '  Всего подключений: ' + CAST(@TotalConnections AS VARCHAR);
    PRINT '  Активных сессий: ' + CAST(@ActiveConnections AS VARCHAR);
    PRINT '  Самый активный: ' + ISNULL(@MostActiveUser, 'не определен');
    
    -- 3. Рекомендации
    PRINT '';
    PRINT 'РЕКОМЕНДАЦИИ:';
    
    IF @TotalConnections > 100
        PRINT '  ВНИМАНИЕ: Большое количество подключений (>100)';
    
    IF @ActiveConnections > 50
        PRINT '  ВНИМАНИЕ: Много активных сессий (>50)';
    
    IF @TotalConnections < 10 AND @ActiveConnections < 5
        PRINT '  Сервер недогружен';
    
    PRINT '';
    PRINT 'Мониторинг завершён.';
END
GO
```

### **🔹 Процедура для быстрой диагностики**

```sql
CREATE PROCEDURE usp_QuickDiagnostics
AS
BEGIN
    PRINT '🚀 БЫСТРАЯ ДИАГНОСТИКА СЕРВЕРА';
    PRINT '===============================';
    PRINT '';
    
    -- Проверяем несколько ключевых параметров за раз
    
    -- 1. Базы данных
    PRINT '1. БАЗЫ ДАННЫХ:';
    IF EXISTS (SELECT 1 FROM sys.databases WHERE state != 0)
        PRINT '   ⚠️  Есть базы не в сети!';
    ELSE
        PRINT '   ✅ Все базы в сети';
    
    -- 2. Процессор
    PRINT '';
    PRINT '2. НАГРУЗКА CPU:';
    DECLARE @BusySchedulers INT;
    SELECT @BusySchedulers = COUNT(*) 
    FROM sys.dm_os_schedulers 
    WHERE status = 'VISIBLE ONLINE' AND runnable_tasks_count > 0;
    
    IF @BusySchedulers > 2
        PRINT '   ⚠️  Высокая нагрузка на CPU';
    ELSE
        PRINT '   ✅ Нагрузка CPU в норме';
    
    -- 3. Память
    PRINT '';
    PRINT '3. ПАМЯТЬ:';
    DECLARE @MemoryGrants INT;
    SELECT @MemoryGrants = COUNT(*) 
    FROM sys.dm_exec_query_memory_grants;
    
    IF @MemoryGrants > 10
        PRINT '   ⚠️  Много запросов с выделением памяти';
    ELSE
        PRINT '   ✅ Память в норме';
    
    -- Итог
    PRINT '';
    PRINT '===============================';
    PRINT 'Диагностика завершена за ' + 
          CAST(DATEDIFF(MILLISECOND, GETDATE(), GETDATE()) AS VARCHAR) + ' мс';
END
GO
```

---

## **📖 Часть 6. Управление процедурами (10 минут)**

### **🔹 Полный цикл жизни процедуры**

```sql
-- 1. СОЗДАНИЕ (CREATE)
CREATE PROCEDURE usp_TestProcedure
AS
BEGIN
    PRINT 'Версия 1.0';
    SELECT 'Первая версия' AS Info;
END
GO

-- 2. ТЕСТИРОВАНИЕ
EXEC usp_TestProcedure;
-- Результат: Версия 1.0

-- 3. ИЗМЕНЕНИЕ (ALTER)
ALTER PROCEDURE usp_TestProcedure
AS
BEGIN
    PRINT 'Версия 2.0 - улучшенная!';
    SELECT 
        'Вторая версия' AS Info,
        GETDATE() AS CurrentTime;
END
GO

-- 4. ТЕСТИРОВАНИЕ изменений
EXEC usp_TestProcedure;
-- Результат: Версия 2.0 - улучшенная!

-- 5. ПРОСМОТР кода процедуры
EXEC sp_helptext 'usp_TestProcedure';
-- Показывает текст процедуры

-- 6. УДАЛЕНИЕ (DROP)
DROP PROCEDURE usp_TestProcedure;

-- 7. ПРОВЕРКА удаления
EXEC sp_helptext 'usp_TestProcedure';
-- Сообщение: 'Хранимая процедура не найдена.'
```

### **🔹 Безопасное управление процедурами**

```sql
-- БЕЗОПАСНОЕ СОЗДАНИЕ (если не существует)
IF OBJECT_ID('usp_SafeProcedure', 'P') IS NULL
BEGIN
    PRINT 'Создаю процедуру usp_SafeProcedure...';
    CREATE PROCEDURE usp_SafeProcedure
    AS
    BEGIN
        PRINT 'Безопасно созданная процедура';
    END
    PRINT 'Процедура создана успешно!';
END
ELSE
BEGIN
    PRINT 'Процедура usp_SafeProcedure уже существует';
END
GO

-- БЕЗОПАСНОЕ УДАЛЕНИЕ (если существует)
IF OBJECT_ID('usp_SafeProcedure', 'P') IS NOT NULL
BEGIN
    PRINT 'Удаляю процедуру usp_SafeProcedure...';
    DROP PROCEDURE usp_SafeProcedure;
    PRINT 'Процедура удалена успешно!';
END
ELSE
BEGIN
    PRINT 'Процедура usp_SafeProcedure не существует';
END
GO

-- CREATE OR ALTER (SQL Server 2016 SP1 и выше)
CREATE OR ALTER PROCEDURE usp_ModernProcedure
AS
BEGIN
    PRINT 'Всегда актуальная версия!';
END
GO
-- Если процедура есть - изменит, если нет - создаст
```

### **🔹 Просмотр информации о процедурах**

```sql
-- 1. Все процедуры в базе
SELECT 
    name AS ProcedureName,
    type_desc AS Type,
    create_date AS Created,
    modify_date AS LastModified
FROM sys.procedures
ORDER BY name;

-- 2. Текст конкретной процедуры
EXEC sp_helptext 'usp_ServerInfo';

-- 3. Через системную функцию
SELECT OBJECT_DEFINITION(OBJECT_ID('usp_ServerInfo'));

-- 4. Подробная информация о процедуре
EXEC sp_help 'usp_ServerInfo';

-- 5. Зависимости процедуры (что она использует)
EXEC sp_depends 'usp_ServerInfo';
```

---

## **📖 Часть 7. Где создавать процедуры? (5 минут)**

### **🔹 Два основных места и их особенности**

```sql
-- ВАРИАНТ 1: В базе MASTER (глобальный доступ)
USE master;
GO

CREATE PROCEDURE usp_GlobalTool
AS
BEGIN
    PRINT 'Эта процедура доступна из ЛЮБОЙ базы данных!';
    PRINT 'Текущая база: ' + DB_NAME();
END
GO

-- Вызов из любой базы:
USE AdventureWorks;
EXEC usp_GlobalTool;  -- Сработает!
-- Результат: Текущая база: AdventureWorks

USE master;
EXEC usp_GlobalTool;  -- Тоже сработает!
-- Результат: Текущая база: master

-- ВАРИАНТ 2: В конкретной базе (локальный доступ)
USE MyDatabase;
GO

CREATE PROCEDURE usp_LocalTool
AS
BEGIN
    PRINT 'Эта процедура доступна ТОЛЬКО в базе MyDatabase';
    PRINT 'Работаю с таблицами только этой базы';
    
    SELECT * FROM LocalTable;  -- Таблица из MyDatabase
END
GO

-- Попытка вызова из другой базы:
USE AnotherDatabase;
EXEC usp_LocalTool;  -- ОШИБКА! Процедура не найдена
```

### **🔹 Что выбрать для админ-процедур?**

| Тип процедуры | Где создавать | Почему | Пример |
|--------------|---------------|--------|--------|
| **Админ-инструменты** | `master` | Доступны везде | `usp_CheckAllDatabases` |
| **Мониторинг сервера** | `master` | Нужны для всего сервера | `usp_ServerHealth` |
| **Резервное копирование** | `msdb` или `master` | Работа с заданиями | `adm_BackupMonitor` |
| **Бизнес-логика** | Бизнес-база | Только для этой БД | `app_ProcessOrder` |
| **Отчёты** | Бизнес-база | Данные из конкретной БД | `rpt_SalesReport` |

**Рекомендация для курса:**  
Все админ-процедуры создавайте в `master` — они будут доступны из любой базы, что удобно для обучения и работы.

---

## **🎯 Практическое задание на лекции (30 минут)**

### **Задание 1: Процедура "Моя визитка"**
**Цель:** Научиться использовать PRINT для вывода текста

**Задача:** Создать процедуру `usp_MyCard`, которая выводит:
1. Ваше имя и фамилию
2. Номер вашей группы
3. Текущую дату
4. Сообщение "Моя первая процедура!"

**Пример результата:**
```
=== МОЯ ВИЗИТКА ===
Иван Петров
Группа: ИС-23
Дата: 18.12.2024
Моя первая процедура!
```

**Подсказка:**
```sql
PRINT '=== МОЯ ВИЗИТКА ===';
PRINT 'Иван Петров';
-- Добавьте остальные строки
```

---

### **Задание 2: Процедура "Информация о сервере"**
**Цель:** Использовать системные функции

**Задача:** Создать процедуру `usp_ServerBasicInfo`, которая показывает:
1. Имя сервера (`@@SERVERNAME`)
2. Версию SQL Server (`@@VERSION`)
3. Имя текущей базы данных (`DB_NAME()`)
4. Имя текущего пользователя (`SUSER_NAME()`)

**Пример результата:**
```
Сервер: SQLSERVER01
Версия: Microsoft SQL Server 2019...
Текущая база: master
Пользователь: sa
```

**Подсказка:**
```sql
SELECT @@SERVERNAME AS 'Имя сервера';
SELECT @@VERSION AS 'Версия SQL Server';
-- Добавьте остальные SELECT
```

---

### **Задание 3: Процедура "Список баз данных"**
**Цель:** Работать с системными таблицами

**Задача:** Создать процедуру `usp_ShowDatabases`, которая:
1. Показывает все базы данных на сервере
2. Сортирует их по имени
3. Показывает только имя и дату создания
4. Добавляет заголовок через PRINT

**Пример результата:**
```
СПИСОК БАЗ ДАННЫХ:
master    2009-04-08
model     2009-04-08
msdb      2009-04-08
tempdb    (текущая дата)
```

**Подсказка:**
```sql
PRINT 'СПИСОК БАЗ ДАННЫХ:';
SELECT name, create_date FROM sys.databases ORDER BY name;
```

---

### **Задание 4: Процедура "Кто подключен"**
**Цель:** Использовать системные представления

**Задача:** Создать процедуру `usp_SimpleConnections`, которая:
1. Показывает ID сессии и имя пользователя
2. Только для пользовательских процессов
3. Сортирует по ID сессии
4. Добавляет итоговую строку с количеством подключений

**Пример результата:**
```
Подключенные пользователи:
51    sa
52    user1
53    user2
Всего: 3 подключения
```

**Подсказка:**
```sql
PRINT 'Подключенные пользователи:';
SELECT session_id, login_name FROM sys.dm_exec_sessions 
WHERE is_user_process = 1 ORDER BY session_id;
PRINT 'Всего подключений: ' + CAST(@@ROWCOUNT AS VARCHAR);
```

---

### **Задание 5: Процедура "Проверка таблиц"**
**Цель:** Работа с таблицами текущей базы

**Задача:** Создать процедуру `usp_TableList`, которая:
1. Показывает список всех таблиц в текущей базе
2. Показывает дату создания каждой таблицы
3. Исключает системные таблицы
4. Сортирует по имени таблицы

**Пример результата:**
```
ТАБЛИЦЫ В БАЗЕ AdventureWorks:
Address          2008-04-30
Customer         2008-04-30
Product          2008-04-30
SalesOrderHeader 2008-04-30
```

**Подсказка:**
```sql
PRINT 'ТАБЛИЦЫ В БАЗЕ ' + DB_NAME() + ':';
SELECT name, create_date FROM sys.tables 
WHERE is_ms_shipped = 0 ORDER BY name;
```

---

## **💡 Советы для студентов:**

1. **Копируйте из лекции** — берите готовые примеры и меняйте под свое задание
2. **Тестируйте по частям** — сначала PRINT, потом добавляйте SELECT
3. **Смотрите ошибки** — читайте сообщения, они подскажут что не так
4. **Сохраняйте скрипты** — чтобы можно было показать преподавателю
5. **Не бойтесь пробовать** — если что-то не работает, пробуйте другой вариант

---

## **📋 Ключевые моменты для запоминания**

### **✅ Что нужно запомнить (5 основных правил):**

1. **CREATE PROCEDURE + имя** — создание процедуры
2. **EXEC имя_процедуры** — запуск процедуры
3. **PRINT для сообщений, SELECT для данных** — не путать!
4. **usp_ для своих процедур** — никогда sp_!
5. **BEGIN...END и GO** — обязательные элементы

### **❌ Частые ошибки новичков (и как их избежать):**

| Ошибка | Пример | Как исправить | Почему важно |
|--------|--------|---------------|--------------|
| **Забыли GO** | `CREATE PROCEDURE ... END` | Всегда ставить `GO` после создания | SQL Server выполняет пакетами |
| **Используют sp_** | `CREATE PROCEDURE sp_myproc` | Использовать `usp_` | Производительность + ясность |
| **Путают PRINT/SELECT** | `PRINT SELECT * FROM ...` | Сначала в переменную, потом PRINT | Разный тип вывода |
| **Создают не в той БД** | Создали в MyDB, вызывают из AnotherDB | Использовать `master` для админ-инструментов | Доступность процедуры |
| **Нет BEGIN...END** | `CREATE PROCEDURE ... AS SELECT ...` | Всегда использовать BEGIN...END | Читаемость и структура |

### **🎯 Чек-лист для создания процедуры:**

1. [ ] Выбрали базу (`USE master;`)
2. [ ] Начали с `CREATE PROCEDURE`
3. [ ] Использовали префикс `usp_`
4. [ ] Добавили `AS BEGIN ... END`
5. [ ] Поставили `GO` в конце
6. [ ] Добавили `SET NOCOUNT ON` (будет в следующей лекции)
7. [ ] Протестировали через `EXEC`

---

## **💡 Советы для успешного обучения**

1. **Не бойтесь экспериментировать** — создавайте процедуры, изменяйте их, удаляйте
2. **Используйте комментарии** — пишите, что делает каждая часть кода
3. **Сохраняйте скрипты** — в текстовых файлах или в SSMS
4. **Задавайте вопросы** — если что-то непонятно, спрашивайте сразу
5. **Помогайте одногруппникам** — объясняя другим, лучше понимаете сами


### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR12.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR13_1.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №12](../LESSONS/PR12.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №13](../LESSONS/PR13_1.MD) |

