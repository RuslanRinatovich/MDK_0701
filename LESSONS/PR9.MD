### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON10.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON11.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №9](../LESSONS/LESSON10.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №10](../LESSONS/LESSON11.MD) |

---

# **Практическая работа №9** **Диагностика: список объектов, права, зависимости**

> **Цель:** Научиться использовать системные представления для получения полной картины о структуре базы данных, правах доступа и зависимостях между объектами. Создать набор диагностических запросов для быстрой инвентаризации БД.

> **Продолжительность:** 2 академических часа
> **Оборудование:** SSMS, SQL Server Developer Edition

---

## **📥 Шаг 0. Подготовка учебной базы данных (выполните целиком)**

```sql
-- Создаём учебную БД
DROP DATABASE IF EXISTS AdminDiagLab;
CREATE DATABASE AdminDiagLab;
GO
USE AdminDiagLab;
GO

-- Создаём несколько схем
CREATE SCHEMA sales;
CREATE SCHEMA hr;
GO

-- Создаём таблицы в разных схемах
CREATE TABLE sales.Orders (
    OrderID int IDENTITY(1,1) PRIMARY KEY,
    CustomerID int,
    OrderDate date,
    Amount decimal(10,2)
);

CREATE TABLE sales.Customers (
    CustomerID int IDENTITY(1,1) PRIMARY KEY,
    CustomerName nvarchar(100)
);

CREATE TABLE hr.Employees (
    EmployeeID int IDENTITY(1,1) PRIMARY KEY,
    FirstName nvarchar(50),
    LastName nvarchar(50),
    HireDate date
);

CREATE TABLE hr.Departments (
    DepartmentID int IDENTITY(1,1) PRIMARY KEY,
    DepartmentName nvarchar(50)
);

-- Создаём некластерные индексы
CREATE INDEX IX_Orders_Customer ON sales.Orders(CustomerID);
CREATE INDEX IX_Orders_Date ON sales.Orders(OrderDate);
CREATE INDEX IX_Employees_Name ON hr.Employees(LastName, FirstName);

-- Создаём представление, которое зависит от таблиц
CREATE VIEW sales.ActiveOrders AS
SELECT o.OrderID, c.CustomerName, o.OrderDate, o.Amount
FROM sales.Orders o
INNER JOIN sales.Customers c ON o.CustomerID = c.CustomerID
WHERE o.OrderDate >= DATEADD(MONTH, -1, GETDATE());
GO
```

---

## **📋 Задание 1. Инвентаризация: что есть в моей БД?**

**Цель:** Составить общий обзор всех пользовательских таблиц в базе данных.

**Шаг 1.** Напишите запрос, который выводит для каждой таблицы:
*   Имя схемы
*   Имя таблицы
*   Дата создания
*   Дата последнего изменения

```sql
USE AdminDiagLab;
GO

SELECT 
    SCHEMA_NAME(t.schema_id) AS SchemaName,
    t.name AS TableName,
    t.create_date AS CreateDate,
    t.modify_date AS LastModifiedDate
FROM sys.tables t
ORDER BY SchemaName, TableName;
```

**В отчёт:** Вставьте скриншот с результатом выполнения запроса.

**Шаг 2.** Ответьте на вопросы:
1.  Все ли таблицы отсортированы по схемам?
2.  Какая таблица была создана последней?

---

## **📋 Задание 2. Анализ индексов**

**Цель:** Получить полную картину по индексам всех таблиц.

**Шаг 1.** Напишите запрос, который выводит:
*   Имя схемы
*   Имя таблицы
*   Имя индекса
*   Тип индекса (Clustered, NonClustered, Heap)
*   Уникальность индекса

```sql
SELECT 
    SCHEMA_NAME(t.schema_id) AS SchemaName,
    t.name AS TableName,
    i.name AS IndexName,
    i.type_desc AS IndexType,
    i.is_unique AS IsUnique
FROM sys.tables t
INNER JOIN sys.indexes i ON t.object_id = i.object_id
WHERE i.index_id > 0 -- Исключаем XML/пространственные индексы
ORDER BY SchemaName, TableName, i.index_id;
```

**В отчёт:**
1.  Вставьте скриншот с результатом.
2.  Сколько всего индексов в таблице `sales.Orders`? Назовите их.

---

## **📋 Задание 3. Поиск таблиц без первичного ключа (кандидаты на доработку)**

**Цель:** Выявить потенциально проблемные таблицы, у которых нет кластерного индекса (кучи).

**Шаг 1.** Напишите запрос, который находит все таблицы, у которых нет ни одного индекса с `index_id = 1` (кластерный индекс, обычно PK).

```sql
SELECT 
    SCHEMA_NAME(t.schema_id) AS SchemaName,
    t.name AS TableName
FROM sys.tables t
WHERE NOT EXISTS (
    SELECT 1 
    FROM sys.indexes i 
    WHERE i.object_id = t.object_id 
    AND i.index_id = 1 -- Нет кластерного индекса
)
ORDER BY SchemaName, TableName;
```

**В отчёт:**
1.  Вставьте скриншот с результатом.
2.  Объясните, почему отсутствие первичного ключа может быть проблемой.

---

## **📋 Задание 4. Аудит прав доступа к схемам**

**Цель:** Увидеть, какие права на какие схемы были выданы.

**Шаг 1.** Напишите запрос, который показывает выданные права на уровне схем.

```sql
SELECT 
    USER_NAME(grantee_principal_id) AS UserOrRoleName,
    permission_name AS Permission,
    state_desc AS PermissionState,
    SCHEMA_NAME(major_id) AS SchemaName
FROM sys.database_permissions
WHERE class_desc = 'SCHEMA'
AND minor_id = 0;
```

**В отчёт:** Вставьте скриншот. Объясните, что означает каждый столбец в результате.

---

## **📋 Задание 5. Анализ зависимостей объектов**

**Цель:** Увидеть, какие объекты от каких зависят (например, представление `sales.ActiveOrders` зависит от таблиц `sales.Orders` и `sales.Customers`).

**Шаг 1.** Напишите запрос, который показывает зависимости для созданного нами представления.

```sql
SELECT 
    OBJECT_NAME(referencing_id) AS DependentObject,
    referenced_schema_name AS ReferencedSchema,
    referenced_entity_name AS ReferencedObject
FROM sys.sql_expression_dependencies
WHERE referencing_id = OBJECT_ID('sales.ActiveOrders');
```

**В отчёт:**
1.  Вставьте скриншот с результатом.
2.  Перечислите, от каких объектов зависит представление `sales.ActiveOrders`.

---

## **📋 Задание 6. Создание сводного диагностического отчёта**

**Цель:** Создать один универсальный запрос, который даёт общую картину по БД.

**Шаг 1.** Напишите запрос, который для каждой таблицы показывает:
*   Схему и имя таблицы
*   Количество строк
*   Количество индексов
*   Имя первичного ключа (если есть)

```sql
SELECT 
    SCHEMA_NAME(t.schema_id) AS SchemaName,
    t.name AS TableName,
    p.rows AS RowCount,
    COUNT(i.index_id) AS IndexCount,
    ISNULL(pk_index.name, 'HEAP') AS PrimaryKeyName
FROM sys.tables t
INNER JOIN sys.partitions p ON t.object_id = p.object_id AND p.index_id IN (0,1)
LEFT JOIN sys.indexes i ON t.object_id = i.object_id
LEFT JOIN (
    SELECT i.object_id, i.name
    FROM sys.indexes i
    WHERE i.index_id = 1
) pk_index ON t.object_id = pk_index.object_id
GROUP BY SCHEMA_NAME(t.schema_id), t.name, p.rows, pk_index.name
ORDER BY SchemaName, TableName;
```

**В отчёт:** Вставьте скриншот итогового результата.

---

## **📋 Задание 7. Ответы на контрольные вопросы**

1.  **Для чего администратору баз данных необходимо уметь работать с системными представлениями? Приведите три практических примера.**
2.  **Объясните разницу между `sys.tables` и `sys.objects`. Какое представление более универсально?**
3.  **Что означает `index_id = 0` в представлении `sys.indexes`? Почему большое количество таких таблиц на сервере может быть признаком плохого проектирования?**
4.  **С какими трудностями вы столкнулись при написании запросов к системным представлениям? Что оказалось самым сложным?**

---

## **Требования к оформлению отчёта**

**Файл должен называться:** `Отчёт_Фамилия_Группа.docx` (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**
1. **Титульный лист** (ФИО, группа, дата, название работы)
2. **Цель работы**
3. **Ход выполнения:**
   - Полный текст стартового скрипта (с вашей фамилией)
   - Скриншоты из SSMS со всеми выполненными SQL-командами из заданий 1-6
   - На скриншоте должны быть видны:
     - Текст команды(ы)
     - Сообщение об успешном выполнении
     - Имя базы данных в заголовке окна
   - Под каждым скриншотом — подпись с пояснением

4. **Сравнительный анализ:**
   - Таблица сравнения структуры объектов (см. выше)

5. **Ответы на вопросы** из задания 7

6. **Вывод:**
   - Что нового вы узнали о системных представлениях?
   - Какие запросы из этой работы могут пригодиться вам в реальной работе администратора?
   - С какими трудностями столкнулись при работе с системными представлениями?

---

## **Критерии оценивания**

| Критерий | Оценка «5» (отлично) | Оценка «4» (хорошо) | Оценка «3» (удовл.) |
|----------|---------------------|---------------------|---------------------|
| **Выполнение стартового скрипта** | Скрипт выполнен, фамилия заменена, все объекты созданы | Скрипт выполнен, но часть объектов отсутствует | Скрипт не выполнен или содержит критические ошибки |
| **Задания 1-2: инвентаризация** | Все запросы работают, анализ структуры полный | Запросы выполнены, но анализ поверхностный | Запросы не выполнены или с ошибками |
| **Задание 3: диагностика проблем** | HEAP-таблицы найдены, объяснены риски | HEAP-таблицы найдены, но анализ неполный | Задание не выполнено |
| **Задания 4-5: безопасность** | Права и зависимости проанализированы полностью | Анализ выполнен частично | Задания не выполнены |
| **Задание 6: комплексный отчет** | Отчет создан, все метрики корректны | Отчет создан, но часть метрик отсутствует | Отчет не создан |
| **Ответы на вопросы** | Все ответы полные, с использованием терминов | Ответы даны, но 1-2 — неполные | Ответы поверхностные |
| **Оформление отчёта** | Структура соблюдена, скриншоты чёткие, есть таблица | Есть мелкие нарушения оформления | Отчёт неструктурирован |

**💡 Максимальный балл: 5**
**❗ Без таблицы сравнения — максимальная оценка не выше «4»**

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **27 ноября 2025 г.**
---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON9.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON10.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №9](../LESSONS/LESSON10.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №10](../LESSONS/LESSON11.MD) |

---
