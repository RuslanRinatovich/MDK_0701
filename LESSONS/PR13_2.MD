🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON14_2.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON14_3.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №13.2](../LESSONS/LESSON14_2.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №13.3](../LESSONS/LESSON14_3.MD) |

---

# **Практическая работа №13.2. Процедуры с параметрами и переменными**

## **📋 Общая информация**

| Параметр | Значение |
|----------|----------|
| **Время выполнения** | 90 минут |
| **Количество заданий** | 5 |
| **Вариантов** | 30 (по числу студентов в группе) |
| **Срок сдачи** | До конца занятия |

---

## **🎯 Цель работы**
Научиться создавать процедуры с параметрами и использовать переменные для хранения данных.

---

## **📝 Определите свой вариант**

**Ваш номер в группе:** _____ (от 1 до 30)

**Ваш вариант = Ваш номер в группе**

---

## **📋 Технические требования**
1. Все процедуры создавайте в базе `master`
2. Используйте `SET NOCOUNT ON` в начале каждой процедуры
3. Все параметры должны иметь понятные имена
4. Тестируйте каждую процедуру с разными значениями параметров

---

## **Задание 1. Процедура с одним параметром (15 минут)**

### **Ваша задача:**
Создать процедуру `usp_Task1`, которая принимает один параметр и выполняет действие в зависимости от вашего варианта.

### **Шаблон кода:**
```sql
-- Задание 1: Процедура с одним параметром
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_Task1
    @InputValue INT  -- Входной параметр
AS
BEGIN
    SET NOCOUNT ON;
    
    -- ВАШ КОД ЗДЕСЬ
    -- Используйте параметр @InputValue
    -- Выводите результаты через PRINT или SELECT
    
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна: |
|---------|-------------------|
| **1-6** | Вывести квадрат числа и проверить, положительное ли оно |
| **7-12** | Проверить, делится ли число на 5, и вывести остаток от деления на 3 |
| **13-18** | Вывести число в двоичном виде (используйте деление на 2) |
| **19-24** | Проверить, является ли число простым (делится только на 1 и себя) |
| **25-30** | Вывести факториал числа (1*2*3*...*n) для чисел до 10 |

---

## **Задание 2. Процедура с параметром по умолчанию (15 минут)**

### **Ваша задача:**
Создать процедуру `usp_Task2` с параметром, имеющим значение по умолчанию.

### **Шаблон кода:**
```sql
-- Задание 2: Процедура с параметром по умолчанию
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_Task2
    @Filter NVARCHAR(20) = 'ALL'  -- Параметр со значением по умолчанию
AS
BEGIN
    SET NOCOUNT ON;
    
    -- ВАШ КОД ЗДЕСЬ
    -- Используйте параметр @Filter для фильтрации данных
    -- Обработайте разные значения параметра (IF...ELSE или CASE)
    
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | При @Filter = | Показывать: |
|---------|---------------|-------------|
| **1-10** | 'ALL' | Все базы данных |
| | 'ONLINE' | Только базы со статусом ONLINE |
| | 'SYSTEM' | Только системные базы (database_id ≤ 4) |
| **11-20** | 'ALL' | Все таблицы текущей базы |
| | 'USER' | Только пользовательские таблицы (is_ms_shipped = 0) |
| | 'SYS' | Только системные таблицы (is_ms_shipped = 1) |
| **21-30** | 'ALL' | Все подключения |
| | 'ACTIVE' | Только активные подключения (status = 'running') |
| | 'WAITING' | Только ожидающие подключения |

---

## **Задание 3. Процедура с двумя параметрами (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_Task3` с двумя параметрами, которые работают вместе.

### **Шаблон кода:**
```sql
-- Задание 3: Процедура с двумя параметрами
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_Task3
    @Param1 INT = 0,      -- Первый параметр с умолчанием
    @Param2 INT = 100     -- Второй параметр с умолчанием
AS
BEGIN
    SET NOCOUNT ON;
    
    -- ВАШ КОД ЗДЕСЬ
    -- Используйте оба параметра @Param1 и @Param2
    -- Реализуйте логику фильтрации или расчета
    
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна: |
|---------|-------------------|
| **1-6** | Показать таблицы с количеством строк между @Param1 и @Param2 |
| **7-12** | Показать базы данных, созданные между двумя датами (используйте DATEADD) |
| **13-18** | Проверить диапазон чисел (@Param1 до @Param2) и найти простые числа в нем |
| **19-24** | Рассчитать сумму чисел от @Param1 до @Param2 |
| **25-30** | Показать таблицы, размер которых между @Param1 и @Param2 МБ |

---

## **Задание 4. Процедура с OUTPUT параметром (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_Task4`, которая возвращает результат через OUTPUT параметр.

### **Шаблон кода:**
```sql
-- Задание 4: Процедура с OUTPUT параметром
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_Task4
    @Result INT OUTPUT  -- OUTPUT параметр для возврата результата
AS
BEGIN
    SET NOCOUNT ON;
    
    -- ВАШ КОД ЗДЕСЬ
    -- Вычислите значение и присвойте его @Result
    -- Используйте SET @Result = ... или SELECT @Result = ...
    
    PRINT 'Результат вычислен и сохранен в @Result';
END
GO
```

### **Что должна вычислять процедура (по вариантам):**

| Вариант | Вычислять и возвращать в @Result: |
|---------|-----------------------------------|
| **1-6** | Количество всех баз данных на сервере |
| **7-12** | Количество таблиц в текущей базе данных |
| **13-18** | Количество индексов в текущей базе данных |
| **19-24** | Суммарный размер всех баз данных в МБ |
| **25-30** | Количество активных пользовательских подключений |

---

## **Задание 5. Комплексная процедура (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_Task5` с несколькими параметрами, реализующую комплексную логику.

### **Шаблон кода:**
```sql
-- Задание 5: Комплексная процедура
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_Task5
    @Mode NVARCHAR(20) = 'BASIC',  -- Режим работы
    @ShowDetails BIT = 0,          -- Показывать детали?
    @Limit INT = 10                -- Ограничение вывода
AS
BEGIN
    SET NOCOUNT ON;
    
    -- ВАШ КОД ЗДЕСЬ
    -- Используйте все три параметра
    -- Реализуйте разную логику в зависимости от @Mode
    -- Используйте @ShowDetails для детализации
    -- Ограничивайте вывод с помощью @Limit
    
    PRINT 'Процедура выполнена в режиме: ' + @Mode;
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Режимы (@Mode): | Действия: |
|---------|-----------------|-----------|
| **1-10** | 'BASIC', 'DETAILED', 'FULL' | Анализ состояния сервера (базы, подключения, память) |
| **11-20** | 'COUNT', 'LIST', 'STATS' | Работа с таблицами (подсчет, список, статистика) |
| **21-30** | 'QUICK', 'NORMAL', 'DEEP' | Проверка целостности и производительности |

---

## **📊 Полный шаблон для выполнения работы**

```sql
-- ========================================
-- ПРАКТИЧЕСКАЯ РАБОТА 13.2
-- Студент: [Фамилия Имя]
-- Группа: [Ваша группа]
-- Вариант: [Ваш номер в группе]
-- Дата: [Дата выполнения]
-- ========================================

-- Подключаемся к базе master
USE master;
GO

-- ========== ЗАДАНИЕ 1 ==========
-- Процедура с одним параметром
PRINT '=== СОЗДАНИЕ ПРОЦЕДУРЫ usp_Task1 ===';
GO

CREATE PROCEDURE usp_Task1
    @InputValue INT
AS
BEGIN
    SET NOCOUNT ON;
    
    -- КОД ЗАДАНИЯ 1
    -- Вариант: [ваш номер]
    
END
GO

-- ========== ЗАДАНИЕ 2 ==========
-- Процедура с параметром по умолчанию
PRINT '=== СОЗДАНИЕ ПРОЦЕДУРЫ usp_Task2 ===';
GO

CREATE PROCEDURE usp_Task2
    @Filter NVARCHAR(20) = 'ALL'
AS
BEGIN
    SET NOCOUNT ON;
    
    -- КОД ЗАДАНИЯ 2
    -- Вариант: [ваш номер]
    
END
GO

-- ========== ЗАДАНИЕ 3 ==========
-- Процедура с двумя параметрами
PRINT '=== СОЗДАНИЕ ПРОЦЕДУРЫ usp_Task3 ===';
GO

CREATE PROCEDURE usp_Task3
    @Param1 INT = 0,
    @Param2 INT = 100
AS
BEGIN
    SET NOCOUNT ON;
    
    -- КОД ЗАДАНИЯ 3
    -- Вариант: [ваш номер]
    
END
GO

-- ========== ЗАДАНИЕ 4 ==========
-- Процедура с OUTPUT параметром
PRINT '=== СОЗДАНИЕ ПРОЦЕДУРЫ usp_Task4 ===';
GO

CREATE PROCEDURE usp_Task4
    @Result INT OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    
    -- КОД ЗАДАНИЯ 4
    -- Вариант: [ваш номер]
    
END
GO

-- ========== ЗАДАНИЕ 5 ==========
-- Комплексная процедура
PRINT '=== СОЗДАНИЕ ПРОЦЕДУРЫ usp_Task5 ===';
GO

CREATE PROCEDURE usp_Task5
    @Mode NVARCHAR(20) = 'BASIC',
    @ShowDetails BIT = 0,
    @Limit INT = 10
AS
BEGIN
    SET NOCOUNT ON;
    
    -- КОД ЗАДАНИЯ 5
    -- Вариант: [ваш номер]
    
END
GO

-- ========== ТЕСТИРОВАНИЕ ==========
PRINT '=== НАЧАЛО ТЕСТИРОВАНИЯ ===';
GO

-- Тест процедуры 1
PRINT 'Тест usp_Task1:';
DECLARE @TestValue INT = 7;  -- Измените для тестирования
EXEC usp_Task1 @InputValue = @TestValue;
GO

-- Тест процедуры 2
PRINT 'Тест usp_Task2 (по умолчанию):';
EXEC usp_Task2;
GO

PRINT 'Тест usp_Task2 (с параметром):';
EXEC usp_Task2 @Filter = 'ONLINE';  -- Измените значение
GO

-- Тест процедуры 3
PRINT 'Тест usp_Task3:';
EXEC usp_Task3 @Param1 = 1, @Param2 = 50;  -- Измените значения
GO

-- Тест процедуры 4
PRINT 'Тест usp_Task4:';
DECLARE @OutputResult INT;
EXEC usp_Task4 @Result = @OutputResult OUTPUT;
PRINT 'Результат из OUTPUT: ' + CAST(@OutputResult AS VARCHAR);
GO

-- Тест процедуры 5
PRINT 'Тест usp_Task5:';
EXEC usp_Task5 @Mode = 'DETAILED', @ShowDetails = 1, @Limit = 5;
GO

PRINT '=== ТЕСТИРОВАНИЕ ЗАВЕРШЕНО ===';
GO
```

---

## **📝 Что нужно сдать:**

### **1. SQL файл с кодом**
Имя файла: `Практика13.2_Фамилия_Группа.sql`

Файл должен содержать:
- Все 5 процедур с вашим кодом
- Тестовые вызовы процедур
- Комментарии с вашим вариантом

### **2. Скриншоты (5 штук)**
Для каждой процедуры:
- Скриншот создания процедуры
- Скриншот результатов тестирования
Подписать: "Фамилия - Процедура X - Вариант Y"

### **3. Ответы на вопросы**
В конце SQL файла добавьте ответы:

```sql
-- ========== ОТВЕТЫ НА ВОПРОСЫ ==========
/*
1. В чем разница между параметром и переменной в процедуре?

2. Когда использовать OUTPUT параметры вместо RETURN?

3. Зачем нужен SET NOCOUNT ON в начале процедуры?

4. Что будет, если не указать значение для обязательного параметра?

5. Какой тип данных лучше использовать для хранения имен пользователей и почему?
*/
```

---

## **✅ Критерии оценки**

| Критерий | Максимальный балл | Описание |
|----------|-------------------|----------|
| **Задание 1** | 20 | Правильное использование одного параметра |
| **Задание 2** | 20 | Параметр с умолчанием и обработка разных значений |
| **Задание 3** | 20 | Работа с двумя параметрами, корректная логика |
| **Задание 4** | 20 | OUTPUT параметр работает корректно |
| **Задание 5** | 20 | Комплексная логика с несколькими параметрами |
| **Итого** | **100** | |

**Шкала оценок:**
- **85-100 баллов** — Отлично (5)
- **70-84 баллов** — Хорошо (4)
- **50-69 баллов** — Удовлетворительно (3)
- **0-49 баллов** — Неудовлетворительно (2)

---

## **💡 Подсказки для выполнения:**

### **Полезные SQL команды:**
```sql
-- Для работы с параметрами:
@ParameterName -- Обращение к параметру
@VariableName  -- Обращение к переменной

-- Условные операторы:
IF @Condition = 1 BEGIN ... END ELSE BEGIN ... END
CASE WHEN @Condition THEN ... ELSE ... END

-- Циклы (если понадобятся):
WHILE @Counter <= 10 BEGIN ... SET @Counter = @Counter + 1 END

-- Работа с текстом:
LEN(@Text) -- Длина строки
LEFT(@Text, 3) -- Первые 3 символа
SUBSTRING(@Text, 2, 5) -- С 2-го символа 5 символов
CHARINDEX(' ', @Text) -- Поиск пробела
```

### **Частые ошибки и решения:**
1. **"Необходимо объявить скалярную переменную"**
   → Проверьте, что переменная объявлена (DECLARE)

2. **"Синтаксическая ошибка около '@Parameter'"**
   → Проверьте запятые между параметрами

3. **Процедура создается, но не видна**
   → Обновите список объектов в SSMS (F5)

4. **OUTPUT параметр возвращает NULL**
   → Убедитесь, что присваиваете значение параметру

---

## **🎯 Советы для успеха:**

1. **Начинайте с простого** — сначала напишите PRINT, потом добавляйте логику
2. **Тестируйте каждую процедуру отдельно** — прежде чем создавать следующую
3. **Используйте понятные имена** — @UserName лучше чем @U
4. **Комментируйте сложную логику** — чтобы было понятно, что делает код
5. **Проверяйте граничные случаи** — что если параметр = 0, или = NULL?

---

## **🚀 Дополнительное задание (для быстрых):**

Если выполнили все 5 заданий, создайте процедуру, которая объединяет функциональность всех пяти:

```sql
-- Супер-процедура, которая использует все изученное
CREATE PROCEDURE usp_MasterProcedure
    @TaskNumber INT = 1,
    @Param1 SQL_VARIANT = NULL,
    @Param2 SQL_VARIANT = NULL,
    @Result SQL_VARIANT OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    
    -- Ваш код здесь
    -- В зависимости от @TaskNumber вызывайте соответствующую процедуру
    -- Используйте SQL_VARIANT для работы с разными типами данных
    
END
GO
```

---

## **📞 Если возникли трудности:**

1. **Перечитайте задание для своего варианта**
2. **Посмотрите аналогичные примеры из лекции**
3. **Спросите у соседа с близким вариантом**
4. **Поднимите руку** — преподаватель поможет

**Удачи! Помните: параметры делают процедуры гибкими, а переменные — умными!** 💻🚀

---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON14_2.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON14_3.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №13.2](../LESSONS/LESSON14_2.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №13.3](../LESSONS/LESSON14_3.MD) |
