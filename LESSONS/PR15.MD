🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON15.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON16.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №15](../LESSONS/LESSON15.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №16](../LESSONS/LESSON16.MD) |

---

# 🧪 Практическая работа №15. Создание полной резервной копии (SSMS + T-SQL)

⏱️ **Продолжительность:** 90 минут  
🎯 **Цель работы:**  
Научиться создавать полные резервные копии базы данных двумя способами: через графический интерфейс SSMS и с помощью T-SQL команд. Освоить просмотр истории резервных копий, проверку целостности и анализ метаданных бэкапа. Углубить понимание того, как физически устроена резервная копия и какие факторы влияют на её размер.

🖥️ **Оборудование:**  
Виртуальная машина с Windows Server / Windows 10, установленными SQL Server Developer Edition и SSMS.

---

## 📋 Общая информация  

| Параметр               | Значение                                                                      |
|------------------------|-------------------------------------------------------------------------------|
| Время выполнения       | 90 минут                                                                      |
| Количество заданий     | 7                                                                             |
| Вариантов              | 30 (по числу студентов в группе)                                             |

---

## 🧠 Глубокое погружение: перед началом ответьте себе на вопросы

Прежде чем приступить к выполнению, подумайте:
- Что физически представляет собой файл `.bak`?
- Почему размер полной копии примерно равен размеру базы данных?
- Какая информация кроме самих данных попадает в бэкап?
- Зачем нужны опции `CHECKSUM`, `COMPRESSION`, `STATS`?

В ходе работы вы не только создадите бэкапы, но и научитесь «читать» их внутреннюю структуру.

---

## 📋 Ход выполнения работы  

### 🔹 Этап 1. Подготовка среды (15 мин)

1. Запустите **SSMS** и подключитесь к вашему экземпляру SQL Server.  
2. Создайте базу данных для экспериментов с уникальным именем, содержащим вашу фамилию:

```sql
CREATE DATABASE BackupPractice_Фамилия;
GO
USE BackupPractice_Фамилия;
GO
```

3. Создайте тестовые таблицы и наполните их данными. Обратите внимание на размер строки – это позволит увидеть рост файла бэкапа.

```sql
-- Таблица сотрудников
CREATE TABLE dbo.Employees (
    EmployeeID INT IDENTITY(1,1) PRIMARY KEY,
    LastName NVARCHAR(50) NOT NULL,
    FirstName NVARCHAR(50) NOT NULL,
    HireDate DATE DEFAULT GETDATE(),
    Salary DECIMAL(10,2),
    Department NVARCHAR(50),
    Notes CHAR(1000) DEFAULT 'A'  -- для увеличения размера строки
);

-- Таблица заказов
CREATE TABLE dbo.Orders (
    OrderID INT IDENTITY(1,1) PRIMARY KEY,
    EmployeeID INT REFERENCES dbo.Employees(EmployeeID),
    OrderDate DATETIME DEFAULT GETDATE(),
    Amount DECIMAL(10,2),
    Status NVARCHAR(20) DEFAULT 'Новый',
    Description CHAR(500) DEFAULT 'B'
);

-- Наполнение данными (достаточно много, чтобы бэкап имел заметный размер)
INSERT INTO dbo.Employees (LastName, FirstName, Salary, Department)
SELECT TOP 1000 
    'Фамилия' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS NVARCHAR(10)),
    'Имя' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS NVARCHAR(10)),
    50000 + (ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) * 100),
    CASE WHEN ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) % 3 = 0 THEN 'IT'
         WHEN ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) % 3 = 1 THEN 'Finance'
         ELSE 'HR' END
FROM sys.objects a
CROSS JOIN sys.objects b;

INSERT INTO dbo.Orders (EmployeeID, Amount, Status)
SELECT TOP 5000
    ABS(CHECKSUM(NEWID())) % 1000 + 1,
    ROUND(RAND(CHECKSUM(NEWID())) * 10000, 2),
    CASE WHEN RAND(CHECKSUM(NEWID())) < 0.3 THEN 'Новый'
         WHEN RAND(CHECKSUM(NEWID())) < 0.6 THEN 'В обработке'
         ELSE 'Выполнен' END
FROM sys.objects a
CROSS JOIN sys.objects b;
GO
```

4. Создайте папку для хранения резервных копий, например, `C:\Backup\ПР15\`.  
   Убедитесь, что у вас есть права на запись в эту папку.

5. **Проверьте текущую модель восстановления** (для полной копии подходит любая, но мы будем работать в FULL для единообразия):

```sql
SELECT name, recovery_model_desc FROM sys.databases WHERE name = DB_NAME();

-- Если не FULL:
ALTER DATABASE BackupPractice_Фамилия SET RECOVERY FULL;
GO
```

---

### 🔹 Этап 2. Создание полной резервной копии через SSMS (20 мин)

1. В обозревателе объектов найдите вашу базу данных `BackupPractice_Фамилия`.

2. **ПКМ** на базе → **Задачи** → **Создать резервную копию...**

3. В открывшемся окне настройте параметры:
   - **База данных:** `BackupPractice_Фамилия` (должна быть выбрана автоматически)
   - **Тип резервной копии:** `Полная`
   - **Компонент резервного копирования:** `База данных`
   - **Набор резервных копий:**
     - Имя: `BackupPractice_Фамилия-Полная (SSMS)`
     - Описание: `Создано через SSMS, вариант N`
   - **Назначение:** нажмите **Добавить** и укажите путь:
     ```
     C:\Backup\ПР15\BackupPractice_Фамилия_Full_SSMS.bak
     ```
   - **Сжатие:** выберите `Сжать резервную копию`

4. Перейдите на вкладку **Параметры медиа** и отметьте:
   - **Перезаписать все существующие наборы резервных копий** (INIT)
   - **Проверить целостность резервной копии после завершения** (VERIFY)
   - **Создать резервную копию с контрольной суммой** (CHECKSUM) – *это повысит надёжность, но немного замедлит процесс*

5. Нажмите **ОК** для выполнения.

6. **Сделайте скриншот** окна с настройками (до нажатия ОК) и окна с сообщением об успешном завершении.  
   На скриншоте должно быть видно имя вашей базы и путь к файлу.

7. Откройте папку `C:\Backup\ПР15\` через Проводник и убедитесь, что файл `.bak` создан.  
   **Запишите размер файла** (в МБ) – пригодится для сравнения.

---

### 🔹 Этап 3. Создание полной резервной копии через T-SQL (20 мин)

1. Откройте новое окно запроса (Ctrl+N) и убедитесь, что выбрана ваша база данных.

2. Выполните команду создания полной резервной копии с расширенными параметрами:

```sql
BACKUP DATABASE [BackupPractice_Фамилия]
TO DISK = N'C:\Backup\ПР15\BackupPractice_Фамилия_Full_TSQL.bak'
WITH 
    INIT,                           -- перезаписать файл
    NAME = N'BackupPractice_Фамилия-Полная T-SQL',
    DESCRIPTION = N'Создано через T-SQL, вариант N',
    STATS = 10,                     -- показывать прогресс каждые 10%
    CHECKSUM,                       -- проверка целостности страниц
    COMPRESSION,                     -- сжатие
    FORMAT;                          -- инициализировать новый носитель (очистить метаданные)
```

3. После успешного выполнения вы увидите сообщение вроде:
   ```
   Processed 1234 pages for database 'BackupPractice_Фамилия', file 'BackupPractice_Фамилия' on file 1.
   Processed 2 pages for database 'BackupPractice_Фамилия', file 'BackupPractice_Фамилия_log' on file 1.
   BACKUP DATABASE successfully processed 1236 pages in 2.345 seconds (4.2 MB/sec).
   ```

4. **Сделайте скриншот** окна с командой и результатом выполнения. Обратите внимание на количество обработанных страниц и скорость записи.

5. Сравните размер файла, созданного через T-SQL, с файлом из этапа 2. Они должны быть примерно равны. Если отличаются – подумайте, почему (разные опции сжатия?).

---

### 🔹 Этап 4. Анализ содержимого резервной копии (15 мин)

Выполните запрос для просмотра заголовка созданного файла:

```sql
RESTORE HEADERONLY 
FROM DISK = N'C:\Backup\ПР15\BackupPractice_Фамилия_Full_TSQL.bak';
```

Изучите полученные столбцы. Обратите особое внимание на:
- **BackupType** – тип копии (1 = полная)
- **DatabaseName** – имя базы
- **BackupStartDate**, **BackupFinishDate** – время выполнения
- **BackupSize** – размер в байтах
- **FirstLSN**, **LastLSN** – номера записей в журнале, охваченных бэкапом
- **CheckpointLSN** – контрольная точка на момент начала
- **DatabaseVersion** – версия БД
- **CompressedBackupSize** – размер после сжатия (если применялось)
- **SoftwareVersionMajor** – версия SQL Server

**Задание:**  
- Запишите значения **FirstLSN** и **LastLSN**.  
- Рассчитайте разницу между BackupSize и CompressedBackupSize (если есть) – это степень сжатия.  
- Подумайте, почему в бэкап попадают страницы журнала (Processed ... pages for file '..._log').

**Скриншот** результатов RESTORE HEADERONY.

---

### 🔹 Этап 5. Добавление данных и вторая копия в тот же файл (15 мин)

1. Добавьте новые записи в таблицы, чтобы имитировать изменения после бэкапа:

```sql
USE BackupPractice_Фамилия;
GO

INSERT INTO dbo.Employees (LastName, FirstName, Salary, Department)
VALUES 
    ('Новиков', 'Андрей', 72000, 'IT'),
    ('Волкова', 'Татьяна', 89000, 'Finance');

INSERT INTO dbo.Orders (EmployeeID, Amount, Status)
VALUES 
    (1001, 1750.00, 'Новый'),
    (1002, 2100.50, 'В обработке');
GO
```

2. Создайте **ещё одну полную копию**, но теперь **в тот же файл** (добавлением, а не перезаписью):

```sql
BACKUP DATABASE [BackupPractice_Фамилия]
TO DISK = N'C:\Backup\ПР15\BackupPractice_Фамилия_Full_TSQL.bak'
WITH 
    NOINIT,                         -- НЕ перезаписывать, а добавить
    NAME = N'BackupPractice_Фамилия-Полная T-SQL (вторая)',
    DESCRIPTION = N'Вторая копия после добавления данных',
    STATS = 10,
    CHECKSUM,
    COMPRESSION;
```

3. Снова выполните `RESTORE HEADERONLY` для того же файла. Вы увидите **два набора** (две строки). Обратите внимание, как изменились значения LSN, размер, дата.

4. **Скриншот** результата RESTORE HEADERONLY с двумя наборами.

---

### 🔹 Этап 6. Просмотр истории бэкапов в msdb (10 мин)

Системная база данных msdb хранит историю всех операций резервного копирования. Выполните запрос:

```sql
SELECT 
    bs.database_name,
    bs.backup_start_date,
    bs.backup_finish_date,
    CASE bs.type
        WHEN 'D' THEN 'Полная'
        WHEN 'I' THEN 'Разностная'
        WHEN 'L' THEN 'Журнал'
    END AS Тип_копии,
    bs.backup_size/1048576 AS Размер_МБ,
    bs.compressed_backup_size/1048576 AS Сжатый_размер_МБ,
    bs.name AS Имя_набора,
    bs.description AS Описание,
    bmf.physical_device_name AS Файл,
    bs.first_lsn,
    bs.last_lsn,
    bs.checkpoint_lsn
FROM msdb.dbo.backupset bs
JOIN msdb.dbo.backupmediafamily bmf 
    ON bs.media_set_id = bmf.media_set_id
WHERE bs.database_name = 'BackupPractice_Фамилия'
ORDER BY bs.backup_start_date DESC;
```

**Вопросы для анализа:**
- Сколько записей вы видите? Соответствует ли это количеству созданных вами бэкапов?
- Какая разница между `backup_size` и `compressed_backup_size`?
- Что означают поля `first_lsn`, `last_lsn`, `checkpoint_lsn`? Почему они важны для восстановления?

**Скриншот** результатов запроса.

---

### 🔹 Этап 7. Проверка целостности резервной копии (10 мин)

Выполните проверку созданных копий:

```sql
-- Проверить первый набор в файле
RESTORE VERIFYONLY 
FROM DISK = N'C:\Backup\ПР15\BackupPractice_Фамилия_Full_TSQL.bak'
WITH FILE = 1;

-- Проверить второй набор
RESTORE VERIFYONLY 
FROM DISK = N'C:\Backup\ПР15\BackupPractice_Фамилия_Full_TSQL.bak'
WITH FILE = 2;
```

Сообщение `The backup set is valid` означает, что копия цела и может быть использована для восстановления.

**Скриншот** успешной проверки.

---

## 📊 Итоговая таблица наблюдений

| Параметр | SSMS | T-SQL (первая) | T-SQL (вторая) |
|----------|------|----------------|----------------|
| Имя файла | | | |
| Размер файла (МБ) | | | |
| Количество страниц данных (data pages) | | | |
| Количество страниц журнала (log pages) | | | |
| Время выполнения (сек) | | | |
| Скорость записи (МБ/сек) | | | |
| BackupSize (из заголовка, байт) | | | |
| CompressedBackupSize (из заголовка, байт) | | | |
| FirstLSN | | | |
| LastLSN | | | |
| BackupStartDate | | | |

*Заполните таблицу, используя информацию из этапов 2, 3, 5 и 6.*

---

## ❓ Контрольные вопросы (с углублением)

1. Какая команда T-SQL создаёт полную резервную копию базы данных? Перечислите все опции, которые вы использовали, и объясните их назначение.  
2. В чём разница между параметрами `INIT` и `NOINIT`? Как это влияет на возможность хранения нескольких бэкапов в одном файле?  
3. Что означают поля `first_lsn`, `last_lsn` и `checkpoint_lsn` в заголовке резервной копии? Почему они критически важны для восстановления?  
4. Почему в выводе `BACKUP DATABASE` пишется, что обработано некоторое количество страниц журнала, хотя мы делаем полную копию данных?  
5. Зачем нужна опция `CHECKSUM`? Что произойдёт, если при проверке будет обнаружено повреждение страницы?  
6. Как сжатие (`COMPRESSION`) влияет на размер бэкапа и на скорость его создания/восстановления?  
7. Где SQL Server хранит историю всех выполненных резервных копий? Какой запрос позволяет её получить?  
8. Предположим, вы создали полную копию, затем добавили 100 МБ данных и сделали ещё одну полную копию в тот же файл с `NOINIT`. Какой будет общий размер файла?  
9. Можно ли восстановить базу из файла `.bak`, если исходная база была удалена? Что для этого нужно?  
10. **Глубокий вопрос:** В лекции упоминалось, что разностная копия работает на уровне экстентов. А на каком уровне работает полная копия – экстентов или страниц? Поясните.  
11. **Глубокий вопрос:** Если вы сделали полную копию в модели `SIMPLE`, а затем переключились в `FULL` и сделали ещё одну полную, будут ли отличаться их заголовки по каким-либо полям?  
12. **Глубокий вопрос:** Какой минимальный набор опций необходим, чтобы гарантировать возможность восстановления базы на другой сервер с той же версией SQL Server?

---

## 🏁 Заключение  

Вы выполнили полный цикл работы с полными резервными копиями: от создания через графический интерфейс и T-SQL до анализа внутренней структуры и проверки целостности. Теперь вы:  
- ✅ Умеете создавать полные бэкапы двумя способами.  
- ✅ Понимаете, какие метаданные хранятся в файле `.bak` и как их прочитать.  
- ✅ Знаете, где искать историю резервного копирования.  
- ✅ Осознаёте важность опций `CHECKSUM` и `COMPRESSION`.  
- ✅ Готовы к изучению разностных копий и копий журнала транзакций.

Эти навыки являются **фундаментом** для построения любой стратегии резервного копирования и восстановления.

---

## 📋 Требования к оформлению отчёта (Практическая работа №15)

**Файл должен называться:**  
`Отчёт_Фамилия_Группа_ПР15.docx`  
(например, `Отчёт_Ivanov_IS-23_ПР15.docx`)

**Структура отчёта:**

1. **Титульный лист**  
   — ФИО, номер группы, дата выполнения, название работы («Практическая работа №15. Создание полной резервной копии (SSMS + T-SQL)»).

2. **Цель работы**  
   (формулируется студентом).

3. **Ход выполнения:**  
   - **Стартовый скрипт** (создание БД и таблиц с вашей фамилией) – полный текст.  
   - **Скриншоты из SSMS** для каждого этапа (2–7) с подписями.  
     * Этап 2: окно настроек SSMS + сообщение об успехе.  
     * Этап 3: команда T-SQL и результат.  
     * Этап 4: результат `RESTORE HEADERONLY`.  
     * Этап 5: команда `BACKUP` с `NOINIT` и результат `RESTORE HEADERONLY` с двумя наборами.  
     * Этап 6: результат запроса к msdb.  
     * Этап 7: результат `RESTORE VERIFYONLY`.  
   - **Требования к скриншотам:**  
     * Виден текст команды (если применимо).  
     * Видно сообщение об успешном выполнении.  
     * В заголовке окна SSMS отображается имя вашей базы данных.  
     * Под каждым скриншотом — краткая подпись (что именно показано, какой этап).

4. **Сравнительный анализ (таблица наблюдений)**  
   Заполнить таблицу из раздела 📊 выше.

5. **Ответы на контрольные вопросы** (все 12 вопросов).

6. **Вывод**  
   - Что нового узнали о полных резервных копиях?  
   - Какие команды и приёмы обязательно пригодятся в реальной работе администратора?  
   - С какими трудностями столкнулись при выполнении эксперимента и как их преодолели?

---

## 📊 Критерии оценивания

| Критерий | Оценка «5» (отлично) | Оценка «4» (хорошо) | Оценка «3» (удовл.) |
|----------|-----------------------|----------------------|----------------------|
| **Стартовый скрипт** | БД создана с уникальным именем, таблицы и данные соответствуют заданию, скрипт без ошибок | БД создана, но есть незначительные замечания (например, пропущена одна из таблиц) | Скрипт не выполнен или содержит критические ошибки |
| **Этап 2 (SSMS)** | Настроены все параметры (INIT, VERIFY, CHECKSUM, сжатие), скриншоты чёткие, есть подписи | Копия создана, но отсутствует один из параметров или скриншотов | Задание не выполнено |
| **Этап 3 (T-SQL)** | Команда с полным набором параметров, скриншот с количеством страниц и скоростью | Команда выполнена, но часть параметров отсутствует | Команда не выполнена или с ошибками |
| **Этап 4 (анализ заголовка)** | Выполнен `RESTORE HEADERONLY`, проанализированы ключевые поля (LSN, размер, сжатие), скриншот | Запрос выполнен, но анализ поверхностный или отсутствует скриншот | Задание не выполнено |
| **Этап 5 (добавление + NOINIT)** | Добавлены данные, создана вторая копия, `RESTORE HEADERONLY` показывает два набора, скриншоты | Вторая копия создана, но нет анализа двух наборов | Задание не выполнено |
| **Этап 6 (msdb)** | Выполнен запрос к msdb, проанализированы все поля, скриншот | Запрос выполнен, но анализ неполный | Запрос не выполнен |
| **Этап 7 (проверка)** | `RESTORE VERIFYONLY` выполнен для обоих наборов, результат зафиксирован | Проверка выполнена только для одного набора | Проверка не выполнена |
| **Таблица наблюдений** | Таблица полностью заполнена, все числовые значения совпадают со скриншотами, логические выводы корректны | Таблица заполнена, но есть 1–2 пропуска или неточности | Таблица отсутствует или заполнена менее чем на 50% |
| **Ответы на вопросы** | Даны полные, развёрнутые ответы на все 12 вопросов, с использованием профессиональной терминологии | Ответы есть, но 2–3 ответа поверхностны или неполны | Большинство ответов отсутствуют или неверны |
| **Оформление отчёта** | Структура полностью соблюдена, скриншоты чёткие, таблица аккуратно оформлена, есть вывод | Незначительные нарушения структуры (например, нет титульного листа) | Отчёт неструктурирован, скриншоты нечитаемы |

**💡 Максимальная оценка: 5**  
**❗ Без заполненной таблицы наблюдений — максимальная оценка не выше «4»**  
**❗ Отсутствие выводов снижает оценку на 1 балл**

---

## 📤 Сдача работы

1. Оформите отчёт в **Microsoft Word**.  
2. Сохраните файл в формате **.docx** с именем по шаблону:  
   `Отчёт_Фамилия_Группа_ПР15.docx`  
3. Перейдите по ссылке на **Google-форму**:  
   🔗 [Google-форму 237](https://forms.gle/hazYv7aM2L71oC3s6)  
   *(та же ссылка, что и для практической работы №14)*  
4. Укажите **ФИО** и **группу**.  
5. Загрузите файл отчёта.  

📅 **Срок сдачи:** до **23:59 24 февраля 2026 г.**  

---

✅ **Удачи в выполнении!** Помните: резервное копирование — это единственная страховка от потери данных. Делайте бэкапы регулярно и всегда проверяйте их целостность.

---

📜 **Лицензия:** CC BY-NC-SA 4.0  
👨‍🏫 **Автор:** Руслан Ринатович Сафиулин  
📅 **Дата:** 16.02.2026  

---

🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON15.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON16.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №15](../LESSONS/LESSON15.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №16](../LESSONS/LESSON16.MD) |

---
