| [Предыдущее занятие](../LESSONS/LESSON12.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON13.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №11](../LESSONS/LESSON12.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №12](../LESSONS/LESSON13.MD) |

---

# **Практическая работа №11. Использование процедур для быстрой диагностики**

**Цель:** Научиться использовать системные хранимые процедуры для быстрой диагностики сервера, сравнить их эффективность с DMV и создать собственные процедуры для повседневного мониторинга.

**Продолжительность:** 2 академических часа  
**Оборудование:** SSMS, SQL Server Developer Edition

---

## **📊 Общий план работы**

**В ходе этой практической работы вам предстоит:**

1. **Использовать основные системные процедуры** для быстрой диагностики
2. **Сравнить эффективность** процедур и DMV-запросов
3. **Создать собственные диагностические процедуры**
4. **Проанализировать результаты** и сделать выводы

**📋 Таблица для заполнения по ходу работы:**

| № задания | Диагностическая задача | Используемые инструменты | Время выполнения (мс) | Преимущества | Недостатки |
|-----------|------------------------|--------------------------|---------------------|-------------|------------|
| 1 | Мониторинг активных сессий | `sp_who2` vs DMV | | | |
| 2 | Анализ баз данных | `sp_helpdb` vs DMV | | | |
| 3 | Анализ использования пространства | `sp_spaceused` vs DMV | | | |
| 4 | Создание процедуры быстрой диагностики | Custom procedure | | | |
| 5 | Комплексный сценарий диагностики | Все процедуры | | | |

**💡 Рекомендация:** Заполняйте таблицу по мере выполнения каждого задания.

---

## **Раздел 1. Сравнительный анализ процедур и DMV**

### **📋 Задание 1. Мониторинг активных сессий: sp_who2 vs DMV**

**Цель:** Сравнить скорость и удобство использования `sp_who2` с эквивалентным DMV-запросом.

**Шаг 1.1.** Использование системной процедуры:
```sql
SET STATISTICS TIME ON;
EXEC sp_who2 'active';
SET STATISTICS TIME OFF;
```

**Шаг 1.2.** Использование DMV-запросов:
```sql
SET STATISTICS TIME ON;

-- Эквивалент sp_who2 на DMV
SELECT 
    s.session_id AS SPID,
    s.login_name AS Login,
    s.host_name AS HostName,
    COALESCE(r.status, s.status) AS Status,
    r.blocking_session_id AS BlkBy,
    DB_NAME(COALESCE(r.database_id, s.database_id)) AS DBName,
    r.command AS Command,
    r.cpu_time AS CPUTime,
    r.reads + r.writes AS DiskIO,
    r.total_elapsed_time AS Time,
    s.program_name AS ProgramName,
    s.last_request_end_time AS LastBatch
FROM sys.dm_exec_sessions s
LEFT JOIN sys.dm_exec_requests r ON s.session_id = r.session_id
WHERE s.is_user_process = 1
AND (r.session_id IS NOT NULL OR s.status = 'running')
ORDER BY r.cpu_time DESC;

SET STATISTICS TIME OFF;
```

**💡 Что нужно проанализировать:**
- Время выполнения каждого подхода
- Полноту информации
- Удобство чтения результатов
- Возможности фильтрации и сортировки

**В отчёт:**
- Скриншоты результатов обоих подходов
- Время выполнения из вкладки "Сообщения"
- Подпись: *Рис. 1. Сравнение sp_who2 и DMV для мониторинга сессий*
- Заполненная строка таблицы для задания 1

---

### **📋 Задание 2. Анализ баз данных: sp_helpdb vs DMV**

**Цель:** Сравнить получение информации о базах данных разными методами.

**Шаг 2.1.** Использование системной процедуры:
```sql
SET STATISTICS TIME ON;
EXEC sp_helpdb;
SET STATISTICS TIME OFF;
```

**Шаг 2.2.** Использование DMV-запросов:
```sql
SET STATISTICS TIME ON;

-- Основная информация о БД
SELECT 
    name AS DatabaseName,
    database_id AS DBD,
    create_date AS Created,
    state_desc AS Status,
    recovery_model_desc AS RecoveryModel,
    compatibility_level AS CompatLevel,
    size/128.0 AS SizeMB
FROM sys.databases
WHERE state = 0;  -- Только онлайн БД

-- Детальная информация о файлах
SELECT 
    DB_NAME(database_id) AS DatabaseName,
    name AS LogicalName,
    type_desc AS FileType,
    size/128.0 AS SizeMB,
    max_size/128.0 AS MaxSizeMB,
    growth AS Growth,
    physical_name AS FilePath
FROM sys.master_files
ORDER BY database_id, file_id;

SET STATISTICS TIME OFF;
```

**💡 Что нужно проанализировать:**
- Какой подход дает более полную информацию о файлах
- Удобство получения информации о росте файлов
- Возможности фильтрации по конкретной БД

**В отчёт:**
- Скриншоты результатов обоих подходов
- Время выполнения из вкладки "Сообщения"
- Подпись: *Рис. 2. Сравнение sp_helpdb и DMV для анализа БД*
- Заполненная строка таблицы для задания 2

---

### **📋 Задание 3. Анализ использования пространства: sp_spaceused vs DMV**

**Цель:** Сравнить анализ использования дискового пространства.

**Шаг 3.1.** Использование системной процедуры для базы данных:
```sql
SET STATISTICS TIME ON;
USE MonitorLab;
EXEC sp_spaceused;
SET STATISTICS TIME OFF;
```

**Шаг 3.2.** Использование системной процедуры для таблицы:
```sql
SET STATISTICS TIME ON;
EXEC sp_spaceused 'sales.Orders';
SET STATISTICS TIME OFF;
```

**Шаг 3.3.** Использование DMV-запросов:
```sql
SET STATISTICS TIME ON;

-- Для всей БД
SELECT 
    DB_NAME() AS DatabaseName,
    SUM(size)/128.0 AS DatabaseSizeMB,
    SUM(size)/128.0 - SUM(FILEPROPERTY(name, 'SpaceUsed'))/128.0 AS FreeSpaceMB,
    SUM(FILEPROPERTY(name, 'SpaceUsed'))/128.0 AS UsedSpaceMB
FROM sys.database_files
WHERE type_desc = 'ROWS';

-- Для конкретной таблицы
SELECT 
    t.name AS TableName,
    SUM(s.used_page_count)/128.0 AS UsedSpaceMB,
    SUM(s.reserved_page_count)/128.0 AS ReservedSpaceMB,
    (SUM(s.reserved_page_count) - SUM(s.used_page_count))/128.0 AS FreeSpaceMB,
    p.rows AS RowCounts
FROM sys.dm_db_partition_stats s
INNER JOIN sys.tables t ON s.object_id = t.object_id
INNER JOIN sys.indexes i ON s.object_id = i.object_id AND s.index_id = i.index_id
INNER JOIN sys.partitions p ON s.partition_id = p.partition_id
WHERE t.name = 'Orders' AND i.index_id IN (0,1) -- Heap or clustered index
GROUP BY t.name, p.rows;

SET STATISTICS TIME OFF;
```

**💡 Что нужно проанализировать:**
- Точность данных о размерах
- Удобство получения информации для конкретных объектов
- Возможности анализа распределения пространства

**В отчёт:**
- Скриншоты результатов всех подходов
- Время выполнения из вкладки "Сообщений"
- Подпись: *Рис. 3. Сравнение sp_spaceused и DMV для анализа пространства*
- Заполненная строка таблицы для задания 3

---

## **Раздел 2. Создание собственных диагностических процедур**

### **📋 Задание 4. Разработка процедуры быстрой диагностики**

**Цель:** Создать собственную хранимую процедуру, объединяющую преимущества системных процедур и DMV.

**Шаг 4.1.** Создайте процедуру комплексной диагностики:
```sql
SET STATISTICS TIME ON;

CREATE PROCEDURE dbo.usp_QuickServerDiagnostics
    @DatabaseName NVARCHAR(128) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    
    PRINT '====== СИСТЕМНАЯ ДИАГНОСТИКА SQL SERVER ======';
    PRINT 'Время выполнения: ' + CONVERT(VARCHAR, GETDATE(), 120);
    PRINT '';
    
    -- 1. Активные сессии
    PRINT '1. АКТИВНЫЕ СЕССИИ (топ-10 по CPU):';
    SELECT TOP 10
        session_id AS SPID,
        login_name AS Login,
        host_name AS Host,
        status AS Status,
        cpu_time AS CPUMs,
        reads + writes AS DiskIO,
        program_name AS Program
    FROM sys.dm_exec_sessions 
    WHERE is_user_process = 1
    ORDER BY cpu_time DESC;
    
    PRINT '';
    
    -- 2. Топ-5 самых больших БД
    PRINT '2. ТОП-5 САМЫХ БОЛЬШИХ БАЗ ДАННЫХ:';
    SELECT TOP 5
        name AS DatabaseName,
        state_desc AS Status,
        size/128.0 AS SizeMB,
        recovery_model_desc AS RecoveryModel
    FROM sys.databases 
    WHERE state = 0
    ORDER BY size DESC;
    
    PRINT '';
    
    -- 3. Блокировки (если есть)
    PRINT '3. АКТИВНЫЕ БЛОКИРОВКИ:';
    SELECT 
        request_session_id AS BlockedSPID,
        blocking_session_id AS BlockingSPID,
        resource_type AS ResourceType,
        request_status AS Status,
        DB_NAME(resource_database_id) AS DatabaseName
    FROM sys.dm_tran_locks
    WHERE request_status = 'WAIT'
    AND resource_type <> 'DATABASE';
    
    -- 4. Если указана конкретная БД - детальная информация
    IF @DatabaseName IS NOT NULL
    BEGIN
        PRINT '';
        PRINT '4. ДЕТАЛЬНАЯ ИНФОРМАЦИЯ ПО БАЗЕ: ' + @DatabaseName;
        
        DECLARE @SQL NVARCHAR(MAX);
        SET @SQL = '
            USE [' + @DatabaseName + '];
            EXEC sp_spaceused;
        ';
        EXEC sp_executesql @SQL;
    END
    
    PRINT '';
    PRINT '====== ДИАГНОСТИКА ЗАВЕРШЕНА ======';
END
GO

SET STATISTICS TIME OFF;
```

**Шаг 4.2.** Протестируйте созданную процедуру:
```sql
SET STATISTICS TIME ON;

-- Быстрая диагностика всего сервера
EXEC dbo.usp_QuickServerDiagnostics;

-- Диагностика с фокусом на конкретную БД
EXEC dbo.usp_QuickServerDiagnostics @DatabaseName = 'MonitorLab';

SET STATISTICS TIME OFF;
```

**💡 Что нужно проанализировать:**
- Удобство использования одной процедуры вместо нескольких вызовов
- Полноту предоставляемой информации
- Скорость выполнения по сравнению с отдельными вызовами

**В отчёт:**
- Полный текст созданной процедуры
- Скриншоты результатов тестирования
- Время выполнения из вкладки "Сообщения"
- Подпись: *Рис. 4. Создание и тестирование процедуры быстрой диагностики*
- Заполненная строка таблицы для задания 4

---

### **📋 Задание 5. Процедура мониторинга использования пространства**

**Цель:** Создать специализированную процедуру для детального анализа использования дискового пространства.

**Шаг 5.1.** Создайте процедуру анализа пространства:
```sql
SET STATISTICS TIME ON;

CREATE PROCEDURE dbo.usp_DatabaseSpaceAnalysis
    @DatabaseName NVARCHAR(128) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    
    PRINT '====== АНАЛИЗ ИСПОЛЬЗОВАНИЯ ПРОСТРАНСТВА ======';
    
    -- Информация о файлах БД
    PRINT '1. ФАЙЛЫ БАЗЫ ДАННЫХ:';
    SELECT 
        DB_NAME(database_id) AS DatabaseName,
        name AS FileName,
        type_desc AS FileType,
        size/128.0 AS CurrentSizeMB,
        max_size/128.0 AS MaxSizeMB,
        growth AS GrowthPages,
        physical_name AS FilePath
    FROM sys.master_files
    WHERE @DatabaseName IS NULL OR DB_NAME(database_id) = @DatabaseName
    ORDER BY database_id, type_desc;
    
    PRINT '';
    
    -- Крупнейшие таблицы
    PRINT '2. КРУПНЕЙШИЕ ТАБЛИЦЫ:';
    
    DECLARE @SQL NVARCHAR(MAX) = '
    SELECT TOP 10
        t.name AS TableName,
        s.name AS SchemaName,
        SUM(p.rows) AS RowCounts,
        SUM(a.total_pages) * 8 AS TotalSpaceKB,
        SUM(a.used_pages) * 8 AS UsedSpaceKB,
        (SUM(a.total_pages) - SUM(a.used_pages)) * 8 AS UnusedSpaceKB
    FROM sys.tables t
    INNER JOIN sys.schemas s ON t.schema_id = s.schema_id
    INNER JOIN sys.indexes i ON t.object_id = i.object_id
    INNER JOIN sys.partitions p ON i.object_id = p.object_id AND i.index_id = p.index_id
    INNER JOIN sys.allocation_units a ON p.partition_id = a.container_id
    WHERE t.name NOT LIKE ''dt%'' AND i.object_id > 255
    GROUP BY t.name, s.name
    ORDER BY TotalSpaceKB DESC;';
    
    IF @DatabaseName IS NOT NULL
    BEGIN
        SET @SQL = 'USE [' + @DatabaseName + ']; ' + @SQL;
    END
    
    EXEC sp_executesql @SQL;
    
    PRINT '';
    PRINT '====== АНАЛИЗ ЗАВЕРШЕН ======';
END
GO

SET STATISTICS TIME OFF;
```

**Шаг 5.2.** Протестируйте процедуру:
```sql
SET STATISTICS TIME ON;

-- Анализ всех БД
EXEC dbo.usp_DatabaseSpaceAnalysis;

-- Анализ конкретной БД
EXEC dbo.usp_DatabaseSpaceAnalysis @DatabaseName = 'MonitorLab';

SET STATISTICS TIME OFF;
```

**В отчёт:**
- Полный текст процедуры анализа пространства
- Скриншоты результатов работы процедуры
- Время выполнения
- Подпись: *Рис. 5. Процедура детального анализа использования пространства*

---

## **Раздел 3. Комплексный сценарий диагностики**

### **📋 Задание 6. Решение практической задачи**

**Цель:** Применить полученные знания для решения реального сценария диагностики.

**Сценарий:** "Пользователи жалуются на медленную работу системы. Необходимо быстро определить причину."

**Шаг 6.1.** Выполните экспресс-диагностику с помощью системных процедур:
```sql
SET STATISTICS TIME ON;

-- 1. Быстрая проверка активностей
PRINT '=== ЭКСПРЕСС-ДИАГНОСТИКА СИСТЕМЫ ===';
PRINT '1. Активные сессии и блокировки:';
EXEC sp_who2 'active';

PRINT '2. Размеры баз данных:';
EXEC sp_helpdb;

PRINT '3. Использование пространства текущей БД:';
EXEC sp_spaceused;

SET STATISTICS TIME OFF;
```

**Шаг 6.2.** Если найдены проблемы, выполните углубленную диагностику:
```sql
SET STATISTICS TIME ON;

-- Использование созданных процедур
PRINT '=== УГЛУБЛЕННАЯ ДИАГНОСТИКА ===';
EXEC dbo.usp_QuickServerDiagnostics @DatabaseName = 'MonitorLab';
EXEC dbo.usp_DatabaseSpaceAnalysis @DatabaseName = 'MonitorLab';

SET STATISTICS TIME OFF;
```

**Шаг 6.3.** Создайте итоговый отчетный скрипт:
```sql
-- Процедура для генерации отчета диагностики
CREATE PROCEDURE dbo.usp_GenerateDiagnosticsReport
AS
BEGIN
    DECLARE @Report TABLE (
        Section NVARCHAR(100),
        Item NVARCHAR(200),
        Value NVARCHAR(500)
    );
    
    -- Активные сессии
    INSERT INTO @Report
    SELECT 'Active Sessions', 
           'Total user sessions', 
           CAST(COUNT(*) AS NVARCHAR) + ' sessions'
    FROM sys.dm_exec_sessions 
    WHERE is_user_process = 1 AND status IN ('running', 'sleeping');
    
    -- Базы данных
    INSERT INTO @Report
    SELECT 'Databases', 
           'Total online databases', 
           CAST(COUNT(*) AS NVARCHAR) + ' databases'
    FROM sys.databases 
    WHERE state = 0;
    
    -- Крупнейшая БД
    INSERT INTO @Report
    SELECT 'Databases', 
           'Largest database', 
           name + ' (' + CAST(size/128.0 AS NVARCHAR) + ' MB)'
    FROM sys.databases 
    WHERE state = 0
    ORDER BY size DESC OFFSET 0 ROWS FETCH NEXT 1 ROWS ONLY;
    
    -- Блокировки
    INSERT INTO @Report
    SELECT 'Locks', 
           'Active blocks', 
           CAST(COUNT(*) AS NVARCHAR) + ' blocking situations'
    FROM sys.dm_tran_locks
    WHERE request_status = 'WAIT';
    
    -- Вывод отчета
    SELECT Section, Item, Value FROM @Report
    ORDER BY Section, Item;
END
GO

-- Тестирование отчета
EXEC dbo.usp_GenerateDiagnosticsReport;
```

**В отчёт:**
- Скриншоты всех шагов диагностики
- Анализ эффективности каждого подхода
- Подпись: *Рис. 6. Комплексный сценарий диагностики проблем производительности*
- **Полностью заполненная таблица** для всех заданий

---

## **Раздел 4. Анализ результатов и выводы**

### **📋 Задание 7. Сравнительный анализ и ответы на вопросы**

**На основе выполненной работы ответьте на вопросы:**

1. **В каких ситуациях системные процедуры оказываются эффективнее DMV, а в каких — наоборот?**

2. **Какие преимущества дают созданные вами процедуры по сравнению со стандартными системными процедурами?**

3. **Какой подход быстрее для ежедневного рутинного мониторинга? Обоснуйте свой выбор.**

4. **С какими ограничениями системных процедур вы столкнулись и как их обошли?**

5. **Как бы вы организовали ежедневный мониторинг сервера на основе изученных инструментов?**

6. **Какие три процедуры вы бы включили в свой «боевой набор» администратора и почему?**

---

## **Требования к оформлению отчёта**

**Файл:** `Отчёт_Фамилия_Группа.docx` (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**
1. **Титульный лист** (ФИО, группа, дата, название работы)
2. **Цель работы**
3. **Ход выполнения:**
   - **Полностью заполненная таблица** сравнения
   - Скриншоты всех заданий 1-6 с подписями
   - Тексты созданных процедур
4. **Ответы на вопросы** из задания 7
5. **Вывод:**
   - Сравнительный анализ процедур и DMV
   - Практическая ценность созданных процедур
   - Рекомендации по использованию в реальной работе

---

## **Критерии оценивания**

| Критерий | Оценка «5» | Оценка «4» | Оценка «3» |
|----------|------------|------------|------------|
| **Сравнительный анализ** | Полное сравнение всех подходов | Сравнение 3-4 подходов | Поверхностное сравнение |
| **Создание процедур** | 2+ рабочие процедуры с комментариями | 1 процедура работает | Процедуры не работают |
| **Практический сценарий** | Полная диагностика с выводами | Диагностика выполнена частично | Сценарий не выполнен |
| **Ответы на вопросы** | Полные, с примерами из работы | 1-2 ответа неполные | Поверхностные ответы |
| **Оформление отчёта** | Структура соблюдена, есть таблица | Мелкие нарушения | Неструктурирован |

**💡 Максимальный балл: 5**  
**❗ Без таблицы сравнения — максимальная оценка «4»**

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **4 декабря 2025 г.**
---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON12.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON13.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №11](../LESSONS/LESSON12.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №12](../LESSONS/LESSON13.MD) |

---
