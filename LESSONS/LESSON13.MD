### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR11.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №11](../LESSONS/PR11.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №12](../LESSONS/PR12.MD) |

---

# **Лекция 12. Ограничения ресурсов: Resource Governor**

⏱️ **Продолжительность:** 90 минут

🎯 **Цель лекции:**  
Понять, как SQL Server распределяет ресурсы между разными приложениями и пользователями. Научиться создавать "виртуальные серверы" внутри одного физического сервера с гарантированной производительностью для критичных задач.

### **📖 Часть 0. Главное — запомните с самого начала**

> **💡 Resource Governor — это «виртуальные серверы» внутри одного физического SQL Server.**
>
> Представьте: у вас есть один мощный сервер, но нужно одновременно запускать:
> - Критичное ERP-приложение (должно работать всегда)
> - Тяжелые отчеты для аналитиков (могут подождать)  
> - Тестовые среды разработчиков (не должны мешать продакшену)
>
> **Resource Governor создает для каждого из них «виртуальный сервер» с гарантированными ресурсами.**

### **1. Зачем это нужно? Проблемы без управления ресурсами**

**🔹 Реальные сценарии, когда нужен Resource Governor:**

| Проблема | Что происходит | Решение |
|----------|----------------|---------|
| **«Шумный сосед»** | Один тяжелый отчет "вешает" все приложение | Ограничение ресурсов для отчетов |
| **Голодание критичных систем** | ERP не хватает CPU из-за аналитических запросов | Резервирование CPU для ERP |
| **Бесконтрольный рост** | Разработчики "убивают" сервер тестами | Жесткие лимиты для тестовых сред |

**🔹 Resource Governor доступен:**
- ✅ **Enterprise Edition** — все версии
- ✅ **Standard Edition** — начиная с SQL Server 2025
- ❌ **Express Edition** — недоступен

### **2. Архитектура Resource Governor: Три кита**

**🎯 Вся система построена на трех компонентах:**

#### **🏗️ Кит 1. Пулы ресурсов (Resource Pools) — «Бюджеты отделов»**

**Что это:** Виртуальные контейнеры с гарантированными ресурсами CPU и памяти.

**Как работает:**
```sql
-- Пул для критичных приложений - 60% ресурсов
CREATE RESOURCE POOL CriticalAppsPool
WITH (
    MIN_CPU_PERCENT = 40,      -- ГАРАНТИРОВАНО 40% CPU
    MAX_CPU_PERCENT = 100,     -- МОЖЕТ ИСПОЛЬЗОВАТЬ до 100%
    MIN_MEMORY_PERCENT = 50    -- ГАРАНТИРОВАНО 50% памяти
);

-- Пул для отчетов - 20% ресурсов  
CREATE RESOURCE POOL ReportsPool
WITH (
    MIN_CPU_PERCENT = 10,      -- ГАРАНТИРОВАНО 10% CPU
    MAX_CPU_PERCENT = 40,      -- НЕ БОЛЕЕ 40% CPU
    MIN_MEMORY_PERCENT = 10    -- ГАРАНТИРОВАНО 10% памяти
);
```

**💡 Ключевые параметры:**
- **MIN_%** — гарантированный минимум (даже при нагрузке)
- **MAX_%** — максимальный лимит (даже если свободно)
- **CAP_CPU_PERCENT** — абсолютный потолок (не может превысить)

#### **🏗️ Кит 2. Группы рабочей нагрузки (Workload Groups) — «Проекты внутри отделов»**

**Что это:** Логические группы для сессий с похожими характеристиками.

**Как работает:**
```sql
-- Группа для CRM-системы (привязана к пулу CriticalAppsPool)
CREATE WORKLOAD GROUP CRM_Group USING CriticalAppsPool
WITH (
    IMPORTANCE = HIGH,         -- ВЫСОКИЙ приоритет в пуле
    MAX_DOP = 4,               -- Макс. 4 ядра на запрос
    REQUEST_MAX_MEMORY_GRANT_PERCENT = 40  -- Макс. 40% памяти на запрос
);

-- Группа для ERP-системы (ТОТ ЖЕ ПУЛ, но другие настройки)
CREATE WORKLOAD GROUP ERP_Group USING CriticalAppsPool  
WITH (
    IMPORTANCE = HIGH,
    MAX_DOP = 2,               -- Макс. 2 ядра на запрос
    REQUEST_MAX_MEMORY_GRANT_PERCENT = 30
);
```

### **🚨 ВАЖНОЕ УГЛУБЛЕНИЕ: Параметр IMPORTANCE**

**Что такое IMPORTANCE?**  
Это настройка, которая определяет **приоритет доступа к ресурсам ЦПУ внутри одного пула**, когда возникает конкуренция.

**💡 Ключевые особенности IMPORTANCE:**

```sql
-- Пример из лекции с пояснениями:
CREATE WORKLOAD GROUP ERP_Group USING BusinessPool WITH (IMPORTANCE = HIGH);
CREATE WORKLOAD GROUP Sales_Group USING BusinessPool WITH (IMPORTANCE = HIGH);  
CREATE WORKLOAD GROUP Monthly_Reports USING ReportsPool WITH (IMPORTANCE = MEDIUM);
```

**🔹 Как работает IMPORTANCE на практике:**

| Сценарий | Поведение |
|----------|-----------|
| **Без конкуренции** | Все группы работают на полной скорости |
| **Конкуренция ВНУТРИ пула** | Сначала обслуживаются HIGH, потом MEDIUM, потом LOW |
| **Конкуренция МЕЖДУ пулами** | IMPORTANCE не работает между разными пулами |

**🔹 Детальное объяснение на примере:**

```sql
-- ПУЛ 1: Бизнес-приложения
CREATE WORKLOAD GROUP ERP_Group USING BusinessPool WITH (IMPORTANCE = HIGH);
CREATE WORKLOAD GROUP Internal_Tools USING BusinessPool WITH (IMPORTANCE = LOW);

-- ПУЛ 2: Отчеты  
CREATE WORKLOAD GROUP Critical_Reports USING ReportsPool WITH (IMPORTANCE = HIGH);
CREATE WORKLOAD GROUP Regular_Reports USING ReportsPool WITH (IMPORTANCE = MEDIUM);
```

**Что происходит при нагрузке:**
1. **Внутри BusinessPool**: ERP_Group получит ЦПУ в 4 раза чаще, чем Internal_Tools
2. **Внутри ReportsPool**: Critical_Reports получит ЦПУ в 2 раза чаще, чем Regular_Reports  
3. **Между пулами**: BusinessPool с MIN_CPU=60% гарантированно получит свои 60%, даже если в ReportsPool все группы имеют HIGH importance

**🔹 Коэффициенты приоритета:**
- **HIGH** = 3 единицы ресурса
- **MEDIUM** = 2 единицы ресурса  
- **LOW** = 1 единица ресурса

**То есть при конкуренции в одном пуле:**
- HIGH получает в 3 раза больше ресурсов, чем LOW
- HIGH получает в 1.5 раза больше ресурсов, чем MEDIUM
- MEDIUM получает в 2 раза больше ресурсов, чем LOW

**🔹 Когда IMPORTANCE НЕ РАБОТАЕТ:**
- Когда ресурсов хватает всем
- Между разными пулами ресурсов
- Для операций, не требующих CPU (ожидание I/O, блокировки)

#### **🏗️ Кит 3. Классификатор (Classifier Function) — «Секретарь на ресепшене»**

**Что это:** Функция, которая автоматически определяет, в какую группу направить новое подключение.

**Как работает:**
```sql
CREATE FUNCTION dbo.rg_classifier()
RETURNS SYSNAME
AS
BEGIN
    DECLARE @group SYSNAME;
    
    -- CRM-система → группа CRM_Group
    IF APP_NAME() LIKE '%CRM%'
        SET @group = 'CRM_Group';
    
    -- ERP-система → группа ERP_Group  
    ELSE IF APP_NAME() LIKE '%ERP%'
        SET @group = 'ERP_Group';
        
    -- Система отчетов → группа Reports_Group
    ELSE IF APP_NAME() LIKE '%Report%'
        SET @group = 'Reports_Group';
        
    -- Все остальные → группа по умолчанию
    ELSE
        SET @group = 'default';
        
    RETURN @group;
END
```

**💡 Что анализирует классификатор:**
- `APP_NAME()` — имя приложения
- `SUSER_NAME()` — логин пользователя  
- `HOST_NAME()` — компьютер подключения
- `DB_NAME()` — база данных

### **3. Как все это работает вместе**

#### **🎯 Полный цикл работы:**

1. **Пользователь открывает приложение** (например, CRM-систему)
2. **Приложение подключается** к SQL Server
3. **Автоматически вызывается классификатор** 
4. **Классификатор анализирует** APP_NAME = 'SalesCRM_Application'
5. **Возвращает имя группы** → 'CRM_Group'
6. **Сессия попадает в группу** CRM_Group
7. **Группа CRM_Group использует пул** CriticalAppsPool
8. **Все запросы этой сессии** получают ресурсы из CriticalAppsPool

#### **🎯 Ключевые правила:**

| Правило | Объяснение |
|---------|------------|
| **1 сессия = 1 группа** | Одно подключение не может быть в двух группах |
| **1 группа = 1 пул** | Группа всегда привязана к одному пулу |
| **1 пул = много групп** | Пул может обслуживать несколько групп |
| **Автоматическое распределение** | Не нужно вручную назначать пользователей |

#### **🎯 Визуальная архитектура:**

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   ПОДКЛЮЧЕНИЯ   │    │  ГРУППЫ          │    │   ПУЛЫ          │
│                 │    │  WORKLOAD        │    │  RESOURCE       │
│  CRM-приложение ├───►│  CRM_Group       ├───►│  CriticalApps   │
│                 │    │  (HIGH priority) │    │  (MIN_CPU=40%)  │
│  ERP-приложение ├───►│  ERP_Group       │    │                 │
│                 │    │  (HIGH priority) │    │                 │
│  Отчеты         ├───►│  Reports_Group   ├───►│  ReportsPool    │
│                 │    │  (MEDIUM priority)│   │  (MAX_CPU=40%)  │
│  Тесты          ├───►│  Dev_Group       ├───►│  DevPool        │
│                 │    │  (LOW priority)  │    │  (MAX_CPU=10%)  │
└─────────────────┘    └──────────────────┘    └─────────────────┘
         ↑                      ↑                       ↑
         │                      │                       │
    Классификатор          Приоритеты внутри       Гарантии и лимиты
   (автоматическое          общего бюджета          физических ресурсов
    распределение)                                 
```

### **4. Практические сценарии использования**

#### **🎯 Сценарий 1: Защита продакшена от отчетов**

**Проблема:** В конце месяца отчеты по продажам "подвешивают" ERP-систему.

**Решение:**
```sql
-- Резервируем 60% ресурсов для бизнес-приложений
CREATE RESOURCE POOL BusinessPool WITH (MIN_CPU_PERCENT = 60);

-- Ограничиваем отчеты 30% ресурсов
CREATE RESOURCE POOL ReportsPool WITH (MAX_CPU_PERCENT = 30);

-- Группы для разных типов workload'ов
CREATE WORKLOAD GROUP ERP_Group USING BusinessPool WITH (IMPORTANCE = HIGH);
CREATE WORKLOAD GROUP Sales_Group USING BusinessPool WITH (IMPORTANCE = HIGH);  
CREATE WORKLOAD GROUP Monthly_Reports USING ReportsPool WITH (IMPORTANCE = MEDIUM);
```

**💡 Что происходит в этом сценарии:**
- **ERP_Group и Sales_Group** получают ВЫСОКИЙ приоритет внутри пула BusinessPool
- **Monthly_Reports** получает СРЕДНИЙ приоритет внутри пула ReportsPool  
- **При конкуренции внутри BusinessPool**: ERP и Sales получают равный высокий приоритет
- **При конкуренции внутри ReportsPool**: Monthly_Reports уступит место, если появятся группы с HIGH importance

**Результат:** ERP и продажи всегда получат минимум 60% CPU, даже когда отчеты создают пиковую нагрузку.

#### **🎯 Сценарий 2: Приоритизация внутри одного отдела**

**Проблема:** В отделе продаж CRM важнее внутренних отчетов.

**Решение:**
```sql
-- Один пул на весь отдел продаж
CREATE RESOURCE POOL SalesPool WITH (MIN_CPU_PERCENT = 40);

-- Но разные приоритеты внутри пула
CREATE WORKLOAD GROUP Sales_CRM USING SalesPool WITH (IMPORTANCE = HIGH);
CREATE WORKLOAD GROUP Sales_Reports USING SalesPool WITH (IMPORTANCE = LOW);
```

**Результат:** Когда не хватает CPU, CRM-запросы получают в 3 раза больше ресурсов, чем внутренние отчеты.

### **5. Что можно контролировать с помощью Resource Governor**

#### **🎯 Управление CPU:**
- **Гарантированный минимум** — даже при 100% нагрузке
- **Максимальный лимит** — даже если ресурсы свободны
- **Приоритеты внутри пула** — кто первый получает ресурсы

#### **🎯 Управление памятью:**
- **Гарантированная память** для пулов
- **Лимиты памяти на запрос** в группах
- **Защита от "прожорливых" запросов**

#### **🎯 Дополнительные ограничения:**
- **MAX_DOP** — максимальное число ядер на запрос
- **GROUP_MAX_REQUESTS** — максимальное число одновременных запросов
- **IMPORTANCE** — приоритет внутри пула (HIGH/MEDIUM/LOW)

### **6. Мониторинг и управление**

#### **🎯 Как проверить, что все работает:**
```sql
-- Какие сессии в каких группах
SELECT 
    s.session_id,
    s.program_name,
    wg.name AS workload_group,
    rp.name AS resource_pool
FROM sys.dm_exec_sessions s
LEFT JOIN sys.dm_resource_governor_workload_groups wg ON s.group_id = wg.group_id
LEFT JOIN sys.dm_resource_governor_resource_pools rp ON wg.pool_id = rp.pool_id
WHERE s.is_user_process = 1;

-- Посмотреть статистику использования IMPORTANCE
SELECT 
    name,
    total_cpu_usage_ms,
    total_cpu_delayed_ms,
    importance = CASE 
        WHEN importance = 'High' THEN 'ВЫСОКИЙ (3x)'
        WHEN importance = 'Medium' THEN 'СРЕДНИЙ (2x)' 
        WHEN importance = 'Low' THEN 'НИЗКИЙ (1x)'
    END
FROM sys.dm_resource_governor_workload_groups;
```

#### **🎯 Основные команды управления:**
```sql
-- Включить Resource Governor
ALTER RESOURCE GOVERNOR ENABLE;

-- Выключить Resource Governor  
ALTER RESOURCE GOVERNOR DISABLE;

-- Применить изменения настроек
ALTER RESOURCE GOVERNOR RECONFIGURE;
```

### **7. Ограничения и лучшие практики**

#### **⚠️ Важные ограничения:**
- **Только CPU и память** — не управляет I/O, сетью
- **Минимальные квоты** — SUM(MIN_%) не может превышать 100%
- **Сложность отладки** — требует тщательного тестирования
- **IMPORTANCE работает только внутри пула** — не влияет на распределение между пулами

#### **✅ Лучшие практики:**
1. **Мониторинг перед настройкой** — неделю собирайте статистику
2. **Постепенное внедрение** — начинайте с не-критичных workload'ов  
3. **Резерв capacity** — всегда оставляйте 10-20% для непредвиденных нагрузок
4. **Тестирование на staging** — никогда не настраивайте сразу на production
5. **Используйте IMPORTANCE для тонкой настройки** — после настройки пулов

### **8. Вопросы для самопроверки**

1. **В чем главное преимущество Resource Governor?**
   → Гарантированная производительность критичных систем при конкуренции за ресурсы.

2. **Может ли одна сессия быть в двух группах одновременно?**
   → НЕТ! 1 сессия = 1 группа.

3. **Чем MIN_CPU_PERCENT отличается от MAX_CPU_PERCENT?**
   → MIN — гарантированный минимум, MAX — максимальный лимит.

4. **Как классификатор понимает, куда направить подключение?**
   → Анализирует APP_NAME(), SUSER_NAME(), HOST_NAME() и другие параметры.

5. **Зачем создавать несколько групп в одном пуле?**
   → Для разных приоритетов (IMPORTANCE) и ограничений внутри общего "бюджета" ресурсов.

6. **Сравните IMPORTANCE = HIGH в маленьком пуле и IMPORTANCE = LOW в большом пуле. Что важнее?**
   → Размер пула важнее! Группа с LOW importance в пуле с MIN_CPU=60% получит больше ресурсов, чем группа с HIGH importance в пуле с MIN_CPU=10%.

### **9. Подведение итогов**

**🎯 5 ключевых мыслей:**

1. **Resource Governor — это «виртуальные серверы»** внутри одного физического сервера
2. **Пулы → Группы → Классификатор** — три уровня управления ресурсами  
3. **MIN/MAX проценты** — основа гарантий и ограничений
4. **IMPORTANCE** — мощный инструмент для приоритизации внутри пулов
5. **Автоматическое распределение** — не требует ручного вмешательства

**🔜 Что дальше?**
В Практической работе №12 вы:
- Научитесь мониторить использование CPU и памяти через DMV
- Проанализируете текущую нагрузку на сервере  
- Создадите базовую конфигурацию Resource Governor для тестовой среды
- На практике увидите, как работает IMPORTANCE при конкуренции за ресурсы

---

## Теперь вся архитектура Resource Governor должна быть понятна — от подключения пользователя до распределения физических ресурсов! 🎯

---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR11.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №11](../LESSONS/PR11.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №12](../LESSONS/PR12.MD) |
