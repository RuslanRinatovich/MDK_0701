# 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR13_3.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR14.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №13.3](../LESSONS/PR13_3.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №14](../LESSONS/PR14.MD) |

---

# 🎓 Лекция 14. Модели восстановления: SIMPLE, FULL, BULK_LOGGED  
⏱️ **Продолжительность:** 90 минут  
🎯 **Цель лекции:**  
Сформировать у студентов чёткое понимание, что выбор модели восстановления — это **компромисс между производительностью и безопасностью данных**. Студенты научатся различать модели, прогнозировать поведение журнала транзакций и обоснованно выбирать модель для конкретной базы данных.

---

## 📖 Справочник терминов (официальные названия из русской SSMS)  
💡 *Используйте эту таблицу как шпаргалку — все термины соответствуют интерфейсу SSMS и русской локализации.*

| Русский термин (в SSMS)               | Английский эквивалент       | Что это?                                                                                   |
|----------------------------------------|-----------------------------|--------------------------------------------------------------------------------------------|
| Модель восстановления                 | Recovery model              | Режим работы журнала транзакций, определяющий возможность восстановления.                  |
| Простая                               | SIMPLE                      | Автоматическое усечение журнала; точка во времени недоступна.                              |
| Полная                                | FULL                        | Полное логирование; требуется резервное копирование журнала.                               |
| С неполным протоколированием          | BULK_LOGGED                 | Минимальное логирование массовых операций; ограниченный PITR.                              |
| Журнал транзакций                    | Transaction log             | Физический файл `.ldf`, содержащий записи обо всех изменениях.                             |
| Резервная копия журнала транзакций   | Transaction log backup      | Бэкап, позволяющий усечь журнал и восстановить данные на момент времени.                   |
| Усечение журнала                     | Log truncation              | Освобождение места в журнале (помечается как переиспользуемое).                            |
| Размер журнала (МБ)                 | Log Size (MB)               | Текущий размер файла `.ldf`.                                                               |
| Используемое пространство (%)       | Log Space Used (%)          | Доля активной части журнала.                                                               |
| Точка во времени                    | Point in Time               | Конкретный момент в прошлом, до которого можно восстановить БД.                            |
| Восстановление до точки отказа      | Restore to point of failure | Восстановление всех изменений, произошедших до момента аварии.                             |
| Контрольная точка                  | CHECKPOINT                  | Запись грязных страниц на диск; в SIMPLE запускает усечение журнала.                       |

---

## 1. 🎥 Зачем нужна модель восстановления? Аналогия с камерой наблюдения  
Представьте, что вы смотрите прямую трансляцию с камеры видеонаблюдения. У вас есть три режима записи:

- **SIMPLE** 🟢 — камера пишет поверх старой записи каждые 10 минут. Вернуться на час назад — невозможно.  
- **FULL** 🔴 — есть огромный жёсткий диск, записывается **каждое** движение. Можно отмотать на любую секунду.  
- **BULK_LOGGED** 🟡 — вы снимаете закат. Вместо покадровой записи делаете одно длинное фото с выдержкой. Файл меньше, но отдельные кадры не восстановить.  

**Главный вопрос лекции:** сколько данных вы готовы потерять в случае сбоя?

---

## 2. 🧱 Архитектурная основа: Журнал транзакций (Transaction Log)  

### 📁 Физический файл `.ldf`
- Хранит **все** изменения данных (INSERT, UPDATE, DELETE, CREATE, ALTER и др.).  
- Без журнала транзакций SQL Server не может гарантировать ACID (Atomicity, Consistency, Isolation, Durability).  

### 🧩 Логическая структура: виртуальные файлы журнала (VLF)
- Журнал разбит на VLF (Virtual Log Files) — небольшие сегменты.  
- Количество VLF зависит от начального размера и автонастройки.  
- Чрезмерное количество мелких VLF замедляет восстановление и резервное копирование.  

### ✍️ Принцип Write-Ahead Logging (WAL)
> «Сначала запись в журнал, потом — в файл данных».  
Если произойдёт сбой, SQL Server при старте **накатит** зафиксированные транзакции из журнала в данные или **откатит** незавершённые.

### 🔴 Активная и ⚪ неактивная части журнала
- **Активная часть** — содержит незавершённые транзакции. Никогда не удаляется!  
- **Неактивная часть** — транзакции завершены (`COMMIT`), но их записи могут храниться для восстановления. Судьба неактивной части **определяется моделью восстановления**.

---

## 3. 🔍 Три модели восстановления: детальный разбор  

### 🟢 **A. Простая (SIMPLE)**

| Характеристика               | Описание                                                                         |
|------------------------------|----------------------------------------------------------------------------------|
| Запись в журнал              | Минимальная информация, достаточная для отката транзакций.                       |
| Усечение журнала             | **Автоматическое** при каждой контрольной точке (CHECKPOINT).                    |
| Резервное копирование журнала| **НЕ требуется** (команда `BACKUP LOG` запрещена).                              |
| Восстановление               | Только на момент последнего полного/разностного бэкапа. **Point-in-Time НЕТ**.  |
| Риск потери данных           | Все изменения после последнего бэкапа.                                           |
| Где применять                | Dev/Test, статические хранилища, базы с допустимой потерей данных за сутки.     |

```sql
ALTER DATABASE MyDB SET RECOVERY SIMPLE;
```

---

### 🔴 **Б. Полная (FULL)**

| Характеристика               | Описание                                                                                     |
|------------------------------|----------------------------------------------------------------------------------------------|
| Запись в журнал              | Каждая транзакция логируется **полностью**.                                                  |
| Усечение журнала             | **ТОЛЬКО** после выполнения `BACKUP LOG`. Автоматического усечения НЕТ!                     |
| Резервное копирование журнала| **ОБЯЗАТЕЛЬНО** (обычно каждые 10–15 минут).                                                |
| Восстановление               | До любого момента времени (Point-in-Time Recovery, PITR), включая точку отказа.              |
| Риск потери данных           | Только изменения, выполненные после последнего бэкапа лога (обычно 0–15 минут).              |
| Где применять                | Промышленные БД с высокими требованиями к целостности (банки, CRM, ERP).                     |

```sql
ALTER DATABASE MyDB SET RECOVERY FULL;
```

⚠️ **Важно!**  
Если в модели FULL **не делать резервные копии журнала**, файл `.ldf` будет расти до бесконечности, пока не заполнит диск. Сервер не остановится автоматически — база станет недоступна для записи.

---

### 🟡 **В. С неполным протоколированием (BULK_LOGGED)**

| Характеристика               | Описание                                                                                     |
|------------------------------|----------------------------------------------------------------------------------------------|
| Обычные операции             | Логируются полностью (как в FULL).                                                           |
| Массовые операции            | `BULK INSERT`, `SELECT INTO`, `CREATE INDEX`, `ALTER INDEX REBUILD` — логируются **минимально** (только факт и ссылки на экстенты). |
| Размер журнала               | Значительно меньше при массовых загрузках.                                                   |
| Point-in-Time Recovery       | **НЕ РАБОТАЕТ**, если в логе есть хотя бы одна массовая операция.                            |
| Усечение                     | Только после `BACKUP LOG`.                                                                   |
| Применение                   | Временный режим на период массового импорта/индексации. После операции — вернуть FULL.       |

```sql
ALTER DATABASE MyDB SET RECOVERY BULK_LOGGED;
-- выполнить массовую вставку
ALTER DATABASE MyDB SET RECOVERY FULL;
BACKUP DATABASE MyDB TO DISK = '...';
```

---

## 4. 🛠️ Как посмотреть и изменить модель (SSMS vs T-SQL)  

### 💻 **Через SSMS:**
1. ПКМ на базе данных → **Свойства** → **Параметры**.  
2. В разделе **Состояние** строка **Модель восстановления**.  

### ⌨️ **Через T-SQL:**

```sql
-- Узнать модель всех баз
SELECT name, recovery_model_desc 
FROM sys.databases;

-- Узнать модель текущей БД
SELECT recovery_model_desc 
FROM sys.databases 
WHERE name = DB_NAME();

-- Изменить модель
ALTER DATABASE Имя_БД SET RECOVERY FULL;
ALTER DATABASE Имя_БД SET RECOVERY SIMPLE;
ALTER DATABASE Имя_БД SET RECOVERY BULK_LOGGED;
```

---

## 5. 📍 Где находится файл .ldf?  

Файл журнала транзакций (`.ldf`) — это физический файл на диске. Его местоположение можно узнать и изменить.

### 🔎 Как узнать путь к .ldf?

**Через T-SQL:**
```sql
USE имя_базы;
GO
SELECT 
    name AS [Логическое_имя],
    physical_name AS [Физический_путь],
    type_desc AS [Тип_файла]
FROM sys.database_files
WHERE type_desc = 'LOG';
```

**Через SSMS:**
1. ПКМ на базе данных → **Свойства** → **Файлы**.  
2. В столбце **Путь** будет полный путь к файлу `.ldf`.

### 📂 Типичные расположения по умолчанию:

| Версия SQL Server          | Путь по умолчанию (Program Files)                                      |
|----------------------------|------------------------------------------------------------------------|
| SQL Server 2019/2022       | `C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA` |
| SQL Server Developer/Express | Аналогично, в папке `DATA` экземпляра                                 |

### 🛠️ Можно ли переместить .ldf?

Да. Это частая задача администратора — вынести журнал на отдельный быстрый диск (RAID 10, SSD).  
Выполняется через:
```sql
ALTER DATABASE Имя_БД
MODIFY FILE (NAME = логическое_имя_файла, FILENAME = 'Новый_путь\файл.ldf');
```
*После этого требуется остановить/запустить экземпляр или перевести БД в OFFLINE/ONLINE.*

---

## 6. 🔍 Можно ли заглянуть внутрь .ldf?  

Содержимое журнала транзакций — бинарные структуры. Просто открыть `.ldf` в Блокноте нельзя.  
Но SQL Server предоставляет **недокументированные** функции для чтения записей журнала. Также есть сторонние инструменты.

### 🛠️ Способ 1. Функция `fn_dblog`
Читает **активный** журнал транзакций текущей базы.
```sql
USE Имя_БД;
SELECT * FROM fn_dblog(NULL, NULL);
```
⚠️ Вывод огромен и труден для восприятия без фильтров.  
Пример: найти все `DELETE` операции:
```sql
SELECT 
    [Current LSN],
    Operation,
    [Transaction ID],
    [Begin Time],
    [Description]
FROM fn_dblog(NULL, NULL)
WHERE Operation IN ('LOP_DELETE_ROWS', 'LOP_DELETE_ROWS');
```

### 🛠️ Способ 2. Функция `fn_dump_dblog`
Читает **резервную копию журнала** (файл `.trn`).
```sql
SELECT * 
FROM fn_dump_dblog(
    NULL, NULL, N'DISK', 1, N'C:\Backup\TestRecovery_LogBackup.trn',
    DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT,
    DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT,
    DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT,
    DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT, DEFAULT
);
```

### 🛠️ Способ 3. DBCC PAGE
Позволяет читать отдельные страницы (если известен номер страницы журнала). Требует флага трассировки 3604.

### 🛠️ Способ 4. Сторонние инструменты
| Продукт                 | Разработчик       | Особенности                                  |
|-------------------------|-------------------|----------------------------------------------|
| **ApexSQL Log**         | ApexSQL           | Читает .ldf и бэкапы, восстанавливает строки|
| **Quest Toad for SQL Server** | Quest        | Графический просмотр лога                   |
| **Red Gate SQL Log Rescue** (бесплатный, только SQL 2000) | Red Gate | Устарел |

✅ Эти инструменты показывают изменения в понятном виде:  
> *«Пользователь Ivanov удалил 15 строк из таблицы dbo.Orders 22.01.2026 в 14:35:12»*

### ⚠️ Предостережения
- **Не читайте лог на production-серверах** без крайней необходимости.  
- Недокументированные функции могут измениться в новых версиях.  
- Конфиденциальные данные могут быть видны в логе (если не используется TDE).  
- Запрос `SELECT * FROM fn_dblog(NULL, NULL)` на большом логе может выполняться долго и грузить диск — всегда фильтруйте.

📌 **Для администратора в повседневной работе прямой анализ лога — это «тяжёлая артиллерия».**  
Достаточно:
- знать размер лога (`DBCC SQLPERF(LOGSPACE)`);
- настроить регулярные бэкапы лога (в FULL);
- уметь восстанавливать базу на момент времени.

---

## 7. 📊 Демонстрация: поведение лога в разных моделях (живой код)  

### 🔹 Подготовка (для демонстрации)

```sql
CREATE DATABASE DemoRecovery;
GO
USE DemoRecovery;
GO
CREATE TABLE dbo.Demo (id INT IDENTITY, col CHAR(1000) DEFAULT 'A');
GO
```

### 🔹 **SIMPLE** – усечение по CHECKPOINT
```sql
ALTER DATABASE DemoRecovery SET RECOVERY SIMPLE;
CHECKPOINT;
-- Вставляем 5000 строк
INSERT INTO dbo.Demo (col) SELECT TOP 5000 'B' FROM sys.objects a CROSS JOIN sys.objects b;
DBCC SQLPERF(LOGSPACE);  -- Log Space Used ≈ высокий
CHECKPOINT;
DBCC SQLPERF(LOGSPACE);  -- Log Space Used упал (усечение)
```

### 🔹 **FULL** – лог растёт, пока не сделаем BACKUP LOG
```sql
ALTER DATABASE DemoRecovery SET RECOVERY FULL;
INSERT INTO dbo.Demo (col) SELECT TOP 5000 'C' FROM sys.objects a CROSS JOIN sys.objects b;
DBCC SQLPERF(LOGSPACE);  -- Log Size вырос, Log Space Used ~90-100%
BACKUP LOG DemoRecovery TO DISK = 'C:\Backup\DemoLog.bak';
DBCC SQLPERF(LOGSPACE);  -- Log Space Used упал
```

---

## 8. ✅ Резюме: чек-лист администратора  

| Модель     | Усечение            | Нужен бэкап лога? | Point-in-Time | Риск роста лога          |
|------------|---------------------|-------------------|---------------|--------------------------|
| **SIMPLE** | Авто (CHECKPOINT)   | ❌ Нет            | ❌ Нет        | Низкий (автоусечение)   |
| **FULL**   | Только BACKUP LOG   | ✅ Да (обязательно) | ✅ Да         | **Высокий** без бэкапов |
| **BULK_LOGGED** | Только BACKUP LOG | ✅ Да (обязательно) | ❌ Нет (при массовых) | Средний |

🔑 **Золотое правило администрирования:**  
> **«Выбрал FULL — настрой резервное копирование журнала транзакций!»**

---

## 9. ❓ Вопросы для самопроверки  

1. Почему в модели `SIMPLE` журнал транзакций не растёт бесконечно?  
2. Какая команда освобождает пространство в журнале в модели `FULL`?  
3. Можно ли восстановить БД на момент «за 5 минут до удаления данных», если модель была `SIMPLE`?  
4. Почему после массовой операции `BULK INSERT` в модели `BULK_LOGGED` теряется возможность PITR?  
5. Администратор установил `FULL`, забыл настроить бэкапы лога. Диск с логом заполнен. Сервер работает, но операции записываются? (Да/Нет, объяснить).  
6. Что такое VLF и почему их не должно быть слишком много?  
7. Как узнать текущую модель восстановления базы данных через T-SQL?  
8. Где физически располагается файл `.ldf` и как его переместить?  
9. Можно ли прочитать содержимое журнала транзакций? Если да, то какими способами?  

---

## 10. 🔜 Подведение итогов и связь с практической работой  

Сегодня мы разобрали **теоретический фундамент** резервного копирования. Без понимания моделей восстановления невозможно грамотно настроить стратегию бэкапов и, что важнее, **восстановить данные при реальной аварии**.  

🧪 **В практической работе №14** вы:  
- создадите тестовую БД и измените её модель;  
- **увидите своими глазами**, как растёт журнал в `FULL` и как он усекается в `SIMPLE`;  
- выполните резервное копирование журнала и убедитесь, что только оно освобождает место;  
- проанализируете логический размер журнала с помощью `DBCC SQLPERF` и отчётов SSMS.  

---

## 📎 Приложение: основные команды по теме  

| Действие                                    | Команда T-SQL                                                                          |
|---------------------------------------------|----------------------------------------------------------------------------------------|
| Узнать модель восстановления               | `SELECT name, recovery_model_desc FROM sys.databases;`                                |
| Переключить в SIMPLE                       | `ALTER DATABASE Имя SET RECOVERY SIMPLE;`                                             |
| Переключить в FULL                         | `ALTER DATABASE Имя SET RECOVERY FULL;`                                               |
| Переключить в BULK_LOGGED                  | `ALTER DATABASE Имя SET RECOVERY BULK_LOGGED;`                                        |
| Резервное копирование журнала              | `BACKUP LOG Имя TO DISK = 'путь\файл.trn';`                                           |
| Информация о журнале (размер/заполнение)   | `DBCC SQLPERF(LOGSPACE);`                                                             |
| Размер файлов данных и журнала             | `SELECT name, type_desc, size*8/1024 AS size_MB FROM sys.database_files;`             |
| Принудительная контрольная точка           | `CHECKPOINT;`                                                                         |
| Узнать путь к файлу .ldf                  | `SELECT physical_name FROM sys.database_files WHERE type_desc = 'LOG';`               |
| Переместить .ldf (после изменения пути)    | `ALTER DATABASE Имя MODIFY FILE (NAME = лог_имя, FILENAME = 'новый_путь\файл.ldf');` |
| Чтение активного лога (недокументировано)  | `SELECT * FROM fn_dblog(NULL, NULL);`                                                |
| Чтение резервной копии лога               | `SELECT * FROM fn_dump_dblog(...);` (см. выше)                                       |

---

📜 **Лицензия:** CC BY-NC-SA 4.0  
👨‍🏫 **Автор:** Руслан Ринатович Сафиулин  
📅 **Дата:** 12.02.2026  

---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/PR13_3.MD) | &nbsp; | [Следующее занятие](../LESSONS/PR14.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Практика №13.3](../LESSONS/PR13_3.MD) | 📖 [Содержание](../README.MD) | 💻 [Практика №14](../LESSONS/PR14.MD) |
