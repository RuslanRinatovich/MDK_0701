| [Предыдущее занятие](../LESSONS/LESSON11.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №10](../LESSONS/LESSON11.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №11](../LESSONS/LESSON12.MD) |

---

# **Практическая работа №10. Мониторинг активных подключений и нагрузки**
**Цель:** Научиться использовать динамические административные представления (DMV) для мониторинга активности на SQL Server, идентификации проблемных сессий, анализа блокировок и определения ресурсоёмких запросов.

**Продолжительность:** 2 академических часа  
**Оборудование:** SSMS, SQL Server Developer Edition

---

## **📊 Общий план работы**

**В ходе этой практической работы вам предстоит:**

1. **Создать тестовую базу данных** и наполнить её данными
2. **Выполнить 6 диагностических запросов** к различным DMV
3. **Заполнить сравнительную таблицу** по мере выполнения заданий
4. **Проанализировать результаты** и ответить на контрольные вопросы

**📋 Таблица для заполнения по ходу работы:**

| № задания | Диагностическая задача | Используемые DMV | Ключевые метрики | Время выполнения (мс) | Практическое применение |
|-----------|------------------------|------------------|------------------|---------------------|-------------------------|
| 1 | Мониторинг активных сессий | `sys.dm_exec_sessions` | `session_id`, `login_name`, `cpu_time`, `status` | | Ежедневный мониторинг подключений |
| 2 | Анализ выполняемых запросов | `sys.dm_exec_requests`, `sys.dm_exec_sql_text` | `wait_type`, `wait_time`, `QueryText`, `cpu_time` | | Диагностика "тормозящих" запросов |
| 3 | Диагностика блокировок | `sys.dm_exec_requests`, `sys.dm_exec_sql_text`, `sys.dm_tran_active_transactions` | `BlockedSessionID`, `WaitType`, `WaitTimeMs`, `TranState` | | Обнаружение блокировок в реальном времени |
| 4 | Анализ статистики ожиданий | `sys.dm_os_wait_stats` | `WaitType`, `TotalWaitTimeMs`, `AvgWaitTimeMs`, `SignalWaitPercent` | | Выявление "узких мест" сервера |
| 5 | Поиск ресурсоёмких запросов | `sys.dm_exec_query_stats`, `sys.dm_exec_sql_text` | `TotalCPUMs`, `AvgCPUMs`, `AvgLogicalReads`, `execution_count` | | Поиск "тяжелых" запросов для оптимизации |
| 6 | Комплексный мониторинг | `sys.dm_exec_sessions`, `sys.dm_exec_requests`, `sys.dm_exec_sql_text` | `SPID`, `RequestStatus`, `CurrentWaitType`, `QueryText`, `BlockedBySPID` | | Единый обзор всей активности сервера |

**📝 Справка по заполнению таблицы:**

- **Столбец "Используемые DMV"** - основные динамические представления из задания
- **Столбец "Ключевые метрики"** - 3-5 самых важных показателей из запроса  
- **Столбец "Время выполнения (мс)"** - время из вкладки "Сообщения" с `STATISTICS TIME ON`
- **Столбец "Практическое применение"** - реальные сценарии использования запроса

**💡 Рекомендация:** Заполняйте таблицу по мере выполнения каждого задания.

---

## **Раздел 1. Подготовка учебной среды**

### **📥 Задание 0. Стартовый скрипт**

```sql
-- Практическая работа №10: Мониторинг активных подключений и нагрузки
-- Скрипт подготовлен: [Ваша Фамилия]

USE master;
GO
ALTER DATABASE MonitorLab SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
GO
DROP DATABASE IF EXISTS MonitorLab;
GO
CREATE DATABASE MonitorLab;
GO

USE MonitorLab;
GO
CREATE SCHEMA sales;
GO

CREATE TABLE sales.Orders (
    OrderID int IDENTITY(1,1) PRIMARY KEY,
    CustomerID int,
    OrderDate date,
    Amount decimal(10,2),
    Status varchar(20)
);

CREATE TABLE sales.Customers (
    CustomerID int IDENTITY(1,1) PRIMARY KEY,
    CustomerName nvarchar(100),
    Email nvarchar(100)
);

INSERT INTO sales.Customers (CustomerName, Email)
SELECT TOP 1000
    'Customer ' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS varchar(10)),
    'email' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS varchar(10)) + '@test.com'
FROM master.dbo.spt_values a CROSS JOIN master.dbo.spt_values b;

INSERT INTO sales.Orders (CustomerID, OrderDate, Amount, Status)
SELECT TOP 5000
    ABS(CHECKSUM(NEWID())) % 1000 + 1,
    DATEADD(day, -ABS(CHECKSUM(NEWID())) % 365, GETDATE()),
    CAST(RAND(CHECKSUM(NEWID())) * 1000 AS decimal(10,2)),
    CASE WHEN RAND() > 0.5 THEN 'active' ELSE 'completed' END
FROM master.dbo.spt_values a CROSS JOIN master.dbo.spt_values b;

CREATE INDEX IX_Orders_Customer ON sales.Orders(CustomerID);
CREATE INDEX IX_Orders_Status ON sales.Orders(Status);
CREATE INDEX IX_Orders_Date ON sales.Orders(OrderDate);
GO

PRINT 'База данных MonitorLab успешно создана и наполнена тестовыми данными';
```

**В отчёт:** 
- Полный текст скрипта с вашей фамилией
- Скриншот с сообщением выполнения
- Подпись: *Рис. 1. Создание тестовой базы данных MonitorLab*

---

## **Раздел 2. Мониторинг активных сессий и запросов**

### **📋 Задание 1. Обзор активных сессий**

**Цель:** Получить список всех активных пользовательских сессий и их характеристик.

```sql
SET STATISTICS TIME ON;

SELECT 
    session_id AS SPID,
    login_name AS Login,
    host_name AS Host,
    program_name AS Program,
    status AS Status,
    cpu_time AS CPUMs,
    memory_usage AS MemoryUsageKB,
    reads AS LogicalReads,
    writes AS Writes,
    logical_reads AS TotalReads
FROM sys.dm_exec_sessions
WHERE is_user_process = 1
ORDER BY cpu_time DESC;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**
- **`session_id` (SPID)** - идентификатор процесса сервера
- **`login_name`** - имя пользователя, подключившегося к серверу
- **`host_name`** - компьютер, с которого выполнено подключение
- **`program_name`** - программа, использующая подключение (SSMS, приложение и т.д.)
- **`status`** - состояние сессии (running, sleeping, dormant)
- **`cpu_time`** - время CPU в миллисекундах, использованное сессией
- **`memory_usage`** - использование памяти в 8КБ страницах
- **`reads`, `writes`, `logical_reads`** - операции ввода-вывода

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Нормальные значения |
|---------|----------------|---------------------|
| **`cpu_time`** | Общую нагрузку на CPU от сессии | < 1000 мс - низкая, > 10000 мс - требует внимания |
| **`status`** | Активность сессии | running - активно, sleeping - ожидает |
| **`logical_reads`** | Интенсивность работы с данными | < 1000 - низкая, > 100000 - высокая |

**💡 Особенности работы `sys.dm_exec_sessions`:**
- Показывает все сессии, включая системные (фильтр `is_user_process = 1`)
- Данные накапливаются с момента подключения сессии
- Сбрасываются при отключении сессии

**Пример ожидаемых результатов:**
```
SPID | Login     | Host       | Program | Status  | CPUMs | LogicalReads
-----|-----------|------------|---------|---------|-------|-------------
55   | student   | COMPUTER1  | SSMS    | running | 150   | 1200
63   | student   | COMPUTER1  | SSMS    | sleeping| 80    | 800
```

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 0 мс, истекшее время = 5 мс.

Время выполнения SQL: 
   Время ЦП = 15 мс, истекшее время = 25 мс.
```

**💡 Что делать, если результаты показывают только системные сессии:**
Это нормально! Убедитесь, что фильтр `is_user_process = 1` включен и есть активные пользовательские подключения.

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот **результатов**
2. Перейти на вкладку "Сообщения" и найти **время выполнения диагностического запроса**
3. Проанализировать, какие сессии активны и какие ресурсы они используют
4. Заполнить строку таблицы для задания 1

**В отчёт:**
- Скриншот с **результатом выполнения запроса** (таблица сессий)
- Скриншот с **сообщениями о времени выполнения диагностического запроса**
- Подпись: *Рис. 2. Мониторинг активных пользовательских сессий*
- Заполненная строка в таблице для задания 1

---

### **📋 Задание 2. Анализ выполняющихся запросов**

**Цель:** Увидеть, какие запросы выполняются прямо сейчас, и получить их текст.

```sql
SET STATISTICS TIME ON;

SELECT 
    r.session_id AS SPID,
    r.status AS RequestStatus,
    r.command AS CommandType,
    r.wait_type AS WaitType,
    r.wait_time AS WaitTimeMs,
    r.cpu_time AS CPUMs,
    r.total_elapsed_time AS DurationMs,
    r.reads AS LogicalReads,
    r.writes AS Writes,
    t.text AS QueryText
FROM sys.dm_exec_requests r
CROSS APPLY sys.dm_exec_sql_text(r.sql_handle) t
WHERE r.session_id > 50;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**
- **`session_id` (SPID)** - идентификатор процесса сервера, выполняющего запрос
- **`status`** - состояние запроса (running, suspended, sleeping)
- **`command`** - тип команды (SELECT, INSERT, UPDATE, DELETE и т.д.)
- **`wait_type`** - тип ожидания, если запрос заблокирован (NULL если выполняется)
- **`wait_time`** - время ожидания в миллисекундах
- **`cpu_time`** - время CPU в миллисекундах, использованное ЭТИМ запросом
- **`total_elapsed_time`** - общее время выполнения ЭТОГО запроса
- **`reads`, `writes`** - операции ввода-вывода, выполненные ЭТИМ запросом
- **`text`** - полный текст SQL-запроса, который выполняется

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Критическое значение |
|---------|----------------|---------------------|
| **`wait_type`** | Причину блокировки запроса | LCK_* - блокировки, PAGEIOLATCH_* - проблемы с диском |
| **`wait_time`** | Длительность ожидания | > 5000 мс - требует вмешательства |
| **`total_elapsed_time`** | Общее время выполнения | > 30000 мс - долгий запрос |

**💡 Особенности работы `sys.dm_exec_requests`:**
- Показывает только активные выполняющиеся запросы
- Данные в реальном времени, обновляются постоянно
- Для получения текста запроса требуется соединение с `sys.dm_exec_sql_text`

**🔍 Ключевые отличия от задания 1:**
- `sys.dm_exec_sessions` показывает ВСЕ сессии (даже "спящие")
- `sys.dm_exec_requests` показывает ТОЛЬКО активные выполняющиеся запросы
- Здесь мы видим конкретный текст SQL, который выполняется в каждой сессии

**Пример ожидаемых результатов:**
```
SPID | RequestStatus | CommandType | WaitType  | WaitTimeMs | QueryText
-----|---------------|-------------|-----------|------------|-----------
55   | running       | SELECT      | NULL      | 0          | SELECT * FROM sales.Orders...
63   | suspended     | SELECT      | LCK_M_S   | 2500       | SELECT * FROM sales.Orders...
```

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 2 мс, истекшее время = 8 мс.

Время выполнения SQL: 
   Время ЦП = 12 мс, истекшее время = 45 мс.
```

**💡 Что делать, если в результатах пусто:**
Это нормально! Это значит, что в данный момент нет активных выполняющихся запросов. Чтобы увидеть данные, можно:
- Открыть новое окно запроса и выполнить длительный запрос
- Создать блокировку (как в задании 3)
- Выполнить запрос во время работы других заданий

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот **результатов**
2. Перейти на вкладку "Сообщения" и найти **время выполнения диагностического запроса**
3. Проанализировать активные запросы и их характеристики
4. Заполнить строку таблицы для задания 2

**В отчёт:**
- Скриншот с **результатом выполнения запроса** (таблица с выполняющимися запросами)
- Скриншот с **сообщениями о времени выполнения диагностического запроса**
- Подпись: *Рис. 3. Анализ выполняющихся запросов с текстом SQL*
- Заполненная строка в таблице для задания 2

---

## **Раздел 3. Диагностика блокировок и ожиданий**

### **📋 Задание 3. Создание и анализ блокировок**

**Цель:** Научиться идентифицировать и анализировать ситуации блокировок.

**Шаг 3.1.** Выполните в **первом окне** запроса (создание блокировки):
```sql
BEGIN TRANSACTION;
    UPDATE sales.Orders SET Amount = Amount * 1.1 WHERE Status = 'active';
    WAITFOR DELAY '00:02:00';
-- COMMIT; -- Не выполняем специально для создания блокировки
```

**Шаг 3.2.** Выполните во **втором окне** запрос жертвы блокировки:
```sql
SELECT * FROM sales.Orders WHERE Status = 'active';
```

**Шаг 3.3.** Выполните в **третьем окне** диагностический запрос блокировок:
```sql
SET STATISTICS TIME ON;

SELECT 
    r.session_id AS BlockedSessionID,
    r.blocking_session_id AS BlockingSessionID,
    r.wait_type AS WaitType,
    r.wait_time AS WaitTimeMs,
    r.status AS RequestStatus,
    DB_NAME(r.database_id) AS DatabaseName,
    blocking_text.text AS BlockingQueryText,
    blocked_text.text AS BlockedQueryText
FROM sys.dm_exec_requests r
OUTER APPLY sys.dm_exec_sql_text(r.sql_handle) blocked_text
OUTER APPLY sys.dm_exec_sql_text(
    (SELECT sql_handle FROM sys.dm_exec_requests WHERE session_id = r.blocking_session_id)
) blocking_text
WHERE r.blocking_session_id > 0;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**
- **`BlockedSessionID`** - ID сессии, которая заблокирована и ждет ресурс
- **`BlockingSessionID`** - ID сессии, которая вызывает блокировку
- **`WaitType`** - тип ожидания (LCK_M_S - shared lock, LCK_M_X - exclusive lock и т.д.)
- **`WaitTimeMs`** - время ожидания в миллисекундах
- **`RequestStatus`** - состояние запроса (suspended - приостановлен, running - выполняется)
- **`DatabaseName`** - база данных, где происходит блокировка
- **`BlockingQueryText`** - текст запроса, который вызывает блокировку
- **`BlockedQueryText`** - текст запроса, который заблокирован

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Критическое значение |
|---------|----------------|---------------------|
| **`WaitType`** | Тип блокировки | LCK_M_S - разделяемая, LCK_M_X - монопольная |
| **`WaitTimeMs`** | Время блокировки | > 10000 мс - требует немедленного вмешательства |
| **`BlockingSessionID`** | Источник блокировки | NULL - системная блокировка |

**💡 Особенности анализа блокировок:**
- Блокировки возникают при конкурентном доступе к данным
- Долгие блокировки (> 30 сек) могут указывать на проблемные транзакции
- `LCK_M_S` - запрос ждет снятия разделяемой блокировки
- `LCK_M_X` - запрос ждет снятия монопольной блокировки

**Пример ожидаемых результатов:**
```
BlockedSessionID | BlockingSessionID | WaitType | WaitTimeMs | BlockedQueryText
-----------------|-------------------|----------|------------|-----------------
63              | 55                | LCK_M_S  | 2500       | SELECT * FROM sales.Orders...
```

**Шаг 3.4.** Выполните дополнительный запрос для анализа активных транзакций:
```sql
SET STATISTICS TIME ON;

SELECT 
    st.session_id AS SPID,
    s.login_name AS LoginName,
    s.host_name AS HostName,
    s.program_name AS ProgramName,
    at.transaction_id AS TranID,
    CASE at.transaction_type
        WHEN 1 THEN 'Read/Write' WHEN 2 THEN 'Read-only' 
        WHEN 3 THEN 'System' WHEN 4 THEN 'Distributed'
        ELSE 'Unknown'
    END AS TranType,
    CASE at.transaction_state
        WHEN 0 THEN 'Not initialized' WHEN 1 THEN 'Initialized'
        WHEN 2 THEN 'Active' WHEN 3 THEN 'Ended' 
        WHEN 4 THEN 'Committing' WHEN 5 THEN 'Prepared'
        WHEN 6 THEN 'Committed' WHEN 7 THEN 'Rolling back'
        WHEN 8 THEN 'Rolled back' ELSE 'Unknown'
    END AS TranState,
    at.name AS TranName,
    DB_NAME(s.database_id) AS CurrentDatabase
FROM sys.dm_tran_active_transactions at
INNER JOIN sys.dm_tran_session_transactions st ON at.transaction_id = st.transaction_id
INNER JOIN sys.dm_exec_sessions s ON st.session_id = s.session_id;

SET STATISTICS TIME OFF;
```

**💡 Что показывает дополнительный запрос:**
- **`SPID`** - ID сессии с активной транзакцией
- **`LoginName`, `HostName`, `ProgramName`** - информация о подключении
- **`TranID`** - уникальный идентификатор транзакции
- **`TranType`** - тип транзакции в понятном виде
- **`TranState`** - состояние транзакции (Active, Committing, Rolling back и т.д.)
- **`TranName`** - имя транзакции (если задано)
- **`CurrentDatabase`** - текущая база данных сессии

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 0 мс, истекшее время = 2 мс.

Время выполнения SQL: 
   Время ЦП = 5 мс, истекшее время = 15 мс.
```

**💡 Что делать, если в результатах пусто:**
- Убедитесь, что транзакция в шаге 3.1 не завершилась
- Проверьте, что SELECT в шаге 3.2 все еще выполняется
- Попробуйте быстро выполнить диагностику после создания блокировки

**Что нужно сделать:**
1. Создать блокировку по инструкции (шаги 3.1 и 3.2)
2. Выполнить диагностические запросы (шаги 3.3 и 3.4)
3. Зафиксировать время выполнения каждого диагностического запроса
4. Заполнить строку таблицы для задания 3

**В отчёт:**
- Скриншот с результатом анализа блокировок (шаг 3.3)
- Скриншот с результатом анализа активных транзакций (шаг 3.4)
- Скриншоты с сообщениями о времени выполнения диагностических запросов
- Подпись: *Рис. 4. Анализ блокировок: идентификация заблокированных сессий и активных транзакций*
- Заполненная строка в таблице для задания 3

---

### **📋 Задание 4. Анализ статистики ожиданий**

**Цель:** Определить самые частые причины ожиданий на сервере за все время его работы.

```sql
SET STATISTICS TIME ON;

SELECT TOP 10
    wait_type AS WaitType,
    waiting_tasks_count AS WaitCount,
    wait_time_ms AS TotalWaitTimeMs,
    wait_time_ms / waiting_tasks_count AS AvgWaitTimeMs,
    signal_wait_time_ms AS SignalWaitTimeMs,
    (signal_wait_time_ms * 100.0 / wait_time_ms) AS SignalWaitPercent
FROM sys.dm_os_wait_stats
WHERE waiting_tasks_count > 0
AND wait_time_ms > 0
ORDER BY wait_time_ms DESC;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**
- **`WaitType`** - тип ожидания (CXPACKET, PAGEIOLATCH_*, LCK_*, WRITELOG и т.д.)
- **`WaitCount`** - общее количество ожиданий этого типа с последнего перезапуска
- **`TotalWaitTimeMs`** - общее время ожиданий в миллисекундах
- **`AvgWaitTimeMs`** - среднее время одного ожидания
- **`SignalWaitTimeMs`** - время, которое процессы ждали в очереди CPU ("готовы к выполнению")
- **`SignalWaitPercent`** - процент времени ожидания CPU от общего времени ожидания

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Критическое значение |
|---------|----------------|---------------------|
| **`TotalWaitTimeMs`** | Общую нагрузку от ожиданий | > 1,000,000 мс - требует анализа |
| **`AvgWaitTimeMs`** | Среднюю задержку | > 1000 мс - высокая задержка |
| **`SignalWaitPercent`** | Нагрузку на CPU | > 25% - возможна нехватка CPU |

**💡 Особенности работы `sys.dm_os_wait_stats`:**
- Данные накапливаются с момента последнего перезапуска SQL Server
- Показывает накопленную статистику, а не текущее состояние
- Статистика обнуляется при перезапуске службы

**🔍 Ключевые типы ожиданий и их значение:**

| WaitType | Что означает | Возможные причины |
|----------|---------------|-------------------|
| **CXPACKET** | Ожидание параллельного выполнения | Дисбаланс в параллельных запросах, неоптимальные планы |
| **PAGEIOLATCH_*** | Ожидание чтения/записи страниц с диска | Медленные диски, нехватка памяти (Buffer Pool) |
| **LCK_*** | Ожидание блокировок | Долгие транзакции, недостаток индексов |
| **WRITELOG** | Ожидание записи в журнал транзакций | Медленный диск журнала, частые коммиты |

**💡 Интерпретация SignalWaitPercent:**
- **< 10-15%** - норма, основное время - ожидание ресурсов (диск, блокировки)
- **> 25-30%** - возможна нехватка CPU, процессы ждут в очереди на выполнение

**Пример ожидаемых результатов:**
```
WaitType           | WaitCount | TotalWaitTimeMs | AvgWaitTimeMs | SignalWaitPercent
-------------------|-----------|-----------------|---------------|------------------
CXPACKET           | 15000     | 4500000         | 300           | 12.5
PAGEIOLATCH_SH     | 8000      | 3200000         | 400           | 8.2
LCK_M_S            | 5000      | 2500000         | 500           | 5.1
```

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 1 мс, истекшее время = 3 мс.

Время выполнения SQL: 
   Время ЦП = 8 мс, истекшее время = 22 мс.
```

**💡 Важное замечание:**
Если вы видите в основном системные ожидания (SOS_WORK_DISPATCHER, LAZYWRITER_SLEEP и т.д.) - это нормально для тестового сервера без нагрузки.

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот **результатов** (топ-10 ожиданий)
2. Перейти на вкладку "Сообщения" и найти **время выполнения диагностического запроса**
3. Проанализировать, какие типы ожиданий преобладают на вашем сервере
4. Заполнить строку таблицы для задания 4

**В отчёт:**
- Скриншот с **результатом выполнения запроса** (таблица с типами ожиданий)
- Скриншот с **сообщениями о времени выполнения диагностического запроса**
- Подпись: *Рис. 5. Анализ накопленной статистики ожиданий сервера*
- Заполненная строка в таблице для задания 4

---

## **Раздел 4. Анализ производительности и ресурсов**

### **📋 Задание 5. Поиск ресурсоёмких запросов**

**Цель:** Найти запросы, которые потребляли больше всего ресурсов за все время работы сервера (с момента последнего перезапуска).

```sql
SET STATISTICS TIME ON;

SELECT TOP 5
    qs.total_worker_time / 1000 AS TotalCPUMs,
    qs.execution_count,
    (qs.total_worker_time / 1000) / qs.execution_count AS AvgCPUMs,
    qs.total_logical_reads,
    qs.total_logical_reads / qs.execution_count AS AvgLogicalReads,
    qs.total_elapsed_time / 1000 AS TotalDurationMs,
    SUBSTRING(qt.text, qs.statement_start_offset/2 + 1,
        (CASE WHEN qs.statement_end_offset = -1
              THEN LEN(CONVERT(nvarchar(max), qt.text)) * 2
              ELSE qs.statement_end_offset
         END - qs.statement_start_offset)/2 + 1) AS QueryText
FROM sys.dm_exec_query_stats qs
CROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) qt
ORDER BY qs.total_worker_time DESC;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**
- **`TotalCPUMs`** - общее время CPU в миллисекундах, затраченное на все выполнения этого запроса
- **`execution_count`** - сколько раз выполнялся этот запрос
- **`AvgCPUMs`** - среднее время CPU на одно выполнение запроса
- **`total_logical_reads`** - общее количество логических чтений (страниц из памяти)
- **`AvgLogicalReads`** - среднее количество логических чтений на одно выполнение
- **`TotalDurationMs`** - общее время выполнения запроса (включая ожидания)
- **`QueryText`** - текст SQL-запроса (извлекается часть запроса, если он большой)

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Нормальные значения |
|---------|----------------|---------------------|
| **AvgCPUMs** | Нагрузку на процессор | < 100 мс - хорошо, > 1000 мс - требует оптимизации |
| **AvgLogicalReads** | Эффективность использования памяти/индексов | < 1000 - хорошо, > 10000 - требует оптимизации |
| **execution_count** | Популярность запроса | Высокое значение + высокий AvgCPUMs = кандидат на оптимизацию |

**💡 Особенности работы `sys.dm_exec_query_stats`:**
- Данные накапливаются с момента последнего перезапуска SQL Server
- Показывает только запросы, планы которых еще находятся в кэше
- Статистика обнуляется при перезапуске службы или очистке кэша

**Пример ожидаемых результатов на тестовом сервере:**
```
TotalCPUMs | execution_count | AvgCPUMs | TotalLogicalReads | AvgLogicalReads | QueryText
-----------|-----------------|----------|-------------------|-----------------|-----------
150        | 3               | 50       | 4500              | 1500            | SELECT * FROM sales.Orders...
80         | 2               | 40       | 2400              | 1200            | INSERT INTO sales.Customers...
```

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 2 мс, истекшее время = 5 мс.

Время выполнения SQL: 
   Время ЦП = 15 мс, истекшее время = 35 мс.
```

**💡 Что делать, если результаты пустые или показывают системные запросы:**
Это нормально для чистого тестового сервера! Вы можете создать нагрузку для демонстрации:

```sql
-- Выполните несколько раз чтобы создать статистику
SELECT COUNT(*) FROM sales.Orders WHERE Status = 'active';
SELECT * FROM sales.Customers WHERE CustomerName LIKE 'Customer%';
INSERT INTO sales.Orders (CustomerID, OrderDate, Amount, Status) 
VALUES (1, GETDATE(), 100.00, 'active');
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот **результатов**
2. Перейти на вкладку "Сообщения" и найти **время выполнения диагностического запроса**
3. Проанализировать, какие запросы потребляют больше всего ресурсов
4. Заполнить строку таблицы для задания 5

**В отчёт:**
- Скриншот с **результатом выполнения запроса** (топ-5 ресурсоемких запросов)
- Скриншот с **сообщениями о времени выполнения диагностического запроса**
- Подпись: *Рис. 6. Анализ исторической статистики ресурсоемких запросов*
- Заполненная строка в таблице для задания 5

---

**🎯 Практическое применение:**
Этот запрос помогает ответить на вопросы:
- Какие запросы создают наибольшую нагрузку на CPU?
- Какие запросы неэффективно используют память/индексы?
- Какие запросы выполняются чаще всего и требуют оптимизации?
- Где сосредоточить усилия по настройке производительности?

---

## **Раздел 5. Комплексный мониторинговый отчет**

### **📋 Задание 6. Комплексный мониторинговый отчет**

**Цель:** Создать единый запрос, который даёт максимально полную картину о текущей активности сервера, объединяя информацию о сессиях, запросах и ресурсах.

```sql
SET STATISTICS TIME ON;

SELECT
    s.session_id AS SPID,
    s.login_name AS Login,
    s.host_name AS Host,
    s.program_name AS Program,
    r.status AS RequestStatus,
    r.command AS CommandType,
    r.wait_type AS CurrentWaitType,
    r.wait_time AS WaitTimeMs,
    r.cpu_time AS CPUMs,
    r.total_elapsed_time AS DurationMs,
    r.reads AS LogicalReads,
    r.writes AS Writes,
    t.text AS QueryText,
    r.blocking_session_id AS BlockedBySPID,
    s.memory_usage AS MemoryKB,
    s.logical_reads AS SessionReads
FROM sys.dm_exec_sessions s
LEFT JOIN sys.dm_exec_requests r ON s.session_id = r.session_id
OUTER APPLY sys.dm_exec_sql_text(r.sql_handle) t
WHERE s.is_user_process = 1
AND (r.session_id IS NOT NULL OR s.status = 'sleeping')
ORDER BY r.cpu_time DESC;

SET STATISTICS TIME OFF;
```

**💡 Что показывает основной запрос:**

**Информация о сессиях:**
- **`SPID`** - идентификатор процесса сервера
- **`Login`** - имя пользователя
- **`Host`** - компьютер, с которого подключение
- **`Program`** - программа (SSMS, приложение и т.д.)
- **`MemoryKB`** - использование памяти в 8КБ страницах
- **`SessionReads`** - общее количество логических чтений за сессию

**Информация о текущих запросах:**
- **`RequestStatus`** - состояние запроса (running, suspended, sleeping)
- **`CommandType`** - тип команды (SELECT, INSERT, UPDATE, etc.)
- **`CurrentWaitType`** - тип ожидания если запрос заблокирован
- **`WaitTimeMs`** - время ожидания в миллисекундах
- **`CPUMs`** - время CPU использованное текущим запросом
- **`DurationMs`** - общее время выполнения текущего запроса
- **`LogicalReads`, `Writes`** - операции ввода-вывода текущего запроса
- **`QueryText`** - текст выполняемого SQL-запроса
- **`BlockedBySPID`** - ID сессии, которая блокирует текущую (если есть)

**🔍 Ключевые метрики и их значение:**

| Метрика | Что показывает | Критическое значение |
|---------|----------------|---------------------|
| **`CurrentWaitType`** | Причину простоя запроса | LCK_*, PAGEIOLATCH_* - требуют внимания |
| **`WaitTimeMs`** | Длительность простоя | > 5000 мс - требует вмешательства |
| **`BlockedBySPID`** | Наличие блокировок | NOT NULL - обнаружена блокировка |

**💡 Особенности этого запроса:**
1. **`LEFT JOIN` между сессиями и запросами** - показываются ВСЕ сессии, даже без активных запросов
2. **`OUTER APPLY` для текста запросов** - текст показывается только для активных запросов
3. **Фильтр `s.is_user_process = 1`** - только пользовательские сессии (исключаем системные)
4. **Условие `r.session_id IS NOT NULL OR s.status = 'sleeping'`** - активные запросы + "спящие" сессии
5. **Сортировка `ORDER BY r.cpu_time DESC`** - самые ресурсоемкие запросы вверху

**💡 Интерпретация статусов сессий:**
- **`running`** - запрос активно выполняется на CPU
- **`suspended`** - запрос приостановлен и ждет ресурс (диск, блокировку)
- **`sleeping`** - сессия не выполняет запрос, ждет новой команды
- **`background`** - фоновая системная задача

**Пример ожидаемых результатов:**
```
SPID | Login      | Program          | RequestStatus | CommandType | CurrentWaitType | QueryText
-----|------------|------------------|---------------|-------------|-----------------|----------
63   | student    | SSMS             | running       | SELECT      | NULL            | SELECT * FROM sales.Orders...
55   | student    | SSMS             | suspended     | SELECT      | LCK_M_S         | SELECT * FROM sales.Orders...
51   | student    | SSMS             | sleeping      | NULL        | NULL            | NULL
```

**Пример вывода в "Сообщения":**
```
Время синтаксического анализа и компиляции SQL: 
   Время ЦП = 3 мс, истекшее время = 8 мс.

Время выполнения SQL: 
   Время ЦП = 25 мс, истекшее время = 65 мс.
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот **результатов**
2. Перейти на вкладку "Сообщения" и найти **время выполнения диагностического запроса**
3. Проанализировать общую картину активности на сервере
4. Заполнить строку таблицы для задания 6
5. **Завершить заполнение всей таблицы**

**В отчёт:**
- Скриншот с **результатом выполнения запроса** (комплексный отчет)
- Скриншот с **сообщениями о времени выполнения диагностического запроса**
- Подпись: *Рис. 7. Комплексный отчет мониторинга активности сервера*
- **Полностью заполненная таблица** для всех заданий

---

**🎯 Практическое применение:**
Этот запрос - "швейцарский нож" администратора, который позволяет:
- Видеть всю активность на сервере в одном месте
- Быстро находить блокировки и "тормозящие" запросы
- Определять, какие программы создают нагрузку
- Мониторить использование ресурсов в реальном времени
- Получать текст проблемных запросов для дальнейшего анализа

**💡 Профессиональный совет:**
Этот запрос можно сохранить как шаблон и использовать для ежедневного мониторинга production-серверов!

---

## **Раздел 6. Анализ результатов и выводы**

### **📋 Задание 7. Анализ эффективности диагностических запросов**

**Цель:** Проанализировать полученные результаты и сделать выводы о практическом применении DMV.

**На основе заполненной таблицы ответьте на вопросы:**

1. **Какой диагностический запрос оказался самым быстрым? Какой - самым медленным? С чем это может быть связано?**

2. **Какие DMV предоставляют информацию о текущем состоянии сервера, а какие - о накопленной статистике? Приведите примеры использования каждого типа.**

3. **В задании 3 вы создали и проанализировали блокировку. Опишите по шагам ваши действия для диагностики этой ситуации и какие DMV оказались наиболее полезными?**

4. **Что показывает столбец `SignalWaitPercent` в статистике ожиданий? Какие действия должен предпринять администратор при высоком значении этого показателя?**

5. **Сравните информацию из `sys.dm_exec_sessions` и `sys.dm_exec_requests`. Почему количество записей в этих представлениях может отличаться?**

6. **Какой из изученных диагностических запросов вы бы добавили в свой ежедневный мониторинг и почему?**

---

## **Требования к оформлению отчёта**

**Файл:** `Отчёт_Фамилия_Группа.docx` (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**
1. **Титульный лист** (ФИО, группа, дата, название работы)
2. **Цель работы**
3. **Ход выполнения:**
   - Полный текст стартового скрипта (с фамилией)
   - **Полностью заполненная таблица**
   - Скриншоты всех заданий 1-6 с подписями
4. **Ответы на вопросы** из задания 7
5. **Вывод:**
   - Что нового узнали о DMV?
   - Какие запросы наиболее полезны?
   - С какими трудностями столкнулись?
   - Как организовать ежедневный мониторинг?

---

## **Критерии оценивания**

| Критерий | Оценка «5» | Оценка «4» | Оценка «3» |
|----------|------------|------------|------------|
| **Заполнение таблицы** | Полностью заполнена | 1-2 метрики отсутствуют | Заполнена < 50% |
| **Выполнение заданий** | Все скриншоты с подписями | Подписи недостаточные | Отсутствуют 2+ заданий |
| **Анализ блокировок** | Блокировка создана и проанализирована | Анализ поверхностный | Не выполнено |
| **Ответы на вопросы** | Полные, с примерами | 1-2 ответа неполные | Поверхностные |
| **Оформление отчёта** | Структура соблюдена | Мелкие нарушения | Неструктурирован |

**💡 Максимальный балл: 5**  
**❗ Без таблицы — максимальная оценка «4»**

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **4 декабря 2025 г.**
---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON11.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №10](../LESSONS/LESSON11.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №11](../LESSONS/LESSON12.MD) |

---

