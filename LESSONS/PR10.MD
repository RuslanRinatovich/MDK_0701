| [Предыдущее занятие](../LESSONS/LESSON11.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №10](../LESSONS/LESSON11.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №11](../LESSONS/LESSON12.MD) |


---
# **Практическая работа №10. Мониторинг активных подключений и нагрузки**


**Цель:** Научиться использовать динамические административные представления (DMV) для мониторинга активности на SQL Server, идентификации проблемных сессий, анализа блокировок и определения ресурсоёмких запросов.

**Продолжительность:** 2 академических часа
**Оборудование:** SSMS, SQL Server Developer Edition

---

### **📊 Общий план работы**

**В ходе этой практической работы вам предстоит:**

1. **Создать тестовую базу данных** и наполнить её данными
2. **Выполнить 6 диагностических запросов** к различным DMV
3. **Заполнить сравнительную таблицу** по мере выполнения заданий
4. **Проанализировать результаты** и ответить на контрольные вопросы

**📋 Таблица для заполнения по ходу работы:**

| № задания | Диагностическая задача | Используемые DMV | Ключевые метрики | Время выполнения (мс) | Практическое применение |
|-----------|------------------------|------------------|------------------|---------------------|-------------------------|
| 1 | Мониторинг активных сессий | | | | |
| 2 | Анализ выполняемых запросов | | | | |
| 3 | Диагностика блокировок | | | | |
| 4 | Анализ статистики ожиданий | | | | |
| 5 | Поиск ресурсоёмких запросов | | | | |
| 6 | Комплексный мониторинг | | | | |

**📝 Справка по заполнению таблицы:**

**Столбец "Используемые DMV"**
- Укажите основные динамические представления из каждого задания
- Например: `sys.dm_exec_sessions`, `sys.dm_exec_requests`
- Можно указать вспомогательные функции: `sys.dm_exec_sql_text()`

**Столбец "Ключевые метрики"**
- Перечислите 3-5 самых важных показателей из запроса
- Например: `cpu_time`, `wait_type`, `blocking_session_id`
- Укажите метрики, которые наиболее информативны для диагностики

**Столбец "Время выполнения (мс)"**
- Запишите время выполнения запроса из вкладки "Сообщения"
- Используйте `SET STATISTICS TIME ON;` перед каждым запросом
- Пример: "15 мс" или "Время синтаксического анализа и компиляции SQL: 0 мс. Время выполнения SQL: 15 мс."

**Столбец "Практическое применение"**
- Опишите, в каких реальных ситуациях используется этот запрос
- Например: "Поиск зависших сессий", "Диагностика блокировок в реальном времени"
- Укажите, как часто администратор может выполнять такой мониторинг

**💡 Пример заполненной строки:**
| № задания | Диагностическая задача | Используемые DMV | Ключевые метрики | Время выполнения (мс) | Практическое применение |
|-----------|------------------------|------------------|------------------|---------------------|-------------------------|
| 1 | Мониторинг активных сессий | `sys.dm_exec_sessions` | `session_id`, `login_name`, `cpu_time`, `status` | 12 мс | Ежедневный мониторинг подключений, поиск аномальной активности |

**💡 Рекомендация:** Заполняйте таблицу по мере выполнения каждого задания. Это поможет вам в итоговом анализе.

---

### **Раздел 1. Подготовка учебной среды**

#### **📥 Задание 0. Стартовый скрипт (выполните целиком)**

```sql
-- Практическая работа №10: Мониторинг активных подключений и нагрузки
-- Скрипт подготовлен: [Ваша Фамилия]
DROP DATABASE IF EXISTS MonitorLab;
CREATE DATABASE MonitorLab;
GO
USE MonitorLab;
GO

-- Создаём тестовые таблицы для генерации нагрузки
CREATE TABLE sales.Orders (
    OrderID int IDENTITY(1,1) PRIMARY KEY,
    CustomerID int,
    OrderDate date,
    Amount decimal(10,2),
    Status varchar(20)
);

CREATE TABLE sales.Customers (
    CustomerID int IDENTITY(1,1) PRIMARY KEY,
    CustomerName nvarchar(100),
    Email nvarchar(100)
);

-- Наполняем таблицы тестовыми данными
INSERT INTO sales.Customers (CustomerName, Email)
SELECT 
    'Customer ' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS varchar(10)),
    'email' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS varchar(10)) + '@test.com'
FROM master.dbo.spt_values a, master.dbo.spt_values b
WHERE ROWNUM <= 1000;

INSERT INTO sales.Orders (CustomerID, OrderDate, Amount, Status)
SELECT 
    ABS(CHECKSUM(NEWID())) % 1000 + 1,
    DATEADD(day, -ABS(CHECKSUM(NEWID())) % 365, GETDATE()),
    CAST(RAND(CHECKSUM(NEWID())) * 1000 AS decimal(10,2)),
    CASE WHEN RAND() > 0.5 THEN 'active' ELSE 'completed' END
FROM master.dbo.spt_values a, master.dbo.spt_values b
WHERE ROWNUM <= 5000;

-- Создаём индексы для различных сценариев нагрузки
CREATE INDEX IX_Orders_Customer ON sales.Orders(CustomerID);
CREATE INDEX IX_Orders_Status ON sales.Orders(Status);
CREATE INDEX IX_Orders_Date ON sales.Orders(OrderDate);
GO

PRINT 'База данных MonitorLab успешно создана и наполнена тестовыми данными';
```

**В отчёт:** 
- Полный текст стартового скрипта с вашей фамилией в комментарии
- Скриншот с сообщением «Команды выполнены успешно»
- Подпись: *Рис. 1. Создание тестовой базы данных MonitorLab*

---

### **Раздел 2. Мониторинг активных сессий и запросов**

#### **📋 Задание 1. Обзор активных сессий**

**Цель:** Получить список всех активных пользовательских сессий и их характеристик.

```sql
-- Включим замер времени выполнения
SET STATISTICS TIME ON;

SELECT 
    session_id AS SPID,
    login_name AS Login,
    host_name AS Host,
    program_name AS Program,
    status AS Status,
    cpu_time AS CPUMs,
    memory_usage AS MemoryUsageKB,
    reads AS LogicalReads,
    writes AS Writes,
    logical_reads AS TotalReads
FROM sys.dm_exec_sessions
WHERE is_user_process = 1
ORDER BY cpu_time DESC;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот
2. В сообщениях найти время выполнения запроса
3. Заполнить строку таблицы для задания 1

**В отчёт:**
- Скриншот с результатом выполнения запроса
- Подпись: *Рис. 2. Мониторинг активных пользовательских сессий*
- Заполненная строка в таблице для задания 1

---

#### **📋 Задание 2. Анализ выполняющихся запросов**

**Цель:** Увидеть, какие запросы выполняются прямо сейчас, и получить их текст.

```sql
SET STATISTICS TIME ON;

SELECT 
    r.session_id AS SPID,
    r.status AS RequestStatus,
    r.command AS CommandType,
    r.wait_type AS WaitType,
    r.wait_time AS WaitTimeMs,
    r.cpu_time AS CPUMs,
    r.total_elapsed_time AS DurationMs,
    r.reads AS LogicalReads,
    r.writes AS Writes,
    t.text AS QueryText
FROM sys.dm_exec_requests r
CROSS APPLY sys.dm_exec_sql_text(r.sql_handle) t
WHERE r.session_id > 50;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот
2. Зафиксировать время выполнения
3. Заполнить строку таблицы для задания 2

**В отчёт:**
- Скриншот с результатом выполнения запроса
- Подпись: *Рис. 3. Анализ выполняющихся запросов с текстом SQL*
- Заполненная строка в таблице для задания 2

---

### **Раздел 3. Диагностика блокировок и ожиданий**

#### **📋 Задание 3. Создание и анализ блокировок**

**Цель:** Научиться идентифицировать и анализировать ситуации блокировок.

**Шаг 3.1.** Выполните в **первом окне** запроса (создание блокировки):
```sql
BEGIN TRANSACTION;
    UPDATE sales.Orders SET Amount = Amount * 1.1 WHERE Status = 'active';
    WAITFOR DELAY '00:02:00';
-- COMMIT; -- Не выполняем специально для создания блокировки
```

**Шаг 3.2.** Выполните во **втором окне** запрос жертвы блокировки:
```sql
SELECT * FROM sales.Orders WHERE Status = 'active';
```

**Шаг 3.3.** Выполните в **третьем окне** диагностический запрос блокировок:
```sql
SET STATISTICS TIME ON;

SELECT
    t1.resource_type AS ResourceType,
    DB_NAME(t1.resource_database_id) AS DatabaseName,
    CASE t1.resource_type
        WHEN 'OBJECT' THEN OBJECT_NAME(t1.resource_associated_entity_id)
        WHEN 'DATABASE' THEN 'Database'
        ELSE CONVERT(varchar, t1.resource_associated_entity_id)
    END AS ObjectName,
    t1.request_mode AS LockType,
    t1.request_status AS LockStatus,
    t1.request_session_id AS RequestSessionID,
    t2.blocking_session_id AS BlockingSessionID,
    (SELECT text FROM sys.dm_exec_sql_text(sql_handle)) AS BlockingQueryText
FROM sys.dm_tran_locks t1
INNER JOIN sys.dm_os_waiting_tasks t2 ON t1.lock_owner_address = t2.resource_address
WHERE t1.request_session_id > 50;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Создать блокировку по инструкции
2. Выполнить диагностический запрос
3. Зафиксировать время выполнения
4. Заполнить строку таблицы для задания 3

**В отчёт:**
- Скриншот с результатом диагностики блокировок
- Подпись: *Рис. 4. Анализ блокировок: идентификация блокирующих сессий*
- Заполненная строка в таблице для задания 3

---

#### **📋 Задание 4. Анализ статистики ожиданий**

**Цель:** Определить самые частые причины ожиданий на сервере.

```sql
SET STATISTICS TIME ON;

SELECT TOP 10
    wait_type AS WaitType,
    waiting_tasks_count AS WaitCount,
    wait_time_ms AS TotalWaitTimeMs,
    wait_time_ms / waiting_tasks_count AS AvgWaitTimeMs,
    signal_wait_time_ms AS SignalWaitTimeMs,
    (signal_wait_time_ms * 100.0 / wait_time_ms) AS SignalWaitPercent
FROM sys.dm_os_wait_stats
WHERE waiting_tasks_count > 0
AND wait_time_ms > 0
ORDER BY wait_time_ms DESC;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот
2. Зафиксировать время выполнения
3. Заполнить строку таблицы для задания 4

**В отчёт:**
- Скриншот с результатом выполнения запроса
- Подпись: *Рис. 5. Топ-10 типов ожиданий по общему времени*
- Заполненная строка в таблице для задания 4

---

### **Раздел 4. Анализ производительности и ресурсов**

#### **📋 Задание 5. Поиск ресурсоёмких запросов**

**Цель:** Найти запросы, которые потребляли больше всего ресурсов за время работы сервера.

```sql
SET STATISTICS TIME ON;

SELECT TOP 5
    qs.total_worker_time / 1000 AS TotalCPUMs,
    qs.execution_count,
    (qs.total_worker_time / 1000) / qs.execution_count AS AvgCPUMs,
    qs.total_logical_reads,
    qs.total_logical_reads / qs.execution_count AS AvgLogicalReads,
    qs.total_elapsed_time / 1000 AS TotalDurationMs,
    SUBSTRING(qt.text, qs.statement_start_offset/2 + 1,
        (CASE WHEN qs.statement_end_offset = -1
              THEN LEN(CONVERT(nvarchar(max), qt.text)) * 2
              ELSE qs.statement_end_offset
         END - qs.statement_start_offset)/2 + 1) AS QueryText
FROM sys.dm_exec_query_stats qs
CROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) qt
ORDER BY qs.total_worker_time DESC;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот
2. Зафиксировать время выполнения
3. Заполнить строку таблицы для задания 5

**В отчёт:**
- Скриншот с результатом выполнения запроса
- Подпись: *Рис. 6. Топ-5 самых ресурсоёмких запросов из кэша*
- Заполненная строка в таблице для задания 5

---

### **Раздел 5. Комплексный мониторинговый отчет**

#### **📋 Задание 6. Комплексный диагностический отчёт**

**Цель:** Создать единый запрос, который даёт максимально полную картину о текущей активности.

```sql
SET STATISTICS TIME ON;

SELECT
    s.session_id AS SPID,
    s.login_name AS Login,
    s.host_name AS Host,
    s.program_name AS Program,
    r.status AS RequestStatus,
    r.command AS CommandType,
    r.wait_type AS CurrentWaitType,
    r.wait_time AS WaitTimeMs,
    r.cpu_time AS CPUMs,
    r.total_elapsed_time AS DurationMs,
    r.reads AS LogicalReads,
    r.writes AS Writes,
    t.text AS QueryText,
    r.blocking_session_id AS BlockedBySPID,
    s.memory_usage AS MemoryKB,
    s.logical_reads AS SessionReads
FROM sys.dm_exec_sessions s
LEFT JOIN sys.dm_exec_requests r ON s.session_id = r.session_id
OUTER APPLY sys.dm_exec_sql_text(r.sql_handle) t
WHERE s.is_user_process = 1
AND (r.session_id IS NOT NULL OR s.status = 'sleeping')
ORDER BY r.cpu_time DESC;

SET STATISTICS TIME OFF;
```

**Что нужно сделать:**
1. Выполнить запрос и сделать скриншот
2. Зафиксировать время выполнения
3. Заполнить строку таблицы для задания 6
4. **Завершить заполнение всей таблицы**

**В отчёт:**
- Скриншот итогового результата
- Подпись: *Рис. 7. Комплексный отчет мониторинга активности сервера*
- **Полностью заполненная таблица**

---

### **Раздел 6. Анализ результатов и выводы**

#### **📋 Задание 7. Анализ эффективности диагностических запросов**

**Цель:** Проанализировать полученные результаты и сделать выводы о практическом применении DMV.

**На основе заполненной таблицы ответьте на вопросы:**

1. **Какой диагностический запрос оказался самым быстрым? Какой - самым медленным? С чем это может быть связано?**

2. **Какие DMV предоставляют информацию о текущем состоянии сервера, а какие - о накопленной статистике? Приведите примеры использования каждого типа.**

3. **В задании 3 вы создали и проанализировали блокировку. Опишите по шагам ваши действия для диагностики этой ситуации и какие DMV оказались наиболее полезными?**

4. **Что показывает столбец `SignalWaitPercent` в статистике ожиданий? Какие действия должен предпринять администратор при высоком значении этого показателя?**

5. **Сравните информацию из `sys.dm_exec_sessions` и `sys.dm_exec_requests`. Почему количество записей в этих представлениях может отличаться?**

6. **Какой из изученных диагностических запросов вы бы добавили в свой ежедневный мониторинг и почему?**

---

### **Требования к оформлению отчёта**

**Файл должен называться:** `Отчёт_Фамилия_Группа.docx` (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**
1. **Титульный лист** (ФИО, группа, дата, название работы)
2. **Цель работы**
3. **Ход выполнения:**
   - Полный текст стартового скрипта (с вашей фамилией)
   - **Полностью заполненная таблица** (из начала работы)
   - Скриншоты из SSMS со всеми выполненными SQL-командами из заданий 1-6
   - На скриншоте должны быть видны:
     - Текст команды(ы)
     - Сообщение об успешном выполнении
     - Имя базы данных в заголовке окна
   - Под каждым скриншотом — подпись с пояснением

4. **Ответы на вопросы** из задания 7

5. **Вывод:**
   - Что нового вы узнали о динамических административных представлениях?
   - Какие три диагностических запроса оказались наиболее полезными для вас и почему?
   - С какими практическими трудностями столкнулись при работе с DMV?
   - Как бы вы организовали ежедневный мониторинг сервера на основе изученных DMV?

---

### **Критерии оценивания**

| Критерий | Оценка «5» (отлично) | Оценка «4» (хорошо) | Оценка «3» (удовл.) |
|----------|---------------------|---------------------|---------------------|
| **Заполнение таблицы** | Таблица заполнена полностью, все метрики корректны | Таблица заполнена, но 1-2 метрики отсутствуют | Таблица заполнена менее чем на 50% |
| **Выполнение заданий 1-6** | Все скриншоты присутствуют, подписи информативные | Скриншоты есть, но подписи недостаточно подробные | Отсутствуют скриншоты 2+ заданий |
| **Анализ блокировок (задание 3)** | Блокировка создана и проанализирована, все участники идентифицированы | Блокировка создана, но анализ поверхностный | Задание не выполнено |
| **Ответы на вопросы** | Все ответы полные, с примерами из выполненной работы | Ответы даны, но 1-2 ответа неполные | Ответы поверхностные, без примеров |
| **Выводы и анализ** | Выводы глубокие, даны практические рекомендации | Выводы есть, но носят описательный характер | Выводы отсутствуют или не соответствуют работе |

**💡 Максимальный балл: 5**
**❗ Без заполненной таблицы — максимальная оценка не выше «4»**

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **4 декабря 2025 г.**
---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON11.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON12.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №10](../LESSONS/LESSON11.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №11](../LESSONS/LESSON12.MD) |

---

