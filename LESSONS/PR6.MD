### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON7.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON8.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №7](../LESSONS/LESSON7.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №8](../LESSONS/LESSON8.MD) |

---

# Практическая работа №6  
## Индексы в MS SQL Server: кластерные, некластерные, покрывающие индексы с INCLUDE

> **Цель**: Научиться создавать и анализировать **clustered**, **nonclustered** и **covering indexes**, выявлять проблему **Key Lookup**, устранять её с помощью **INCLUDE-столбцов**, и оценивать влияние индексов на производительность.  
> **Продолжительность**: 2 академических часа  
> **Оборудование**: Виртуальная машина с MS SQL Server Developer Edition и SSMS

---

### ⚙️ Подготовка: как читать результаты выполнения (русский SSMS)

Перед выполнением заданий настройте SSMS для анализа производительности.

#### 1. **Включите Действительный план выполнения**
- **Как**: `Запрос` → `Включить действительный план выполнения запроса`  
  **или** нажмите **`Ctrl + M`** (включить/выключить).  
- **Что даёт**: после выполнения запроса появится вкладка **«План выполнения»** — графическая схема того, *как SQL Server выполнил запрос*: какие операторы использовал, сколько они «стоят».

#### 2. **Включите статистику выполнения**
Выполните **перед каждым запросом**:
```sql
SET STATISTICS IO ON;   -- собирает информацию о чтении данных
SET STATISTICS TIME ON; -- собирает информацию о времени
```

➡️ После выполнения запроса во вкладке **«Сообщения»** появится **дополнительная служебная информация**, например:
```text
Таблица "Students". Сканирований 0, логических операций чтения 2, физических операций чтения 0, ...

Время работы SQL Server:
   Время ЦП = 0 мс, затраченное время = 0 мс.
```

#### 🔍 Краткий разбор ключевых параметров:

| Параметр | Где искать | Что означает | Почему важно |
|---------|------------|--------------|--------------|
| **`логических операций чтения`** | Вкладка **«Сообщения»** | Сколько **страниц данных (по 8 КБ)** SQL Server прочитал из **буфера памяти**. | Чем меньше — тем быстрее запрос. 132 = 132 × 8 КБ = ~1 МБ данных. |
| **`затраченное время`** | Вкладка **«Сообщения»** | **Реальное время выполнения** (в миллисекундах), «по часам». Включает ожидание диска, сети, CPU. | Главный показатель для пользователя: «запрос стал быстрее/медленнее». |
| **`Стоимость: XX%`** | Вкладка **«План выполнения»**, под оператором | Оценка **относительной «дороговизны»** оператора в рамках запроса. | Помогает найти «узкое место» (например, «Вложенный цикл» на 80%). |
| **`3 из 5`** | В правом нижнем углу блока оператора | **3 строки** передано дальше, из **5 прочитанных**. | Показывает эффективность фильтрации. |

> 💡 **Важно**:  
> - **`логических операций чтения` и `затраченное время` — в «Сообщениях»**,  
> - **Операторы (`Просмотр...`, `Вложенный цикл`, `Сортировка`) — в «Плане выполнения»**,  
> - Если чего-то нет — значит, вы **забыли включить** `Ctrl+M` или `SET STATISTICS IO ON` или `SET STATISTICS TIME ON`.

#### 3. **Названия операторов в русском SSMS**
| Английский | Русский | Что это |
|-----------|---------|---------|
| `Table Scan` | **Просмотр строк таблицы** | Чтение HEAP (без индексов) |
| `Clustered Index Scan` | **Поиск кластеризованного индекса** | Чтение упорядоченной таблицы |
| `Index Seek` | **Просмотр по индексу** | Точечный поиск по некластерному индексу |
| `Key Lookup` | **Вложенный цикл** | Доп. обращение к данным для «добора» столбцов |
| `Sort` | **Сортировка** | Принудительная сортировка строк (дорого!) |

#### 4. 🔍 **«Отсортировано: Да» — это НЕ оператор `Sort`!**
Когда вы в результатах нажимаете **ПКМ → «Свойства столбца»**, вы видите:
- **«Отсортировано: Да»** — данные **уже пришли упорядоченными** (например, из индекса),  
- **«Отсортировано: Нет»** — порядок не гарантирован (или был оператор `Sort`).

> 💡 **Флаг *«Отсортировано: Да»* = признак того, что SQL Server *не делал* `Sort`**,  
> а данные **уже лежали в нужном порядке в индексе**.

---

## 📥 Шаг 0. Стартовый скрипт (выполните целиком)

> 💡 **Замените `Ivanov` на свою фамилию!**

```sql
CREATE DATABASE IndexLab_Ivanov;
GO
USE IndexLab_Ivanov;
GO

CREATE TABLE Students (
    StudentID int IDENTITY(1,1) NOT NULL,
    FullName nvarchar(100),
    GroupCode char(10),
    EnrollmentYear smallint,
    Email nvarchar(100),
    AvgScore decimal(3,2)
);
GO

INSERT INTO Students (FullName, GroupCode, EnrollmentYear, Email, AvgScore)
SELECT 
    N'Студент ' + CAST(n AS nvarchar(10)),
    CASE n % 3 WHEN 0 THEN 'ИС-21' WHEN 1 THEN 'ИС-22' ELSE 'ИС-23' END,
    2022 + (n % 3),
    N'student' + CAST(n AS nvarchar(10)) + N'@college.edu',
    CAST(3.0 + (RAND(CHECKSUM(NEWID())) * 2.0) AS decimal(3,2))
FROM (SELECT TOP 10000 ROW_NUMBER() OVER (ORDER BY (SELECT NULL)) AS n FROM sys.columns c1, sys.columns c2) nums;
GO

-- Проверка: таблица — HEAP
SELECT t.name, i.type_desc FROM sys.tables t LEFT JOIN sys.indexes i ON t.object_id = i.object_id WHERE t.name = 'Students';
GO
```

---

## 📋 Задание 0.5. Кластерный индекс — сравнение базового поведения

> 🎯 **Цель**: увидеть, как меняется план выполнения при добавлении `PRIMARY KEY`.

1. Создайте **кластерный индекс** по `StudentID`:
   ```sql
   ALTER TABLE Students 
   ADD CONSTRAINT PK_Students PRIMARY KEY CLUSTERED (StudentID);
   ```

2. Выполните запрос:
   ```sql
   SET STATISTICS IO ON;
   SELECT FullName, Email FROM Students WHERE StudentID = 5;
   ```

3. В отчёте укажите:  
   - Тип оператора: **«Поиск кластеризованного индекса»**,  
   - `логических операций чтения` (ожидаемо: **2–3**),  
   - Почему запрос **быстрый**? (данные физически упорядочены по `StudentID`).

4. Удалите PK (чтобы вернуться к HEAP для следующих заданий):
   ```sql
   ALTER TABLE Students DROP CONSTRAINT PK_Students;
   ```

> 💡 Это **базовый сценарий**: кластерный индекс ускоряет поиск по ключу, но не помогает при фильтрации по другим столбцам.

---

## 📋 Задание 1. Анализ без индексов (HEAP)

1. Выполните:
   ```sql
   SET STATISTICS IO ON;
   SET STATISTICS TIME ON;

   SELECT FullName, Email, AvgScore
   FROM Students
   WHERE GroupCode = 'ИС-22' AND StudentID < 10;
   ```

2. В отчёте укажите:  
   - Тип оператора: **«Просмотр строк таблицы»**,  
   - `логических операций чтения` (ожидаемо: **~150–200**),  
   - `затраченное время`.

---

## 📋 Задание 2. Nonclustered Index → «Вложенные циклы»

1. Создайте индекс:
   ```sql
   CREATE NONCLUSTERED INDEX IX_Students_Group_ID 
   ON Students(GroupCode, StudentID);
   ```

2. Выполните тот же запрос.  
3. В **«Плане выполнения»** найдите:  
   - **«Вложенные циклы»**. 
   - **«Поиск в индексе(NonClustered)»**,  
   - **«Уточняющий запрос RID (Heap)»**.  
4. ✅ **Подтвердите использование индекса**:  
   - Кликните по **Поиск по индексу»**,  
   - В подсказке: `Object: [Students].[IX_Students_Group_ID]`.  
5. В отчёте:  
   - Стоимость «Вложенного цикла»,  
   - `логических операций чтения` (ожидаемо: **~5–10**),  
   - Почему уменьшилось? (индекс сузил выборку, но нужны доп. данные → «Вложенный цикл»).

### 🔍 Справка: Как читать Вложенные циклы(Key Lookup) в Плане выполнения

Когда в плане выполнения появляется **«Вложенные циклы»** с двумя входящими ветками — это **Key Lookup** (дополнительное обращение к данным). Вот как его расшифровать:

```
SELECT  
↑  
Вложенные циклы (Стоимость: 0%)  
├─ Поиск в индексе [IX_...] (Стоимость: X%)  
└─ Уточняющий запрос RID (Heap) или Поиск кластерного индекса (Стоимость: Y%)
```

| Элемент | Что означает | Почему важно |
|--------|--------------|--------------|
| **«Вложенные циклы»** | Оператор-соединитель — сам по себе почти не стоит (0%), но **сигнализирует о Key Lookup**. | → Первый признак проблемы. |
| **«Поиск в индексе»** (X%) | Некластерный индекс быстро нашёл строки по условию (например, по `GroupCode`). | Чем меньше X% — тем эффективнее индекс. |
| **«Уточняющий запрос RID (Heap)»** (Y%) | **Дополнительное обращение в таблицу**, чтобы «добить» столбцы, отсутствующие в индексе. | **Y% > 50% — тревожный сигнал!** Это «дорогая» часть запроса. |
| **Y% >> X%** (например, 81% vs 19%) | Индекс сузил выборку, но «добор» данных — дороже поиска. | → Значит, индекс **не покрывает** запрос. |

#### 💡 Что делать?
1. Посмотрите, какие столбцы запрашиваются в `SELECT`, но отсутствуют в индексе.  
2. Добавьте их через `INCLUDE`:  
   ```sql
   CREATE NONCLUSTERED INDEX IX_Name 
   ON Table(col_where) 
   INCLUDE (col_select1, col_select2);
   ```
3. После этого:  
   - **«Вложенные циклы» и «Уточняющий запрос» исчезнут**,  
   - Останется только **«Поиск в индексе»** → 100% стоимости, но *абсолютно дешёвый*.

> ✅ **Проверка успеха**:  
> - В «Сообщениях»: `logical reads` ↓ (например, с 8 до 2),  
> - В плане: нет «Вложенных циклов»,  
> - В результатах: `elapsed time` ↓.

---

---

## 📋 Задание 3. Покрывающий индекс

1. Удалите индекс:
   ```sql
   DROP INDEX IX_Students_Group_ID ON Students;
   ```

2. Создайте:
   ```sql
   CREATE NONCLUSTERED INDEX IX_Students_Group_Cover 
   ON Students(GroupCode) 
   INCLUDE (FullName, Email, AvgScore);
   ```

3. Выполните запрос.  
4. В отчёте:  
   - Исчез ли **«Вложенный цикл»**?  
   - `логических операций чтения` (ожидаемо: **2–3** — только листья индекса).

---

## 📋 Задание 4. Исследовательская задача + Анализ эффективности

> 🎯 «Топ-5 студентов по баллу в группе ИС-23».

```sql
SELECT TOP 5 FullName, AvgScore, Email
FROM Students
WHERE GroupCode = 'ИС-23'
ORDER BY AvgScore DESC;
```

### ➤ Шаг 1. Выполните без индексов
- В «Плане выполнения» вы увидите:  
  ```
  Top (5)  
  ↑  
  Sort (ORDER BY: [AvgScore] DESC) ← cost ~30–40%  
  ↑  
  Просмотр строк таблицы ← cost ~60–70%
  ```
- В «Сообщениях» запишите:  
  - `логических операций чтения = 132`,  
  - `затраченное время = ...`.

> 🔍 **Откуда берётся 132?**  
> - Таблица содержит ~3300 студентов в «ИС-23»,  
> - `Просмотр строк таблицы` читает все строки группы (~3300),  
> - Каждая страница = 8 КБ → ~132 страниц нужно прочитать, чтобы получить 3300 строк,  
> - Затем `Sort` сортирует их в памяти → дорого!

### ➤ Шаг 2. Создайте оптимальный индекс
```sql
CREATE NONCLUSTERED INDEX IX_Top_IS23 
ON Students(GroupCode, AvgScore DESC) 
INCLUDE (FullName, Email);
```

### ➤ Шаг 3. Выполните снова
- В «Плане» должно быть:  
  ```
  Top (5)  
  ↑  
  Просмотр по индексу ← cost 100%, без Sort и Вложенного цикла!
  ```
- В результатах: кликните ПКМ по `AvgScore` → **«Отсортировано: Да»**,  
- В «Сообщениях» запишите:  
  - `число логических чтений = 3`,  
  - `затраченное время = ...` (ожидаемо: в 10–20 раз меньше).

### ✅ Вывод для отчёта:
> *До оптимизации: `число логических чтений = 132`, `Sort` (cost 38%).  
> После: `число логических чтений = 3`, `Sort` исчез, `AvgScore` → «Отсортировано: Да».  
> Ускорение: в 15 раз по времени, в 44 раза по чтениям.*

---

## 📋 Задание 5. Ответы на вопросы

1. Почему `Key Lookup` в русском SSMS называется **«Вложенный цикл»**?  
2. Как по `Object: [...]` убедиться, что используется именно ваш индекс?  
3. В чём разница между оператором **`Sort`** и флагом **«Отсортировано: Да»**?  
4. Откуда берётся число **132** в `число логических чтений`?  
5. Почему кластерный индекс не помогает в `WHERE GroupCode = 'ИС-22'`?

---

## 📄 **Требования к отчёту (Word-документ)**

# Файл должен называться: **`Отчёт_Фамилия_Группа.docx`**  (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**

1. **Титульный лист** (ФИО, группа, дата, название работы).  
2. **Цель работы**.  
3. **Ход выполнения**:  
   - Полный текст **стартового скрипта** (с вашей фамилией).  
   - **Скриншоты** из SSMS со **всеми выполненными SQL-командами** из заданий 0.5–5.  
     - На скриншоте должны быть видны:  
       - Текст команды(ы),  
       - Сообщение об успешном выполнении («Команды выполнены успешно»),  
       - Имя базы данных в заголовке окна или в строке состояния.  
   - Под каждым скриншотом — **подпись с пояснением**, например:  
     > *Рис. 1. Создание некластерного индекса IX_Students_Group_ID.*  
     > *Рис. 2. Действительный план выполнения с оператором «Вложенный цикл» (cost 78%).*  
     > *Рис. 3. Окно «Сообщения» с числом логических чтений = 132.*  
     > *Рис. 4. Свойства столбца AvgScore: «Отсортировано: Да».*  
   - **Обязательно включите таблицу сравнения эффективности** (в тексте отчёта, после скриншотов):
     | Сценарий | `число логических чтений` | `затраченное время` | `Sort`? | «Отсортировано»? |
     |----------|-----------------|----------------|---------|------------------|
     | Без индексов | 132 | 85 мс | Да | Нет |
     | С кластерным индексом | 120 | 80 мс | Да | Нет |
     | С некластерным индексом | 8 | 22 мс | Да | Нет |
     | С покрывающим индексом | 3 | 6 мс | **Нет** | **Да** |
     > *Примечание: ваши значения могут немного отличаться — укажите реальные.*  
   - Альтернатива (если скриншоты сделать сложно):  
     - Текст всех выполненных команд **с комментариями**, например:  
       ```sql
       -- Создаём кластерный индекс для сравнения
       ALTER TABLE Students ADD CONSTRAINT PK_Students PRIMARY KEY CLUSTERED (StudentID);
       ```  
     - Но **предпочтительны скриншоты**, так как они подтверждают фактическое выполнение.  
   - Ответы на вопросы из задания 5.  
4. **Вывод**:  
   - Что нового вы узнали о кластерных и некластерных индексах?  
   - С какими трудностями столкнулись при поиске «Вложенного цикла»?  
   - Почему важно устранять оператор `Sort` и Key Lookup в производственных системах?

> ❗ **Важно**:  
> - Отчёт **без скриншотов (или без альтернативного кода с комментариями)** не принимается.  
> - **Без таблицы сравнения и пояснения, откуда берётся 132** — максимальная оценка **не выше «4»**.  
> - Скриншоты должны быть **чёткими и читаемыми** (можно использовать инструмент «Ножницы» или `Win + Shift + S`).  
> - Не вставляйте один общий скриншот всего окна SSMS — делайте **отдельные скриншоты по логическим блокам** (индексы, план выполнения, сообщения, результаты).

---

## 📤 Сдача работы

1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/ZyRB7xgMEPcXqGim7).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **27 ноября 2025 г.**

---

## 📊 Критерии оценивания

| Критерий | Оценка «5» (отлично) | Оценка «4» (хорошо) | Оценка «3» (удовл.) |
|--------|----------------------|---------------------|---------------------|
| **Выполнение стартового скрипта** | Скрипт выполнен, фамилия заменена, 10 000 строк созданы | Скрипт выполнен, но объём данных < 5000 строк | Скрипт не выполнен или содержит критические ошибки |
| **Задание 0.5: кластерный индекс** | PK создан и удалён, `число логических чтений` зафиксированы, вывод корректен | PK создан, но анализ неполный | PK не создан или данные не сняты |
| **Задание 2: «Вложенный цикл»** | Индекс создан, «Вложенный цикл» найден и подтверждён через `Object: [IX_...]` | Индекс создан, но подтверждение отсутствует | «Вложенный цикл» не обнаружен |
| **Задание 3: покрывающий индекс** | Покрывающий индекс создан, «Вложенный цикл» устранён, `число логических чтений` ↓ | Индекс создан, но `INCLUDE` не использован | Покрытие не достигнуто |
| **Задание 4: исследовательская задача** | `Sort` и «Вложенный цикл» исчезли, «Отсортировано: Да» подтверждено, дан анализ «132» | Ускорение есть, но анализ «132» или «Отсортировано» неполный | Задача не решена |
| **Ответы на вопросы** | Все ответы полные, с использованием терминов (число логических чтений, затраченное время, INCLUDE) | Ответы даны, но 1–2 — неполные | Ответы поверхностные или отсутствуют |
| **Оформление отчёта** | Соблюдена структура, все скриншоты чёткие, подписаны, есть вывод | Есть мелкие нарушения (например, нет подписей к скриншотам) | Отчёт неструктурирован, скриншоты отсутствуют или нечитаемы |

> 💡 **Максимальный балл**: 5  
> ❗ **Без скриншотов окон «Сообщения» и «План выполнения»** — максимальная оценка не выше «3».


---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON7.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON8.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №7](../LESSONS/LESSON7.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №7](../LESSONS/LESSON8.MD) |

---
