### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON7.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON8.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №7](../LESSONS/LESSON7.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №8](../LESSONS/LESSON8.MD) |

---
# Практическая работа №6  
## Индексы в MS SQL Server: кластерные, некластерные, покрывающие индексы с INCLUDE

> **Цель**: Научиться создавать и анализировать **кластеризованные**, **некластеризованные** и **покрывающие индексы**, выявлять проблему **Key Lookup**, устранять её с помощью **INCLUDE-столбцов**, и оценивать влияние индексов на производительность.  
> **Продолжительность**: 2 академических часа  
> **Оборудование**: Виртуальная машина с MS SQL Server Developer Edition и SSMS

---

### ⚙️ Подготовка: как читать результаты выполнения (русский SSMS)

#### 1. **Включите Действительный план выполнения**
- **Как**: `Запрос` → `Включить действительный план выполнения запроса`  
  **или** нажмите **`Ctrl + M`** (включить/выключить).  
- **Что даёт**: после выполнения запроса появится вкладка **«План выполнения»** — графическая схема того, *как SQL Server выполнил запрос*.

#### 2. **Включите статистику выполнения**
Выполните **перед каждым запросом**:
```sql
SET STATISTICS IO ON;   -- собирает информацию о чтении данных
SET STATISTICS TIME ON; -- собирает информацию о времени
```

➡️ После выполнения запроса во вкладке **«Сообщения»** появится:
```text
Таблица "Students". Число логических операций чтения = 132, ...
Время работы SQL Server: 
   Время ЦП = 16 мс,  затраченное время = 48 мс.
```

#### 🔍 Краткий разбор ключевых параметров:

| Параметр | Где искать | Что означает | Почему важно |
|---------|------------|--------------|--------------|
| **`логические операции чтения`** | Вкладка **«Сообщения»** | Сколько **страниц данных (по 8 КБ)** прочитано из памяти. | Чем меньше — тем быстрее. 132 = ~1 МБ. |
| **`затраченное время`** | Вкладка **«Сообщения»** | **Реальное время выполнения** («по часам»). | Главный показатель для пользователя. |
| **`Стоимость: XX%`** | Вкладка **«План выполнения»**, под оператором | Оценка **относительной «дороговизны»** оператора в рамках запроса. | Помогает найти «узкое место» в сложных планах. |
| **`3 из 5`** | В правом нижнем углу блока оператора | **3 строки** передано дальше, из **5 прочитанных**. | Показывает эффективность фильтрации. |

> 💡 **Важно**:  
> - **`логические операции чтения` и `затраченное время` — в «Сообщениях»**,  
> - **Операторы — в «Плане выполнения»**,  
> - Если чего-то нет — значит, вы **забыли включить** `Ctrl+M` или `SET STATISTICS IO ON` или `SET STATISTICS TIME ON`.

---

#### 🔎 **Зачем нужна «Стоимость: XX%» — честный ответ**

> 💡 **Стоимость в % — это *внутренняя оценка SQL Server*, помогающая *ему самому* выбрать план выполнения.**  
> Она **не измеряет время и не сравнивает разные запросы** — это **относительная доля** оператора *внутри одного плана*.

##### ✅ Когда **полезна**:
- В **сложных запросах** с 3+ операторами:  
  ```
  Сортировка (35%)  
  ↑  
  Соединение хэшем (45%) ← сразу видно: начинать оптимизацию отсюда!
  ```  
  → Помогает быстро найти «самый тяжёлый» шаг.

##### ❌ Когда **вводит в заблуждение** (чаще всего у студентов!):
- **Один оператор** → всегда **100%**, даже если запрос мгновенный.  
- **Сравнение «до/после»**:  
  - Было: `Просмотр строк таблицы — 100%`,  
  - Стало: `Поиск кластеризованного индекса — 100%`,  
  → Кажется, «ничего не изменилось», хотя `логические операции чтения` упали в 50 раз!

> 🚨 **Главная ловушка**: студенты смотрят на 100% и думают: *«индекс не помог»*, хотя `логические операции чтения` упали с 152 до 2.

##### 💡 **Правило для практики**:
1. **Всегда первым делом смотрите**:  
   - `логические операции чтения`,  
   - `затраченное время`.  
2. **% используйте ТОЛЬКО**, если в плане ≥3 операторов.  
3. **Никогда не сравнивайте % между разными запросами!**

> ✅ **Итог**:  
> — `логические операции чтения` и `затраченное время` — **ваши главные метрики**,  
> — `Стоимость: XX%` — вспомогательный инструмент для анализа сложных планов.

---

#### 3. **Названия операторов в русском SSMS**
| Английский | Русский |
|-----------|---------|
| `Table Scan` | **Просмотр строк таблицы** |
| `Clustered Index Seek` | **Поиск кластеризованного индекса** |
| `Nonclustered Index Seek` | **Поиск в некластеризованном индексе** |
| `Key Lookup` | **Вложенные циклы** |
| `RID Lookup` | **Уточняющий запрос RID (куча)** |

#### 4. 🔍 **Справка: Как читать Key Lookup в плане выполнения**

Когда в плане появляется **«Вложенные циклы»** с двумя входящими ветками — это **Key Lookup**:

```
SELECT  
↑  
Вложенные циклы (0%)  
├─ Поиск в некластеризованном индексе [IX_...] (19%)  
└─ Уточняющий запрос RID (куча) [Students] (81%)
```

| Элемент | Что означает | Почему важно |
|--------|--------------|--------------|
| **«Вложенные циклы»** | Сигнал о Key Lookup | Первый признак проблемы |
| **«Поиск в некластеризованном индексе»** (19%) | Индекс быстро нашёл строки | Чем меньше % — тем лучше индекс |
| **«Уточняющий запрос RID (куча)»** (81%) | Доп. обращение в таблицу за недостающими столбцами | **81% > 50% — тревога!** |
| **81% >> 19%** | «Добор» данных дороже поиска | Индекс не покрывает запрос |

#### 💡 Что делать?
1. Добавьте недостающие столбцы через `INCLUDE`:  
   ```sql
   CREATE NONCLUSTERED INDEX IX_Name 
   ON Table(col_where) 
   INCLUDE (col_select1, col_select2);
   ```
2. После этого:  
   - **«Вложенные циклы» и «Уточняющий запрос» исчезнут**,  
   - Останется только **«Поиск в некластеризованном индексе»**,  
   - `логические операции чтения` и `затраченное время` ↓.

---

#### 5. 🔍 **«Отсортировано: Да» — это НЕ оператор `Сортировка`!**
- **«Отсортировано: Да»** — данные **уже пришли упорядоченными** (из индекса),  
- **«Отсортировано: Нет»** — порядок не гарантирован (или был `Сортировка`).  
> 💡 Флаг *«Отсортировано: Да»* = признак того, что SQL Server **не делал** `Сортировку`.

---

## 📥 Шаг 0. Стартовый скрипт (выполните целиком)

> 💡 **Замените `Ivanov` на свою фамилию!**

```sql
CREATE DATABASE IndexLab_Ivanov;
GO
USE IndexLab_Ivanov;
GO

CREATE TABLE Students (
    StudentID int IDENTITY(1,1) NOT NULL,
    FullName nvarchar(100),
    GroupCode char(10),
    EnrollmentYear smallint,
    Email nvarchar(100),
    AvgScore decimal(3,2)
);
GO

INSERT INTO Students (FullName, GroupCode, EnrollmentYear, Email, AvgScore)
SELECT 
    N'Студент ' + CAST(n AS nvarchar(10)),
    CASE n % 3 WHEN 0 THEN 'ИС-21' WHEN 1 THEN 'ИС-22' ELSE 'ИС-23' END,
    2022 + (n % 3),
    N'student' + CAST(n AS nvarchar(10)) + N'@college.edu',
    CAST(3.0 + (RAND(CHECKSUM(NEWID())) * 2.0) AS decimal(3,2))
FROM (SELECT TOP 10000 ROW_NUMBER() OVER (ORDER BY (SELECT NULL)) AS n FROM sys.columns c1, sys.columns c2) nums;
GO

-- Проверка: таблица — куча
SELECT t.name, i.type_desc FROM sys.tables t LEFT JOIN sys.indexes i ON t.object_id = i.object_id WHERE t.name = 'Students';
GO
```

---

## 📋 Задание 0.5. Кластеризованный индекс — сравнение базового поведения

> 🎯 **Цель**: увидеть, как меняется план при добавлении `PRIMARY KEY`.

1. Создайте кластеризованный индекс:
   ```sql
   ALTER TABLE Students 
   ADD CONSTRAINT PK_Students PRIMARY KEY CLUSTERED (StudentID);
   ```

2. Выполните:
   ```sql
   SET STATISTICS IO ON;
   SELECT FullName, Email FROM Students WHERE StudentID = 5;
   ```

3. В отчёте укажите:  
   - Тип оператора: **«Поиск кластеризованного индекса»**,  
   - `логические операции чтения` (ожидаемо: **2–3**).  
4. Удалите PK:
   ```sql
   ALTER TABLE Students DROP CONSTRAINT PK_Students;
   ```

---

## 📋 Задание 1. Анализ без индексов (куча)

1. Выполните:
   ```sql
   SET STATISTICS IO ON;
   SET STATISTICS TIME ON;

   SELECT FullName, Email, AvgScore
   FROM Students
   WHERE GroupCode = 'ИС-22' AND StudentID < 10;
   ```

2. В отчёте укажите:  
   - Тип оператора: **«Просмотр строк таблицы»**,  
   - `логические операции чтения` (ожидаемо: **~150–200**),  
   - `затраченное время`.

---

## 📋 Задание 2. Некластеризованный индекс → «Вложенные циклы» (Key Lookup)

1. Создайте индекс:
   ```sql
   CREATE NONCLUSTERED INDEX IX_Students_Group_ID 
   ON Students(GroupCode, StudentID);
   ```

2. Выполните тот же запрос.  
3. В **«Плане выполнения»** вы увидите:
   ```
   Вложенные циклы (0%)  
   ├─ Поиск в некластеризованном индексе [IX_Students_Group_ID] (19%)  
   └─ Уточняющий запрос RID (куча) [Students] (81%)
   ```
4. ✅ **Подтвердите использование индекса**:  
   - Кликните по **«Поиску в некластеризованном индексе»**,  
   - В подсказке: `Объект: [Students].[IX_Students_Group_ID]`.  
5. В отчёте укажите:  
   - Почему **81%** приходится на «Уточняющий запрос»?  
   - `логические операции чтения` (ожидаемо: **~5–10**).

---

## 📋 Задание 3. Покрывающий индекс

1. Удалите индекс:
   ```sql
   DROP INDEX IX_Students_Group_ID ON Students;
   ```

2. Создайте:
   ```sql
   CREATE NONCLUSTERED INDEX IX_Students_Group_Cover 
   ON Students(GroupCode) 
   INCLUDE (FullName, Email, AvgScore);
   ```

3. Выполните запрос.  
4. В отчёте:  
   - Исчезли ли **«Вложенные циклы»** и **«Уточняющий запрос»**?  
   - `логические операции чтения` (ожидаемо: **2–3**).

---

## 📋 Задание 4. Исследовательская задача + Анализ эффективности

> 🎯 «Топ-5 студентов по баллу в группе ИС-23».

```sql
SELECT TOP 5 FullName, AvgScore, Email
FROM Students
WHERE GroupCode = 'ИС-23'
ORDER BY AvgScore DESC;
```

### ➤ Шаг 1. Без индексов
- В «Плане»:  
  ```
  Сортировка (30–40%)  
  ↑  
  Просмотр строк таблицы (60–70%)
  ```
- В «Сообщениях»:  
  - `логические операции чтения = 132`,  
  - `затраченное время = ...`.

> 🔍 **Откуда 132? Подробный расчёт**  
> У нас **10 000 строк**, структура:  
> - `StudentID` (4 байта) + `FullName` (~60) + `GroupCode` (10) + `EnrollmentYear` (2) + `Email` (~50) + `AvgScore` (5) = **~131 байт/строка**.  
> - 1 страница SQL Server = **8192 байта**, из них полезно ~**8096 байт**.  
> - Строк на страницу: 8096 / 131 ≈ **61**.  
> - Всего страниц: 10 000 / 61 ≈ **164**.  
>   
> Но запрос ищет студентов **только в «ИС-23»** (~3333 строки).  
> SQL Server **не знает**, где они лежат (нет индекса по `GroupCode`), поэтому **читает всю таблицу** → **132 страницы** (реальная плотность выше из-за коротких строк).  
>   
> → **132 логические операции чтения = 132 × 8 КБ = 1.03 МБ данных**, прочитанных из памяти.

### ➤ Шаг 2. Создайте индекс:
```sql
CREATE NONCLUSTERED INDEX IX_Top_IS23 
ON Students(GroupCode, AvgScore DESC) 
INCLUDE (FullName, Email);
```

### ➤ Шаг 3. После оптимизации
- В «Плане»: только **«Поиск в некластеризованном индексе»**,  
- В результатах: `AvgScore` → **«Отсортировано: Да»**,  
- В «Сообщениях»:  
  - `логические операции чтения = 3`,  
  - `затраченное время = ...`.

### ✅ Вывод для отчёта:
> *До: `логические операции чтения = 132`, `Сортировка` (cost 38%).  
> После: `логические операции чтения = 3`, `Сортировка` исчезла, `Отсортировано: Да`.  
> Ускорение: в 15 раз по времени.*

---

## 📋 Задание 5. Ответы на вопросы

1. Почему `Key Lookup` в русском SSMS называется **«Вложенные циклы»**?  
2. Как по `Объект: [...]` убедиться, что используется именно ваш индекс?  
3. В чём разница между **«Уточняющим запросом RID (куча)» (81%)** и **«Поиском в некластеризованном индексе» (19%)**?  
4. Откуда берётся число **132** в `логических операциях чтения`?  
5. Почему кластеризованный индекс не помогает в `WHERE GroupCode = 'ИС-22'`?

---

## 📄 **Требования к отчёту (Word-документ)**

# Файл должен называться: **`Отчёт_Фамилия_Группа.docx`**  (например, `Отчёт_Petrov_IS-23.docx`)

**Структура отчёта:**

1. **Титульный лист** (ФИО, группа, дата, название работы).  
2. **Цель работы**.  
3. **Ход выполнения**:  
   - Полный текст **стартового скрипта** (с вашей фамилией).  
   - **Скриншоты** из SSMS со **всеми выполненными SQL-командами** из заданий 0.5–5.  
     - На скриншоте должны быть видны:  
       - Текст команды(ы),  
       - Сообщение об успешном выполнении («Команды выполнены успешно»),  
       - Имя базы данных в заголовке окна или в строке состояния.  
   - Под каждым скриншотом — **подпись с пояснением**, например:  
     > *Рис. 1. Создание некластеризованного индекса IX_Students_Group_ID.*  
     > *Рис. 2. План выполнения с «Уточняющим запросом RID (куча)» (81%).*  
     > *Рис. 3. Окно «Сообщения» с числом логических операций чтения = 132.*  
     > *Рис. 4. Свойства столбца AvgScore: «Отсортировано: Да».*  
   - **Обязательно включите таблицу сравнения эффективности**:
     | Сценарий | `логические операции чтения` | `затраченное время` | `Сортировка`? | «Отсортировано»? |
     |----------|-----------------------------|---------------------|----------------|------------------|
     | Без индексов | 132 | 85 мс | Да | Нет |
     | С кластеризованным | 120 | 80 мс | Да | Нет |
     | С некластеризованным | 8 | 22 мс | Да | Нет |
     | С покрывающим | 3 | 6 мс | **Нет** | **Да** |
   - Ответы на вопросы из задания 5.  
4. **Вывод**:  
   - Что нового вы узнали о кластеризованных и некластеризованных индексах?  
   - С какими трудностями столкнулись при поиске «Вложенных циклов»?  
   - Почему важно устранять Key Lookup и `Сортировку` в производственных системах?

> ❗ **Важно**:  
> - Отчёт **без скриншотов (или без альтернативного кода с комментариями)** не принимается.  
> - **Без таблицы сравнения и пояснения «132»** — максимальная оценка **не выше «4»**.  
> - Скриншоты должны быть **чёткими и читаемыми**.  
> - Не вставляйте один общий скриншот всего окна SSMS.

---

## 📤 Сдача работы

1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/ZyRB7xgMEPcXqGim7).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **27 ноября 2025 г.**

---

## 📊 Критерии оценивания

| Критерий | Оценка «5» (отлично) | Оценка «4» (хорошо) | Оценка «3» (удовл.) |
|--------|----------------------|---------------------|---------------------|
| **Выполнение стартового скрипта** | Скрипт выполнен, фамилия заменена, 10 000 строк созданы | Скрипт выполнен, но объём данных < 5000 строк | Скрипт не выполнен или содержит критические ошибки |
| **Задание 0.5: кластеризованный индекс** | PK создан и удалён, `логические операции чтения` зафиксированы | PK создан, но анализ неполный | PK не создан |
| **Задание 2: «Вложенные циклы»** | Key Lookup найден, подтверждён через `Объект`, объяснено 81% | Индекс создан, но объяснение 81% отсутствует | Key Lookup не обнаружен |
| **Задание 3: покрывающий индекс** | Покрывающий индекс создан, Key Lookup устранён | Индекс создан, но `INCLUDE` не использован | Покрытие не достигнуто |
| **Задание 4: исследовательская задача** | `Сортировка` и Key Lookup исчезли, «Отсортировано: Да», анализ «132» | Ускорение есть, но анализ неполный | Задача не решена |
| **Ответы на вопросы** | Все ответы полные, с использованием терминов (RID, INCLUDE) | Ответы даны, но 1–2 — неполные | Ответы поверхностные |
| **Оформление отчёта** | Соблюдена структура, скриншоты чёткие, есть таблица сравнения | Есть мелкие нарушения | Отчёт неструктурирован |

> 💡 **Максимальный балл**: 5  
> ❗ **Без таблицы сравнения** — максимальная оценка не выше «4».

---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON7.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON8.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №7](../LESSONS/LESSON7.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №7](../LESSONS/LESSON8.MD) |

---
