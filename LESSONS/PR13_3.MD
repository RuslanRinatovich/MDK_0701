🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON14_3.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON15.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №13.3](../LESSONS/LESSON14_3.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №14](../LESSONS/LESSON15.MD) |

---

# **Практическая работа №13.3. Продвинутые техники создания админ-процедур**

## **📋 Общая информация**

| Параметр | Значение |
|----------|----------|
| **Время выполнения** | 90 минут |
| **Количество заданий** | 5 |
| **Вариантов** | 30 (по числу студентов в группе) |

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму 235**](https://forms.gle/ySj5fEaDgqArqCidA). [**Google-форму 237**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **22 января 2026 г.**

---

## **🎯 Цель работы**
Научиться создавать продвинутые админ-процедуры с использованием динамического SQL, обработки ошибок и временных таблиц.

---

## **📝 Определите свой вариант**

**Ваш номер в группе:** _____ (от 1 до 30)

**Ваш вариант = Ваш номер в группе**

---

## **📋 Технические требования**
1. Все процедуры создавайте в базе `master`
2. Используйте `SET NOCOUNT ON` в начале каждой процедуры
3. Добавляйте обработку ошибок TRY...CATCH
4. Тестируйте каждую процедуру с разными параметрами

---

## **Задание 1. Процедура с динамическим SQL (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_DynamicTask1`, которая использует динамический SQL для работы с таблицами.

### **Шаблон кода:**
```sql
-- Задание 1: Динамический SQL
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_DynamicTask1
    @TableName NVARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- ВАШ КОД ЗДЕСЬ
        -- Проверьте, существует ли таблица
        -- Используйте динамический SQL для запроса к таблице
        -- Покажите первые 5 записей из таблицы
        -- Используйте QUOTENAME для безопасности
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка: ' + ERROR_MESSAGE();
    END CATCH
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна: |
|---------|-------------------|
| **1-6** | Показать структуру таблицы (столбцы и типы данных) |
| **7-12** | Подсчитать количество записей в таблице |
| **13-18** | Показать первые 10 записей из таблицы |
| **19-24** | Показать столбцы таблицы с их типами данных |
| **25-30** | Показать количество строк и столбцов в таблице |

---

## **Задание 2. Процедура с обработкой ошибок (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_SafeTask2`, которая демонстрирует правильную обработку ошибок.

### **Шаблон кода:**
```sql
-- Задание 2: Обработка ошибок
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_SafeTask2
    @Value1 INT,
    @Value2 INT
AS
BEGIN
    SET NOCOUNT ON;
    
    DECLARE @Result DECIMAL(10,2);
    DECLARE @Operation VARCHAR(20) = 'деление';
    
    BEGIN TRY
        -- ВАШ КОД ЗДЕСЬ
        -- Выполните операцию, которая может вызвать ошибку
        -- Обработайте возможные ошибки с помощью TRY...CATCH
        -- Выведите понятное сообщение об ошибке
        
    END TRY
    BEGIN CATCH
        -- ВАШ КОД ОБРАБОТКИ ОШИБОК ЗДЕСЬ
        -- Используйте функции ERROR_* для получения информации
        -- Выведите понятное сообщение для пользователя
        
    END CATCH
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна выполнить: |
|---------|-----------------------------|
| **1-10** | Разделить @Value1 на @Value2, обработать деление на 0 |
| **11-20** | Преобразовать число в строку и обратно, обработать ошибки |
| **21-30** | Выполнить запрос к несуществующей таблице с обработкой ошибки |

---

## **Задание 3. Процедура с временной таблицей (20 минут)**

### **Ваша задача:**
Создать процедуру `usp_TempTask3`, которая использует временную таблицу для анализа данных.

### **Шаблон кода:**
```sql
-- Задание 3: Временная таблица
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_TempTask3
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- Создайте временную таблицу
        -- ВАШ КОД СОЗДАНИЯ ВРЕМЕННОЙ ТАБЛИЦЫ ЗДЕСЬ
        
        -- Заполните временную таблицу данными
        -- ВАШ КОД ЗАПОЛНЕНИЯ ЗДЕСЬ
        
        -- Проанализируйте данные во временной таблице
        -- ВАШ КОД АНАЛИЗА ЗДЕСЬ
        
        -- Выведите результаты
        -- ВАШ КОД ВЫВОДА РЕЗУЛЬТАТОВ ЗДЕСЬ
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка при работе с временной таблицей: ' + ERROR_MESSAGE();
    END TRY
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна собрать информацию о: |
|---------|----------------------------------------|
| **1-6** | Всех базах данных (имя, размер, таблицы) |
| **7-12** | Больших таблицах (> 1000 строк) в текущей базе |
| **13-18** | Активных пользовательских подключениях |
| **19-24** | Системных таблицах и их размерах |
| **25-30** | Размере всех баз данных на сервере |

---

## **Задание 4. Процедура с динамическим SQL и параметрами (15 минут)**

### **Ваша задача:**
Создать процедуру `usp_DynamicParamsTask4`, которая использует динамический SQL с параметрами.

### **Шаблон кода:**
```sql
-- Задание 4: Динамический SQL с параметрами
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_DynamicParamsTask4
    @DatabaseName NVARCHAR(100),
    @MinSizeMB INT = 0
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- ВАШ КОД ЗДЕСЬ
        -- Проверьте существование базы данных
        -- Создайте динамический SQL с параметром @MinSizeMB
        -- Используйте QUOTENAME для безопасности
        -- Покажите таблицы больше указанного размера
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка: ' + ERROR_MESSAGE();
    END CATCH
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | Процедура должна показать: |
|---------|---------------------------|
| **1-10** | Таблицы в указанной базе с размером больше @MinSizeMB |
| **11-20** | Количество таблиц в указанной базе с учетом размера |
| **21-30** | Статистику по таблицам в указанной базе |

---

## **Задание 5. Комплексная админ-процедура (15 минут)**

### **Ваша задача:**
Создать комплексную процедуру `usp_AdminTask5`, которая использует все изученные техники.

### **Шаблон кода:**
```sql
-- Задание 5: Комплексная процедура
-- Вариант: [ваш номер]
CREATE PROCEDURE usp_AdminTask5
    @CheckType NVARCHAR(20) = 'QUICK'
AS
BEGIN
    SET NOCOUNT ON;
    
    DECLARE @ErrorMessage NVARCHAR(4000);
    DECLARE @StartTime DATETIME = GETDATE();
    
    BEGIN TRY
        PRINT '=== НАЧАЛО ПРОВЕРКИ ===';
        PRINT 'Тип проверки: ' + @CheckType;
        PRINT 'Время начала: ' + CONVERT(VARCHAR, @StartTime, 108);
        PRINT '';
        
        -- ВАШ КОД ЗДЕСЬ
        -- Используйте изученные техники:
        -- 1. Динамический SQL (если нужно)
        -- 2. Обработку ошибок
        -- 3. Временные таблицы
        -- 4. Разную логику в зависимости от @CheckType
        
        PRINT '';
        PRINT 'Время выполнения: ' + 
              CAST(DATEDIFF(SECOND, @StartTime, GETDATE()) AS VARCHAR) + ' сек';
        PRINT '=== ПРОВЕРКА ЗАВЕРШЕНА ===';
        
    END TRY
    BEGIN CATCH
        SET @ErrorMessage = ERROR_MESSAGE();
        PRINT 'ОШИБКА В ПРОЦЕДУРЕ:';
        PRINT 'Сообщение: ' + @ErrorMessage;
        PRINT 'Код ошибки: ' + CAST(ERROR_NUMBER() AS VARCHAR);
    END CATCH
END
GO
```

### **Что должна делать процедура (по вариантам):**

| Вариант | При @CheckType = | Процедура должна проверить: |
|---------|------------------|----------------------------|
| **1-10** | 'QUICK' | Только список баз данных |
| | 'FULL' | Базы + таблицы в текущей базе |
| | 'DETAILED' | Все + активные подключения |
| **11-20** | 'BASIC' | Версию SQL и текущую базу |
| | 'MEMORY' | Использование памяти сервером |
| | 'DISK' | Свободное место на дисках |
| **21-30** | 'BACKUP' | Состояние резервных копий |
| | 'SECURITY' | Активные подключения |
| | 'PERFORMANCE' | Количество таблиц и их размер |

---

## **📊 Полный шаблон для выполнения работы**

```sql
-- ========================================
-- ПРАКТИЧЕСКАЯ РАБОТА 13.3
-- Студент: [Фамилия Имя]
-- Группа: [Ваша группа]
-- Вариант: [Ваш номер в группе]
-- Дата: [Дата выполнения]
-- ========================================

USE master;
GO

-- ========== ЗАДАНИЕ 1 ==========
PRINT '=== СОЗДАНИЕ usp_DynamicTask1 ===';
GO

CREATE PROCEDURE usp_DynamicTask1
    @TableName NVARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- КОД ЗАДАНИЯ 1
        -- Вариант: [ваш номер]
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка: ' + ERROR_MESSAGE();
    END CATCH
END
GO

-- ========== ЗАДАНИЕ 2 ==========
PRINT '=== СОЗДАНИЕ usp_SafeTask2 ===';
GO

CREATE PROCEDURE usp_SafeTask2
    @Value1 INT,
    @Value2 INT
AS
BEGIN
    SET NOCOUNT ON;
    
    DECLARE @Result DECIMAL(10,2);
    DECLARE @Operation VARCHAR(20) = 'деление';
    
    BEGIN TRY
        -- КОД ЗАДАНИЯ 2
        -- Вариант: [ваш номер]
        
    END TRY
    BEGIN CATCH
        -- КОД ОБРАБОТКИ ОШИБОК
        -- Вариант: [ваш номер]
        
    END CATCH
END
GO

-- ========== ЗАДАНИЕ 3 ==========
PRINT '=== СОЗДАНИЕ usp_TempTask3 ===';
GO

CREATE PROCEDURE usp_TempTask3
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- КОД ЗАДАНИЯ 3
        -- Вариант: [ваш номер]
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка при работе с временной таблицей: ' + ERROR_MESSAGE();
    END TRY
END
GO

-- ========== ЗАДАНИЕ 4 ==========
PRINT '=== СОЗДАНИЕ usp_DynamicParamsTask4 ===';
GO

CREATE PROCEDURE usp_DynamicParamsTask4
    @DatabaseName NVARCHAR(100),
    @MinSizeMB INT = 0
AS
BEGIN
    SET NOCOUNT ON;
    
    BEGIN TRY
        -- КОД ЗАДАНИЯ 4
        -- Вариант: [ваш номер]
        
    END TRY
    BEGIN CATCH
        PRINT 'Ошибка: ' + ERROR_MESSAGE();
    END CATCH
END
GO

-- ========== ЗАДАНИЕ 5 ==========
PRINT '=== СОЗДАНИЕ usp_AdminTask5 ===';
GO

CREATE PROCEDURE usp_AdminTask5
    @CheckType NVARCHAR(20) = 'QUICK'
AS
BEGIN
    SET NOCOUNT ON;
    
    DECLARE @ErrorMessage NVARCHAR(4000);
    DECLARE @StartTime DATETIME = GETDATE();
    
    BEGIN TRY
        PRINT '=== НАЧАЛО ПРОВЕРКИ ===';
        PRINT 'Тип проверки: ' + @CheckType;
        PRINT '';
        
        -- КОД ЗАДАНИЯ 5
        -- Вариант: [ваш номер]
        
        PRINT '';
        PRINT 'Время выполнения: ' + 
              CAST(DATEDIFF(SECOND, @StartTime, GETDATE()) AS VARCHAR) + ' сек';
        PRINT '=== ПРОВЕРКА ЗАВЕРШЕНА ===';
        
    END TRY
    BEGIN CATCH
        SET @ErrorMessage = ERROR_MESSAGE();
        PRINT 'ОШИБКА В ПРОЦЕДУРЕ:';
        PRINT 'Сообщение: ' + @ErrorMessage;
        PRINT 'Код ошибки: ' + CAST(ERROR_NUMBER() AS VARCHAR);
    END CATCH
END
GO

-- ========== ТЕСТИРОВАНИЕ ==========
PRINT '=== ТЕСТИРОВАНИЕ ВСЕХ ПРОЦЕДУР ===';
GO

-- Тест 1
PRINT 'Тест usp_DynamicTask1:';
EXEC usp_DynamicTask1 @TableName = 'sys.databases';
GO

-- Тест 2
PRINT 'Тест usp_SafeTask2:';
EXEC usp_SafeTask2 @Value1 = 100, @Value2 = 0;  -- Тест на ошибку
EXEC usp_SafeTask2 @Value1 = 100, @Value2 = 5;  -- Нормальный тест
GO

-- Тест 3
PRINT 'Тест usp_TempTask3:';
EXEC usp_TempTask3;
GO

-- Тест 4
PRINT 'Тест usp_DynamicParamsTask4:';
EXEC usp_DynamicParamsTask4 @DatabaseName = 'master', @MinSizeMB = 1;
GO

-- Тест 5
PRINT 'Тест usp_AdminTask5:';
EXEC usp_AdminTask5;
EXEC usp_AdminTask5 @CheckType = 'FULL';
GO

PRINT '=== ТЕСТИРОВАНИЕ ЗАВЕРШЕНО ===';
GO
```

---

## **📝 Что нужно сдать:**

### **1. SQL файл с кодом**
Имя файла: `Практика13.3_Фамилия_Группа.sql`

### **2. Скриншоты (5 штук)**
Для каждой процедуры скриншот создания и результатов тестирования.

### **3. Ответы на вопросы**
Добавьте в конец SQL файла:

```sql
-- ========== ОТВЕТЫ НА ВОПРОСЫ ==========
/*
1. Зачем использовать QUOTENAME() в динамическом SQL?

2. В чем разница между TRY...CATCH и обычной проверкой IF?

3. Когда лучше использовать временную таблицу, а когда табличную переменную?

4. Какие функции ERROR_* вы использовали и для чего?

5. Какая из созданных процедур самая полезная для админа и почему?
*/
```

---

## **✅ Критерии оценки**

| Задание | Баллы | Что проверяется |
|---------|-------|-----------------|
| **1. Динамический SQL** | 20 | Правильное использование, безопасность (QUOTENAME) |
| **2. Обработка ошибок** | 20 | TRY...CATCH, информативные сообщения |
| **3. Временные таблицы** | 20 | Создание, заполнение и использование |
| **4. Параметры в динамическом SQL** | 20 | Безопасная передача параметров |
| **5. Комплексная процедура** | 20 | Комбинирование всех техник |
| **Итого** | **100** | |

**Оценки:**
- **85-100** = 5 (Отлично)
- **70-84** = 4 (Хорошо)
- **50-69** = 3 (Удовлетворительно)
- **0-49** = 2 (Неудовлетворительно)

---

## **💡 Полезные подсказки:**

### **Для динамического SQL:**
```sql
-- Безопасный способ:
DECLARE @SQL NVARCHAR(MAX);
SET @SQL = 'SELECT * FROM ' + QUOTENAME(@TableName);
EXEC sp_executesql @SQL;

-- С параметрами:
DECLARE @Params NVARCHAR(MAX) = '@MinSize INT';
SET @SQL = 'SELECT * FROM Table WHERE Size > @MinSize';
EXEC sp_executesql @SQL, @Params, @MinSize = 100;
```

### **Для обработки ошибок:**
```sql
BEGIN TRY
    -- Опасный код
END TRY
BEGIN CATCH
    PRINT 'Ошибка: ' + ERROR_MESSAGE();
    PRINT 'Строка: ' + CAST(ERROR_LINE() AS VARCHAR);
END CATCH
```

### **Для временных таблиц:**
```sql
-- Создание
CREATE TABLE #TempInfo (
    Name NVARCHAR(100),
    Value INT
);

-- Заполнение
INSERT INTO #TempInfo VALUES ('Тест', 100);

-- Использование
SELECT * FROM #TempInfo;

-- Удаление
DROP TABLE #TempInfo;
```

### **Полезные системные таблицы:**
```sql
sys.databases      -- Все базы данных
sys.tables         -- Все таблицы
sys.columns        -- Все столбцы
sys.dm_exec_sessions  -- Активные сессии
sys.partitions     -- Информация о строках в таблицах
```

---

## **🎯 Советы для успеха:**

1. **Начинайте с простого** — сначала сделайте работающий прототип
2. **Тестируйте по частям** — сначала динамический SQL, потом добавляйте обработку ошибок
3. **Используйте PRINT для отладки** — чтобы видеть, что происходит
4. **Проверяйте крайние случаи** — NULL, пустые строки, отрицательные числа
5. **Комментируйте сложные участки** — чтобы не запутаться

---

## **🚀 Дополнительное задание:**

Если выполнили все задания, создайте процедуру `usp_MasterAdminCheck`, которая:
1. Принимает параметр с типом проверки
2. Использует динамический SQL для работы с разными базами
3. Собирает данные во временные таблицы
4. Обрабатывает все возможные ошибки
5. Формирует красивый отчет с рекомендациями

---

## **📞 Если возникли проблемы:**

1. Проверьте, что правильно указали свой вариант
2. Посмотрите примеры из лекции
3. Спросите у одногруппников с близкими вариантами
4. Обратитесь к преподавателю

---

**Удачи! Помните: хорошая админ-процедура — это безопасная, надежная и понятная!** 🛡️💻

---

### 🔙 📚 🔜 Навигация по курсу

🔙 📚 🔜 Навигация по курсу  
| [Предыдущее занятие](../LESSONS/LESSON14_3.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON15.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №13.3](../LESSONS/LESSON14_3.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №14](../LESSONS/LESSON15.MD) |
