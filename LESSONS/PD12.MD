| [Предыдущее занятие](../LESSONS/LESSON13.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON14.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №12](../LESSONS/LESSON13.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №13](../LESSONS/LESSON14.MD) |

---

# **Практическая работа №12. Мониторинг использования памяти и CPU**

**Цель:** Научиться отслеживать использование ресурсов сервера и анализировать, какие программы и запросы потребляют больше всего CPU и памяти.

**Продолжительность:** 2 академических часа  
**Оборудование:** SSMS, SQL Server

---

## **📊 Общий план работы**

**Сегодня мы научимся:**

1. **Смотреть загрузку процессора** — какие запросы "тормозят" сервер
2. **Контролировать использование памяти** — кто "съедает" всю оперативку  
3. **Создавать тестовую нагрузку** — чтобы было что мониторить
4. **Готовить данные** для настройки Resource Governor

**📋 Таблица для заполнения:**

| Задание | Что делаем | Что узнали | Проблемы | Что сделать |
|---------|------------|------------|----------|-------------|
| 1 | Смотрим CPU | | | |
| 2 | Смотрим память | | | |
| 3 | Создаем нагрузку | | | |
| 4 | Группируем нагрузки | | | |

---

## **Раздел 1. Смотрим использование CPU**

### **📋 Задание 1. Какие запросы грузят процессор?**

**Цель:** Найти самые "тяжелые" запросы на сервере.

**Шаг 1.1.** Смотрим текущие активные запросы:
```sql
-- Показываем все запущенные запросы
SELECT 
    session_id AS 'ID сессии',
    login_name AS 'Пользователь', 
    program_name AS 'Программа',
    cpu_time AS 'CPU (мс)',
    reads AS 'Чтения',
    writes AS 'Записи',
    status AS 'Статус'
FROM sys.dm_exec_sessions 
WHERE is_user_process = 1  -- Только пользовательские процессы
ORDER BY cpu_time DESC;
```

**Шаг 1.2.** Смотрим историю "тяжелых" запросов:
```sql
-- Самые ресурсоемкие запросы за все время
SELECT TOP 10
    total_worker_time/1000 AS 'Всего CPU (сек)',
    execution_count AS 'Кол-во выполнений',
    (total_worker_time/execution_count)/1000 AS 'Среднее CPU (сек)',
    SUBSTRING(text, 
        statement_start_offset/2 + 1,
        (CASE WHEN statement_end_offset = -1 
              THEN LEN(CONVERT(nvarchar(max), text)) * 2 
              ELSE statement_end_offset 
         END - statement_start_offset)/2) AS 'Текст запроса'
FROM sys.dm_exec_query_stats 
CROSS APPLY sys.dm_exec_sql_text(sql_handle) 
WHERE execution_count > 0
ORDER BY total_worker_time DESC;
```

**💡 Вопросы для анализа:**
- Какая программа использует больше всего CPU?
- Есть ли запросы, которые выполняются очень долго?
- Сколько в среднем времени занимает выполнение запросов?

**В отчёт:**
- Скриншот результатов
- Подпись: *Рис. 1. Анализ использования CPU*

---

## **Раздел 2. Смотрим использование памяти**

### **📋 Задание 2. Кто использует память?**

**Цель:** Узнать, какие процессы используют оперативную память.

**Шаг 2.1.** Общая информация о памяти:
```sql
-- Сколько памяти у сервера и сколько используется
SELECT 
    physical_memory_kb/1024 AS 'Память всего (МБ)',
    committed_kb/1024 AS 'Используется (МБ)',
    (committed_kb * 100.0 / physical_memory_kb) AS 'Процент использования'
FROM sys.dm_os_sys_info;
```

**Шаг 2.2.** Детали по использованию памяти:
```sql
-- Кто сколько памяти использует
SELECT 
    type AS 'Тип',
    name AS 'Название', 
    pages_kb/1024 AS 'Память (МБ)'
FROM sys.dm_os_memory_clerks 
WHERE pages_kb > 0
ORDER BY pages_kb DESC;
```

**Шаг 2.3.** Запросы, которым выделена память:
```sql
-- Запросы с выделенной памятью
SELECT 
    session_id AS 'ID сессии',
    requested_memory_kb/1024 AS 'Запрошено (МБ)',
    granted_memory_kb/1024 AS 'Выдано (МБ)',
    used_memory_kb/1024 AS 'Использовано (МБ)'
FROM sys.dm_exec_query_memory_grants 
ORDER BY granted_memory_kb DESC;
```

**💡 Вопросы для анализа:**
- Какой процент памяти используется?
- Какие компоненты SQL Server используют больше всего памяти?
- Есть ли запросы, которым выделено много памяти?

**В отчёт:**
- Скриншоты всех трех запросов
- Подпись: *Рис. 2. Анализ использования памяти*

---

## **Раздел 3. Создаем тестовую нагрузку**

### **📋 Задание 3. Создаем нагрузку для мониторинга**

**Цель:** Создать запросы, которые будут использовать CPU и память.

**Шаг 3.1.** Создаем базу данных для тестов (если нет):
```sql
-- Создаем тестовую базу
CREATE DATABASE TestWorkload;
GO

USE TestWorkload;
GO

-- Создаем тестовую таблицу
CREATE TABLE TestData (
    ID INT IDENTITY PRIMARY KEY,
    Name NVARCHAR(100),
    Value INT,
    CreatedDate DATETIME DEFAULT GETDATE()
);
GO

-- Заполняем данными
INSERT INTO TestData (Name, Value)
SELECT 
    'Record_' + CAST(ROW_NUMBER() OVER(ORDER BY (SELECT NULL)) AS NVARCHAR(10)),
    ABS(CHECKSUM(NEWID())) % 1000
FROM sys.objects a, sys.objects b;  -- Создаем много записей
```

**Шаг 3.2.** Запросы, которые используют CPU:
```sql
-- Запрос 1: Сортировка большого объема данных (использует CPU)
SELECT * 
FROM TestData 
ORDER BY Value DESC, Name ASC;

-- Запрос 2: Сложные вычисления (использует CPU)
SELECT 
    Name,
    Value,
    Value * 2 AS DoubleValue,
    SQRT(Value) AS SqrtValue,
    COUNT(*) OVER() AS TotalCount
FROM TestData 
WHERE Value > 100;
```

**Шаг 3.3.** Запросы, которые используют память:
```sql
-- Запрос 3: Группировка с агрегатами (использует память)
SELECT 
    LEFT(Name, 7) AS NameGroup,
    COUNT(*) AS RecordCount,
    AVG(Value) AS AvgValue,
    SUM(Value) AS TotalValue
FROM TestData 
GROUP BY LEFT(Name, 7)
ORDER BY TotalValue DESC;

-- Запрос 4: Оконные функции (используют память)
SELECT 
    ID,
    Name,
    Value,
    SUM(Value) OVER(ORDER BY ID) AS RunningTotal,
    AVG(Value) OVER(PARTITION BY LEFT(Name, 3)) AS GroupAvg
FROM TestData;
```

**В отчёт:**
- Скриншоты создания базы и таблиц
- Скриншоты выполнения нагрузочных запросов
- Подпись: *Рис. 3. Создание тестовой нагрузки*

---

## **Раздел 4. Мониторим нагрузку в реальном времени**

### **📋 Задание 4. Смотрим что происходит во время нагрузки**

**Цель:** Научиться мониторить сервер во время работы приложений.

**Шаг 4.1.** Запустите мониторинг во время выполнения запросов из задания 3:
```sql
-- Мониторинг в реальном времени
SELECT 
    GETDATE() AS 'Время проверки',
    s.session_id AS 'ID сессии',
    s.program_name AS 'Программа',
    r.cpu_time AS 'CPU (мс)',
    r.reads AS 'Прочитано',
    r.writes AS 'Записано',
    r.logical_reads AS 'Логич. чтения',
    mg.granted_memory_kb/1024 AS 'Память (МБ)'
FROM sys.dm_exec_sessions s
LEFT JOIN sys.dm_exec_requests r ON s.session_id = r.session_id
LEFT JOIN sys.dm_exec_query_memory_grants mg ON s.session_id = mg.session_id
WHERE s.is_user_process = 1
ORDER BY r.cpu_time DESC;
```

**Шаг 4.2.** Простой мониторинг нагрузки:
```sql
-- Быстрая проверка нагрузки
SELECT 
    'CPU нагрузка' AS Метрика,
    (SELECT COUNT(*) FROM sys.dm_os_schedulers WHERE runnable_tasks_count > 0) AS Занято_планировщиков
UNION ALL
SELECT 
    'Память', 
    (SELECT COUNT(*) FROM sys.dm_exec_query_memory_grants)
UNION ALL  
SELECT
    'Активные запросы',
    (SELECT COUNT(*) FROM sys.dm_exec_requests WHERE session_id > 50);
```

**💡 Что смотрим:**
- Увеличилась ли нагрузка на CPU?
- Появились ли запросы с выделенной памятью?
- Какие программы сейчас активны?

**В отчёт:**
- Скриншоты мониторинга во время нагрузки
- Подпись: *Рис. 4. Мониторинг во время тестовой нагрузки*

---

## **Раздел 5. Группируем нагрузки для Resource Governor**

### **📋 Задание 5. Анализируем кто что делает**

**Цель:** Сгруппировать нагрузки по типам для будущей настройки Resource Governor.

**Шаг 5.1.** Анализируем программы и их нагрузку:
```sql
-- Группируем по программам
SELECT 
    program_name AS 'Программа',
    COUNT(*) AS 'Кол-во сессий',
    SUM(cpu_time) AS 'Всего CPU (мс)',
    AVG(cpu_time) AS 'Среднее CPU (мс)',
    SUM(reads) AS 'Всего чтений',
    SUM(writes) AS 'Всего записей'
FROM sys.dm_exec_sessions 
WHERE is_user_process = 1
AND program_name IS NOT NULL
GROUP BY program_name
ORDER BY SUM(cpu_time) DESC;
```

**Шаг 5.2.** Предлагаем группы для Resource Governor:
```sql
-- Предлагаем группы на основе анализа
SELECT 
    CASE 
        WHEN program_name LIKE '%Report%' THEN 'Отчеты'
        WHEN program_name LIKE '%Management%' THEN 'Администрирование' 
        WHEN program_name LIKE '%Service%' THEN 'Службы'
        WHEN program_name IS NULL THEN 'Неизвестно'
        ELSE 'Приложения'
    END AS 'Тип нагрузки',
    COUNT(*) AS 'Сессии',
    SUM(cpu_time) AS 'CPU (мс)',
    SUM(reads) AS 'Чтения'
FROM sys.dm_exec_sessions
WHERE is_user_process = 1
GROUP BY CASE 
    WHEN program_name LIKE '%Report%' THEN 'Отчеты'
    WHEN program_name LIKE '%Management%' THEN 'Администрирование' 
    WHEN program_name LIKE '%Service%' THEN 'Службы'
    WHEN program_name IS NULL THEN 'Неизвестно'
    ELSE 'Приложения'
END
ORDER BY SUM(cpu_time) DESC;
```

**Шаг 5.3.** Простые рекомендации по настройке:
```sql
-- Рекомендации по настройке Resource Governor
SELECT 
    'Отчеты' AS Группа,
    'MAX_CPU_PERCENT = 30' AS Рекомендация,
    'Чтобы отчеты не мешали основным приложениям' AS Объяснение
UNION ALL
SELECT 
    'Основные приложения', 
    'MIN_CPU_PERCENT = 50',
    'Гарантировать работу бизнес-приложений'
UNION ALL  
SELECT
    'Администрирование',
    'IMPORTANCE = HIGH', 
    'Важные системные задачи выполняются первыми';
```

**В отчёт:**
- Скриншоты анализа и рекомендаций
- Подпись: *Рис. 5. Группировка нагрузок для Resource Governor*

---

## **Раздел 6. Создаем простой инструмент мониторинга**

### **📋 Задание 6. Делаем свою процедуру мониторинга**

**Цель:** Создать простую процедуру для быстрой проверки состояния сервера.

**Шаг 6.1.** Создаем процедуру мониторинга:
```sql
-- Создаем процедуру для быстрого мониторинга
CREATE PROCEDURE SimpleResourceMonitor
AS
BEGIN
    PRINT '=== МОНИТОРИНГ РЕСУРСОВ SQL SERVER ===';
    PRINT 'Время: ' + CONVERT(VARCHAR, GETDATE(), 120);
    PRINT '';
    
    -- Проверка CPU
    DECLARE @BusySchedulers INT;
    SELECT @BusySchedulers = COUNT(*) 
    FROM sys.dm_os_schedulers 
    WHERE status = 'VISIBLE ONLINE' AND runnable_tasks_count > 0;
    
    PRINT 'Занято планировщиков CPU: ' + CAST(@BusySchedulers AS VARCHAR);
    
    -- Проверка памяти  
    DECLARE @MemoryGrants INT;
    SELECT @MemoryGrants = COUNT(*) 
    FROM sys.dm_exec_query_memory_grants;
    
    PRINT 'Активных выделений памяти: ' + CAST(@MemoryGrants AS VARCHAR);
    
    -- Активные запросы
    DECLARE @ActiveRequests INT;
    SELECT @ActiveRequests = COUNT(*) 
    FROM sys.dm_exec_requests 
    WHERE session_id > 50;
    
    PRINT 'Активных запросов: ' + CAST(@ActiveRequests AS VARCHAR);
    PRINT '';
    
    -- Топ-3 по CPU
    PRINT 'Топ-3 по использованию CPU:';
    SELECT TOP 3
        session_id AS 'ID',
        program_name AS 'Программа', 
        cpu_time AS 'CPU (мс)'
    FROM sys.dm_exec_sessions 
    WHERE is_user_process = 1
    ORDER BY cpu_time DESC;
    
    PRINT '';
    PRINT '=== МОНИТОРИНГ ЗАВЕРШЕН ===';
END
GO
```

**Шаг 6.2.** Тестируем нашу процедуру:
```sql
-- Запускаем мониторинг
EXEC SimpleResourceMonitor;
```

**В отчёт:**
- Текст созданной процедуры
- Скриншот результата выполнения
- Подпись: *Рис. 6. Процедура простого мониторинга*

---

## **Раздел 7. Анализ и выводы**

### **📋 Задание 7. Отвечаем на вопросы**

**На основе выполненной работы ответьте:**

1. **Какие программы или запросы использовали больше всего CPU на вашем сервере? Почему?**

2. **Какой компонент SQL Server использовал больше всего памяти? Нормально ли это?**

3. **Удалось ли создать нагрузку на сервер? Какие запросы были самыми "тяжелыми"?**

4. **На какие группы вы бы разделили нагрузки для Resource Governor? Почему именно так?**

5. **Как созданная процедура мониторинга может помочь в работе?**

---

## **Требования к оформлению отчёта**

**Файл:** `Практическая12_Фамилия.docx` (например, `Практическая12_Иванов.docx`)

**Что должно быть в отчёте:**
1. **Титульный лист** (ФИО, группа, № работы, дата)
2. **Цель работы**
3. **Выполнение заданий:**
   - Скриншоты всех 6 заданий с подписями
   - Заполненная таблица в начале
   - Тексты запросов которые вы писали
4. **Ответы на вопросы** из задания 7
5. **Вывод:** что узнали, что было сложно, как пригодится

---

## **Критерии оценивания**

| Что проверяем | На "5" | На "4" | На "3" |
|---------------|--------|--------|--------|
| **Задания 1-2** | Все запросы работают, анализ есть | Запросы работают, анализ поверхностный | Запросы есть, анализа нет |
| **Задания 3-4** | Нагрузка создана, мониторинг работает | Нагрузка есть, мониторинг частичный | Только создание нагрузки |
| **Задания 5-6** | Группировка и процедура работают | Группировка есть, процедура простая | Только группировка |
| **Анализ** | Полные ответы на все вопросы | Ответы на большинство вопросов | Ответы на некоторые вопросы |

**📤 Сдача работы:**
1. Оформите отчёт в Microsoft Word.  
2. Сохраните файл в формате `.docx`.  
3. Перейдите по ссылке на [**Google-форму**](https://forms.gle/rvDMNEzms2Pquaet6).  
4. Укажите ФИО и группу.  
5. Загрузите файл отчёта.  
6. **Срок сдачи**: до 23:59 **29 декабря 2025 г.**
---

### 🔙 📚 🔜 Навигация по курсу

| [Предыдущее занятие](../LESSONS/LESSON13.MD) | &nbsp; | [Следующее занятие](../LESSONS/LESSON14.MD) |
|:--------------------------------------:|:------:|:-------------------------------------:|
| 🏠 [Лекция №12](../LESSONS/LESSON13.MD) | 📖 [Содержание](../README.MD) | 💻 [Лекция №13](../LESSONS/LESSON14.MD) |
